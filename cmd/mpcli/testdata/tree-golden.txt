=== tdns-mpcli
use: tdns-mpcli
short: tdns-mpcli is the CLI tool for tdns multi-provider applications
flags: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent
use: agent
short: TDNS Agent commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent config
use: config
short: Commands to reload config, reload zones, etc
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent config reload
use: reload
short: Send config reload command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent config reload-zones
use: reload-zones
short: Send reload-zones command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent config status
use: status
short: Send config status command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent daemon
use: daemon
short: Only useful via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent daemon api
use: api
short: request a statusd api summary
long: |
  The daemon api queries the statusd for the provided API
  and prints that out in a (hopefully) comprehensible fashion.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent daemon reload
use: reload
short: Reload config from file
long: |
  Reload config from file (the assumption is that something in the config has changed).
  Right now this doesn't do much, but later on various services will be able to restart.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent daemon restart
use: restart
short: Stop and then start the management daemon
flags: |
      --clear string   Truncate the specified log file before starting
      --update         Update the server binary from /tmp/{binary} to /usr/local/libexec/ before starting
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent daemon start
use: start
short: Start the axfr-statusd daemon
long: |
  Start the axfr-statusd daemon. If it was already running, then this is a no-op.
flags: |
  -W, --maxwait int   Max seconds to wait until declaring start to have failed (default 5)
      --slurp         Slurp stdout/stderr for errors (debug tool only)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent daemon status
use: status
short: Query for the status of the management daemon
long: |
  Query for the status of the management daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent daemon stop
use: stop
short: Stop the management daemon
long: |
  Stop the management daemon. If it was not running, then this is a no-op.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug
use: debug
short: Debug commands against the configured daemon
flags: |
      --qname string   qname of rrset to examine
      --qtype string   qtype of rrset to examine
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug agentregistry
use: agentregistry
short: Test the agent registry
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug dump-agentregistry
use: dump-agentregistry
short: Dump the agent registry
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug dump-zonedatarepo
use: dump-zonedatarepo
short: Dump the zone data repo (deprecated: use show-synced-data)
hidden: true
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug lav
use: lav
short: Lookup and validate a child RRset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug queue-status
use: queue-status
short: Show reliable message queue status and pending messages
flags: |
  -v, --verbose   Verbose output (show full details for each message)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -z, --zone string     zone name

=== tdns-mpcli agent debug resync
use: resync
short: Re-send all local changes to combiner and remote agents
long: |
  Re-send all locally stored synced data for a zone to the combiner and
  all remote agents. Use this when the combiner or remote agents have lost
  state and need to be brought back in sync.
  
  Example:
    tdns-cliv2 agent debug resync --zone whisky.dnslab.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug rrset
use: rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug send-notify
use: send-notify
short: Tell agent to send a NOTIFY message to the other agents
flags: |
      --RR string       DNS record to send
  -I, --id string       agent identity to claim
  -R, --rrtype string   RR type sent notify for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug send-rfi
use: send-rfi
short: Tell agent to send an RFI message to another agent
flags: |
  -I, --id string        agent identity to claim
      --rfi string       RFI type (CONFIG|SYNC|AUDIT|EDITS)
      --subtype string   RFI subtype for CONFIG (upstream|downstream|sig0key)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug send-sync-to
use: send-sync-to <RR> [<RR>...]
short: Send a SYNC message to a remote agent (real transport)
long: |
  Create and send a real SYNC message to a specified remote agent.
  Uses the actual transport (CHUNK NOTIFY + fallback).
  RRs are validated before sending.
  
  Example:
    tdns-cliv2 debug agent send-sync-to \
      --to agent.provider-b.example.com. \
      --zone example.com. \
      "example.com. 3600 IN NS ns1.provider-a.example.com." \
      "example.com. 3600 IN NS ns2.provider-a.example.com."
flags: |
      --to string     Target agent ID
      --zone string   Zone name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output

=== tdns-mpcli agent debug show-combiner-data
use: show-combiner-data
short: Show combiner's local modifications store
long: |
  Display the combiner's stored local modifications that are applied to zones.
  Data is sorted by: zone → RRtype → RRs
  
  Example:
    tdns-cliv2 debug agent show-combiner-data
    tdns-cliv2 debug agent show-combiner-data --zone example.com
flags: |
      --zone string   Filter by specific zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output

=== tdns-mpcli agent debug show-key-inventory
use: show-key-inventory
short: Show DNSKEY inventory received from signer (KEYSTATE)
long: |
  Display the last KEYSTATE inventory received from the signer for a zone.
  Shows all keys reported by the signer's KeyDB with their state
  (created, published, standby, active, retired, foreign).
  
  Keys marked "foreign" are from other providers' signers.
  
  Example:
    tdns-cliv2 agent debug show-key-inventory --zone whisky.dnslab.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug show-rrsetcache
use: show-rrsetcache
short: List cached RRsets
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug show-synced-data
use: show-synced-data
short: Show synchronized data from peer agents (moved to: agent zone edits list)
flags: |
      --zone string   Filter by specific zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output

=== tdns-mpcli agent debug show-ta
use: show-ta
short: List known DNSSEC trust anchors
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug sig0
use: sig0
short: SIG(0) key helper commands (local)
flags: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
  -r, --rrtype string      rrtype to use for SIG(0)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug sig0 generate
use: generate
inherited: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
      --config string      config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug              debug output
  -H, --headers            show headers
  -Z, --pzone string       parent zone name
      --qname string       qname of rrset to examine
      --qtype string       qtype of rrset to examine
  -r, --rrtype string      rrtype to use for SIG(0)
  -v, --verbose            verbose output
  -z, --zone string        zone name

=== tdns-mpcli agent debug sync-state
use: sync-state
short: Show sync state for a zone
long: |
  Display the current synchronization state for a zone.
  
  Example:
    tdns-cliv2 debug agent sync-state --zone example.com
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug validate-rrset
use: validate-rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent discover
use: discover <agent-identity>
short: Trigger DNS discovery for a remote agent
long: |
  Discover a remote agent by querying DNS for its URI, JWK, TLSA, and SVCB records.
  This triggers async discovery which will:
  1. Query DNS for agent metadata (URI, JWK, TLSA, SVCB)
  2. Register the agent in PeerRegistry
  3. Start Hello retry loop if authorized
  4. Progress agent through state machine to OPERATIONAL
  
  Example:
    tdns-cli agent discover agent2.example.com
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent distrib
use: distrib
short: Manage distributions
long: |
  Commands for managing distributions created by this agent, including listing distributions and purging completed distributions.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent distrib discover
use: discover <agent-identity>
short: Discover and register agent contact information via DNS
long: |
  Performs DNS-based discovery of an agent's contact information and registers it.
  
  Looks up URI, KEY, TLSA, and A/AAAA records to discover:
    - API endpoint (_https._tcp.<identity> URI)
    - DNS endpoint (_dns._udp.<identity> URI)
    - Public key (<identity> KEY)
    - TLS certificate (_443._tcp.<identity> TLSA)
    - IP addresses (<identity> A/AAAA)
  
  After discovery, the agent is registered in PeerRegistry and can be used
  with 'distrib op' commands.
  
  Example:
    tdns-cliv2 agent distrib discover agent.delta.dnslab.
    tdns-cliv2 agent distrib discover provider.example.com
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent distrib list
use: list
short: List all distributions from this agent
flags: |
  -v, --verbose   Verbose output (show full details)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli agent distrib op
use: op [operation]
short: Run an operation toward a peer
long: |
  Run an operation toward a peer by identity (e.g. ping to combiner or a peer agent). Use "distrib peers" to list identities. Supported operations: ping (use --dns or --api for combiner ping).
flags: |
      --api         For ping: use HTTPS API ping
      --dns         For ping: use CHUNK-based DNS ping (default)
  -t, --to string   Recipient identity (e.g. combiner, agent.delta.dnslab.)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent distrib peers
use: peers
short: List all known peer agents with working keys
long: |
  Show all recipient agents that this agent has established keys with and can send distributions to.
flags: |
  -v, --verbose   Verbose output (show full details)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli agent distrib purge
use: purge [--force]
short: Delete distributions
long: |
  Delete distributions from the database. By default, only completed distributions are deleted. Use --force to delete ALL distributions regardless of status.
flags: |
      --force   Delete ALL distributions (not just completed ones)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent gossip
use: gossip
short: Gossip protocol commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent gossip state
use: state
short: Show gossip state matrix for a zone
long: |
  Display the N×N state matrix for the provider group serving a zone.
  Each row is a reporting peer; each column shows that reporter's
  view of another peer's state. A healthy group shows OPERATIONAL
  in every non-diagonal cell.
flags: |
  -z, --zone string   Zone name (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent hsync
use: hsync
short: HSYNC state-reporting commands
long: |
  Query HSYNC state stored in the agent's KeyDB and AgentRegistry.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync agentstatus
use: agentstatus
short: Show HSYNC status for an agent
flags: |
      --agentid string   Remote agent identity to show
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync confirmations
use: confirmations
short: Query HSYNC confirmations
long: |
  Display confirmation records for sync operations.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync events
use: events
short: Show HSYNC transport events
long: |
  Display recent transport events for debugging connectivity issues.
flags: |
  -n, --limit int     Maximum number of events to show (default 100)
  -p, --peer string   Filter by peer ID
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync locate
use: locate <agent-identity>
short: Locate and attempt to contact a remote agent
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync metrics
use: metrics
short: Show HSYNC operational metrics
long: |
  Display aggregated operational metrics for HSYNC operations.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync peers
use: peers
short: Show HSYNC peer status from database
long: |
  Display the status of HSYNC peers stored in the database.
  Shows peer state, transport details, and heartbeat statistics.
flags: |
  -p, --peer string   Filter by peer ID
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync query
use: query
short: Query HSYNC RRset for a zone via DNS
flags: |
      --resolver string   Resolver address for DNS query (e.g. 8.8.8.8:53)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync send-hello
use: send-hello
short: Send a hello message to a remote agent
long: |
  Send a hello message to a remote agent via the running agent server.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync sync-ops
use: sync-ops
short: Show HSYNC sync operations
long: |
  Display sync operations tracked in the database.
  Shows operation details, status, and timestamps.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync zonestatus
use: zonestatus
short: Show HSYNC status for a zone
flags: |
  -T, --transport string   Transport to show, default both api and dns
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent imr
use: imr
short: IMR (Internal Recursive Resolver) cache commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent imr dump-tuning
use: dump-tuning
short: Show effective IMR tuning values (backoff policy, family, discovery, etc.)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent imr dump-zone-backoffs
use: dump-zone-backoffs [zone]
short: Show zone-scoped lame-delegation backoffs (per zone, per address)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent imr flush
use: flush <qname>
short: Flush IMR cache entries at and below qname
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent imr query
use: query <qname> <qtype>
short: Query the IMR cache (cache-only, no external queries)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent imr reset
use: reset
short: Flush entire IMR cache and re-prime (preserves root NS)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent imr show
use: show
short: Show IMR cache entries related to agent discovery
flags: |
      --id string   Agent identity to show cache entries for (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keys
use: keys
short: JOSE keypair for secure CHUNK (generate, show)
long: |
  Generate a JOSE keypair or display the public key. Uses the server config file (agent or combiner) to get long_term_jose_priv_key path, or --server-config.
flags: |
      --server-config string   path to agent/combiner config file (overrides apiservers.*.config_file)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keys generate
use: generate
short: Generate JOSE keypair and write to config path or -output
flags: |
  -o, --output string   path for generated private key (overrides config)
inherited: |
      --config string          config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug                  debug output
  -H, --headers                show headers
  -Z, --pzone string           parent zone name
      --server-config string   path to agent/combiner config file (overrides apiservers.*.config_file)
  -v, --verbose                verbose output
  -z, --zone string            zone name

=== tdns-mpcli agent keys show
use: show
short: Print public key (JWK) from configured long_term_jose_priv_key
inherited: |
      --config string          config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug                  debug output
  -H, --headers                show headers
  -Z, --pzone string           parent zone name
      --server-config string   path to agent/combiner config file (overrides apiservers.*.config_file)
  -v, --verbose                verbose output
  -z, --zone string            zone name

=== tdns-mpcli agent keystore
use: keystore
short: Prefix command to access different features of the keystore
long: |
  The keystore holds SIG(0) and DNSSEC key pairs.
  The CLI contains functions for listing, adding, deleting, and
  changing the state of keys.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec
use: dnssec
short: Prefix command, only usable via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec add
use: add
short: Add a new DNSSEC key pair to the keystore
long: |
  Add a new SIG(0) key pair to the keystore. Required arguments are the name of the file
  containing either the private or the public SIG(0) key and the name of the zone.
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to add DNSSEC key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec algorithms
use: algorithms
short: List the DNSSEC algorithms the server supports
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec auto-rollover
use: auto-rollover
short: Manage and inspect automated KSK rollover (scheduled + manual-ASAP)
long: |
  Subcommands operate on local keystore state for a zone:
  
    when      — compute the earliest safe rollover moment (no state change)
    asap      — schedule a manual rollover at that earliest moment
    cancel    — clear a pending manual rollover request
    status    — print phase + per-key state for the zone
    reset     — clear last_rollover_error on one key after operator action
    unstick   — skip the softfail-delay and probe the parent on the next tick
    validate  — re-parse policy from YAML and report which §4 invariants pass/fail
flags: |
      --ksk   Render only the KSK section (status / when); ignored by other subcommands
      --zsk   Render only the ZSK section (status / when); ignored by other subcommands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec auto-rollover asap
use: asap
short: Schedule a manual KSK rollover at the earliest safe moment
long: |
  Asks the daemon to compute ComputeEarliestRollover and persist
  manual_rollover_* on the zone row. The rollover worker fires
  AtomicRollover when t_earliest is reached. Rejects the request if a
  rollover is already in progress or the pipeline has no standby SEP key.
  
  Online-only: scheduling against a stopped daemon is meaningless
  (the manual_rollover_* row would never be read).
flags: |
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli agent keystore dnssec auto-rollover cancel
use: cancel
short: Cancel a pending manual KSK rollover request
long: |
  Asks the daemon to clear manual_rollover_requested_at and
  manual_rollover_earliest on the zone row. Has no effect on rollovers
  that have already fired or on scheduled (lifetime-driven) rollovers.
  
  Online-only: cancelling against a stopped daemon is meaningless (the
  manual_rollover_* row isn't being read by anything).
flags: |
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli agent keystore dnssec auto-rollover reset
use: reset
short: Clear last_rollover_error for one key (after operator intervention)
long: |
  Asks the daemon to clear the last_rollover_error column on a single
  key's RolloverKeyState row. Use after diagnosing and fixing a
  hard-failed rollover so status output isn't misleading.
  
  Default mode talks to the daemon's API server. Use --offline to write
  directly to the keystore file when the daemon is down (postmortem
  use). The CLI checks the daemon-sentinel row in the keystore and
  refuses to run --offline if it sees a live daemon process; pass
  --force to override (you must ensure the daemon is genuinely
  stopped first).
flags: |
      --force         With --offline: override the daemon-alive check
      --keyid int     Key ID to reset (RFC 4034 keytag)
      --offline       Write directly to keystore file (postmortem use; daemon is down)
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli agent keystore dnssec auto-rollover status
use: status
short: Print rollover state for a zone (KSK and ZSK)
long: |
  Prints rollover state for the zone with an OK / ACTIVE / SOFTFAIL
  headline and per-key tables for KSKs and ZSKs.
  
  Default mode talks to the daemon's API server (no daemon config needed
  on the CLI host). Use --offline to render against the keystore file
  when the daemon is down — that requires --config with the daemon's
  config file so the CLI can find db.file and the zone's policy.
  
  Use --ksk or --zsk to print only the KSK block or only the ZSK block
  (the two flags are mutually exclusive). These flags are inherited
  from the auto-rollover parent and accepted by every subcommand for
  consistency.
  
  The DS range line lists SEP keyids (same numbering as the KSK table and
  as DS digest key tags at the parent).
  
  Use -v / --verbose to show rollover_index spans behind the keyid lists
  and the policy summary.
flags: |
      --offline       Render against keystore file (postmortem use; daemon is down)
  -v, --verbose       Show full last_error text and policy summary
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli agent keystore dnssec auto-rollover unstick
use: unstick
short: Skip the softfail-delay and probe the parent on the next tick
long: |
  Asks the daemon to clear next_push_at on the zone row so the
  rollover engine fires a probe UPDATE on its very next tick instead of
  waiting out the rest of the softfail-delay window. Operator override
  for "I just fixed the parent and want to retry now."
  
  Operationally optional: the engine polls the parent continuously
  regardless of softfail-delay, so a parent fix is auto-detected within
  confirm-poll-max even without 'unstick'. Use only to skip the wait.
  
  Default mode talks to the daemon's API server. Use --offline to write
  directly to the keystore file when the daemon is down (postmortem
  use). The CLI checks the daemon sentinel via refuseIfDaemonAlive
  and refuses to run if a live daemon is detected; --force overrides
  the check for cases where the sentinel is stale.
  
  Hardfail_count and last_softfail_* are preserved so status output
  still shows the most recent failure context. The counter resets to 0
  on the next successful confirmed observation.
  
  Differs from 'reset' (which clears last_rollover_error for one keyid).
flags: |
      --force         With --offline: override the daemon-alive check
      --offline       Write directly to keystore file (postmortem use; daemon is down)
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli agent keystore dnssec auto-rollover validate
use: validate
short: Validate a zone's rollover policy against §4 cache-flush invariants
long: |
  Re-parses the YAML config the daemon is running and runs every §4
  cache-flush invariant check (E5, E10, E11) against the dnssecpolicy
  attached to the named zone. Reports PASS / FAIL / WARN per invariant
  with operator-actionable suggestions.
  
  Online (default): contacts the daemon for its config-file path and the
  zone's active policy name, then reads the YAML directly. Useful for
  checking a candidate change before reload.
  
  Offline (--serverconfig PATH): skips the daemon and reads PATH. The
  zone's policy must be either supplied via --policy or inferable from
  the YAML's zones: block (when --zone matches a configured zone).
  
  DS_TTL handling: the runtime engine uses the parent's observed DS
  RRset TTL. validate doesn't have that observation, so it uses the
  ttls.parent-ds policy override if set, OR --parent-ds-ttl <duration> if
  supplied. Without either, E10/E11 are skipped (and the report says
  so).
flags: |
      --parent-ds-ttl string   Hypothetical parent DS RRset TTL (e.g. 1h) for E10/E11; overrides ttls.parent-ds
      --policy string          Override the dnssecpolicy name to validate (offline mode only)
      --serverconfig string    Read this YAML file instead of asking the daemon (offline)
  -z, --zone string            Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli agent keystore dnssec auto-rollover when
use: when
short: Compute the earliest moment a KSK rollover could safely fire (no state change)
long: |
  Asks the running daemon to compute ComputeEarliestRollover (§8.5) and
  prints the result. Side-effect free; does not request a rollover. Use
  'auto-rollover asap' to actually schedule one.
  
  Default mode talks to the daemon's API server (no daemon config needed
  on the CLI host). Use --offline to compute locally against the keystore
  file when the daemon is down — this requires --config with the daemon's
  config file so the CLI can find db.file and the zone's policy.
flags: |
      --offline       Compute locally against keystore file (postmortem use; daemon is down)
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli agent keystore dnssec clear
use: clear
short: Permanently delete all DNSSEC keys for a zone (KeyStateWorker will regenerate as needed)
flags: |
      --force         Skip confirmation prompt
  -z, --zone string   Zone to clear all DNSSEC keys for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec delete
use: delete
short: Delete DNSSEC key pair from TDNSD keystore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec ds-push
use: ds-push
short: Compute DS RRset from keystore and push to parent (UPDATE-only in this offline mode)
long: |
  Loads tdns config (same as other CLI commands using -c), opens the local keystore DB,
  and pushes the whole DS RRset to the parent. Requires imrengine in config.
  
  Offline mode: this CLI invocation builds a stub *ZoneData with no rollover policy
  attached, so PushDSRRsetForRollover falls through to the legacy single-scheme
  UPDATE path (whole-DS replacement, signed with the zone's active SIG(0) key).
  The auto / prefer-* / force-* dsync-scheme-preference values are honored only
  inside the daemon's rollover engine, where the policy is loaded from
  dnssecpolicies. To exercise NOTIFY pushes, run the daemon and let
  RolloverAutomatedTick drive the push.
  
  Use --dry-run to print the DS set and the UPDATE without sending.
flags: |
      --dry-run       Print DS RRset and UPDATE only; do not send
  -z, --zone string   Child zone (owner of DS RRset)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec export
use: export
short: Export a DNSSEC key pair from the keystore as BIND-style .private/.key files
long: |
  Write the DNSSEC key pair for (zone, keyid) to two files in BIND filename
  convention: K<zone>+<alg-num>+<keyid>.private (PKCS#8 PEM) and .key (zone-file
  DNSKEY RR). The resulting pair is directly consumable by 'keystore dnssec import'.
flags: |
      --keyid int       Key ID of key to export
  -o, --outdir string   Directory to write .private and .key files to (default ".")
  -z, --zone string     Zone the key belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec gen-ds
use: gen-ds
short: Generate DS records for a zone's KSK(s) from the keystore
long: |
  Generate DS (Delegation Signer) records for a zone's KSK (Key Signing Key) DNSKEY records stored in the keystore. The command queries the keystore for DNSKEY records for the specified zone, filters for KSKs (keys with the SEP bit set), and generates DS records using SHA-256 and SHA-384 digest algorithms. If --keyid is not specified, DS records are generated for all KSKs in the zone.
flags: |
      --keyid int     Key ID of specific KSK to generate DS for (optional, if not specified, generates for all KSKs)
  -z, --zone string   Zone to generate DS records for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec generate
use: generate
short: Generate a new DNSSEC key pair and add it to the keystore
flags: |
  -a, --algorithm string   Algorithm to use for DNSSEC key generation (use the 'algorithms' subcommand to list what the server supports)
      --keytype string     Key type to generate (KSK|ZSK|CSK)
      --state string       Inital key state (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec import
use: import
short: Add a new DNSSEC key pair to the keystore
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to import DNSSEC key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec list
use: list
short: List all DNSSEC key pairs in the keystore
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec policy
use: policy
short: DNSSEC policy utilities
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec policy validate
use: validate
short: Validate a YAML fragment with a top-level dnssecpolicies: block
long: |
  Reads a YAML file that contains the same dnssecpolicies: structure as tdns-auth
  config (policy names as keys, algorithm / ksk / zsk / csk / optional rollover+ttls+clamping).
  Runs the same validation as runtime config load. Exits non-zero on any error.
flags: |
  -f, --file string   YAML file path
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec purge
use: purge
short: Delete keys in 'removed' state, keeping the 3 most recent per zone
long: |
  Delete keys in 'removed' state from the keystore, keeping the 3
  most recent per zone (by insert order). Use --zone all to apply to
  every zone with removed keys at once.
  
  Dry-run by default: prints the keys that would be deleted and exits
  without modifying anything. Pass --force to actually delete.
flags: |
      --force         Actually delete; otherwise dry-run
  -z, --zone string   Zone to purge ('all' for every zone)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec query-parent
use: query-parent
short: Query configured parent-agent for child DS (poll until match or timeout)
long: |
  Uses rollover.parent-agent from the zone's dnssec policy (addr:port), or --parent-agent.
  Queries that address over TCP for the zone's DS RRset and compares to the keystore-derived
  expected set (ComputeTargetDSSetForZone, §7.5). Default poll schedule uses policy confirm-* timings,
  or 2s / 60s / 1h when those are unset.
  
  --once performs a single query and exits (no backoff loop).
flags: |
      --once                  Single TCP query; do not poll
      --parent-agent string   Override policy rollover.parent-agent (host:port)
  -z, --zone string           Child zone (owner of DS RRset)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec rollover
use: rollover
short: Perform a manual DNSSEC key rollover (standby→active, active→retired)
flags: |
      --keytype string   Key type to roll over (ZSK|KSK) (default "ZSK")
  -z, --zone string      Zone to perform rollover for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec setstate
use: setstate
short: Set the state of and existing DNSSEC key pair in the TDNSD keystore
flags: |
      --keyid int      Key ID of key to delete
      --state string   New statei of key
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore sig0
use: sig0
short: Prefix command, only usable via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore sig0 add
use: add
short: Add a new SIG(0) key pair to the keystore
long: |
  Add a new SIG(0) key pair to the keystore. Required arguments are the name of the file
  containing either the private or the public SIG(0) key and the name of the zone.
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to add SIG(0) key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore sig0 algorithms
use: algorithms
short: List the SIG(0) algorithms the server supports
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore sig0 delete
use: delete
short: Delete SIG(0) key pair from TDNSD keystore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore sig0 export
use: export
short: Export a SIG(0) key pair from the keystore as BIND-style .private/.key files
long: |
  Write the SIG(0) key pair for (zone, keyid) to two files in BIND filename
  convention: K<zone>+<alg-num>+<keyid>.private (PKCS#8 PEM) and .key (zone-file
  KEY RR). The resulting pair is directly consumable by commands accepting
  --key <basename.private>.
flags: |
      --keyid int       Key ID of key to export
  -o, --outdir string   Directory to write .private and .key files to (default ".")
  -z, --zone string     Zone the key belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore sig0 generate
use: generate
short: Generate a new SIG(0) key pair and add it to the keystore
flags: |
  -a, --algorithm string   Algorithm to use for SIG(0) key generation (use the 'algorithms' subcommand to list what the server supports)
      --state string       Inital key state (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore sig0 import
use: import
short: Add a new SIG(0) key pair to the keystore
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to import SIG(0) key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore sig0 list
use: list
short: List all SIG(0) key pairs in the keystore
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore sig0 setstate
use: setstate
short: Set the state of and existing SIG(0) key pair in the TDNSD keystore
flags: |
      --keyid int      Key ID of key to delete
      --state string   New state of key (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent local
use: local
short: TDNS Agent local commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent local config
use: config
short: Show details of the local agent config
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent local zonedata
use: zonedata
short: TDNS Agent local zone data commands (adding or removing local data about zones)
flags: |
      --RR string   DNS record to add
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent local zonedata add-rr
use: add-rr
short: Add a new local DNS record for an existing zone
inherited: |
      --RR string       DNS record to add
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent local zonedata remove-rr
use: remove-rr
short: Remove a local DNS record for an existing zone
inherited: |
      --RR string       DNS record to add
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent peer
use: peer
short: Peer agent commands (list, ping, zones, resync)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent peer apiping
use: apiping
short: Ping a peer via HTTPS API
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent peer list
use: list
short: List all known peer agents
long: |
  Show all peer agents that this agent has discovered and established communication with.
  Displays both API and DNS transports independently with their current state.
  
  This shows all peers regardless of transport type - both API (TLS) and DNS (JOSE) transports
  are displayed as separate entries to show their independent states.
flags: |
  -v, --verbose   Verbose output (show full details)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli agent peer ping
use: ping
short: Ping a peer via DNS CHUNK
long: |
  Send a DNS CHUNK ping to a peer and report the result.
  The --id flag specifies the peer identity (e.g. agent.beta.dnslab.
  or combiner.dnslab.).
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent peer reset
use: reset
short: Reset peer (agent/auditor only; no-op elsewhere)
long: |
  Reset a peer to initial NEEDED state. Flushes all IMR
  cache entries for the peer's discovery names and restarts
  discovery from scratch. Use this when a peer is stuck in UNKNOWN
  or KNOWN state. Only applicable to roles that use dynamic
  HSYNC3-driven discovery (agent, auditor); a no-op on
  static-peer roles (signer, combiner).
flags: |
      --id string   Peer identity to reset (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent peer resync
use: resync
short: Re-synchronize zone data with peers and combiner
long: |
  Re-synchronize zone data between this agent, its peers, and the combiner.
  
  Modes:
    --push   Re-send all local data to combiner and remote agents
    --pull   Request all remote agents to re-send their data (RFI SYNC)
    --full   Both push and pull (default if no flag specified)
flags: |
      --full   Both push and pull (default)
      --pull   Request peers to re-send their data (RFI SYNC)
      --push   Re-send local data to combiner and peers
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent peer zone
use: zone --zone <zonename>
short: List peer agents for a specific zone
long: |
  Show which peer agents share a specific zone with us.
  Displays all agents that have the specified zone in their shared zones list.
flags: |
  -z, --zone string   Zone name to list agents for (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent peer zones
use: zones
short: List shared zones for each peer agent
long: |
  Show which zones are shared with each peer agent.
  Displays agent identity and their shared zones in a compact format.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent ping
use: ping
short: Send an API ping request and present the response
flags: |
  -c, --count int   #pings to send
  -n, --newapi      use new api client
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent router
use: router
short: DNS message router introspection commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent router describe
use: describe
short: Show detailed router state
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent router list
use: list
short: List all registered message handlers
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent router metrics
use: metrics
short: Show router metrics (use --detailed for per-peer breakdown)
flags: |
      --detailed   Show per-peer breakdown
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent router reset
use: reset
short: Reset router metrics
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent router walk
use: walk
short: Walk all handlers with visitor pattern
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent stop
use: stop
short: Send stop command to the daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent transaction
use: transaction
short: Transaction diagnostics
long: |
  Commands for diagnosing open transactions, pending confirmations, and errors.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent transaction errors
use: errors
short: Show recent transaction errors (combiner only)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent transaction open
use: open
short: Show open transactions
long: |
  Show transactions that have not yet been confirmed.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent transaction open incoming
use: incoming
short: Show open incoming transactions (remote syncs awaiting combiner)
long: |
  Show incoming remote syncs that have been forwarded to the combiner but not yet confirmed.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent transaction open outgoing
use: outgoing
short: Show open outgoing transactions
long: |
  Show outgoing transactions that have not yet been confirmed by the receiver.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent truststore
use: truststore
short: Prefix command to access different features of the truststore
long: |
  The truststore is where SIG(0) public keys for child zones are kept.
  The CLI contains functions for listing trusted SIG(0) keys, adding and
  deleting child keys as well as changing the trust state of individual keys.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent truststore sig0
use: sig0
short: Prefix command, only usable via sub-commands
flags: |
  -c, --child string   Name of child SIG(0) key
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent truststore sig0 add
use: add
short: Add a new SIG(0) public key to the truststore
flags: |
  -s, --src string   Source for SIG(0) public key, a file name or 'dns'
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent truststore sig0 delete
use: delete
short: Delete a SIG(0) public key from the truststore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent truststore sig0 list
use: list
short: List all child SIG(0) public key in the keystore
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent truststore sig0 trust
use: trust
short: Declare a child SIG(0) public key in the keystore as trusted
flags: |
      --keyid int   Keyid of child SIG(0) key to change trust for
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent truststore sig0 untrust
use: untrust
short: Declare a child SIG(0) public key in the keystore as untrusted
flags: |
      --keyid int   Keyid of child SIG(0) key to change trust for
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone
use: zone
short: Agent zone management commands
flags: |
  -F, --force   force operation
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone addrr
use: addrr
short: Add a resource record to a zone and sync with peers and combiner
long: |
  Add a resource record to the specified zone. Supported RR types: NS, DNSKEY, CDS, CSYNC, KEY.
  The RR type is inferred from the --rr argument.
  
  This will:
  - Add the RR to the local synced data store
  - Send sync operations to all remote agents for this zone
  - Send the update to the combiner
  
  Examples:
    tdns-cliv2 agent zone addrr --zone whisky.dnslab. --rr "whisky.dnslab. IN NS ns.alpha.dnslab."
    tdns-cliv2 agent zone addrr --zone whisky.dnslab. --rr "whisky.dnslab. 300 IN DNSKEY 257 3 13 <base64>"
flags: |
      --force       Bypass dedup check and always send transaction
      --rr string   DNS record to add
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone bump
use: bump
short: Bump the SOA serial of an agent-served zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone delrr
use: delrr
short: Delete a resource record from a zone and sync with peers and combiner
long: |
  Delete a resource record from the specified zone. Supported RR types: NS, DNSKEY, CDS, CSYNC, KEY.
  The RR type is inferred from the --rr argument.
  
  This will:
  - Remove the RR from the local synced data store
  - Send sync operations to all remote agents for this zone
  - Send the update to the combiner
  
  Examples:
    tdns-cliv2 agent zone delrr --zone whisky.dnslab. --rr "whisky.dnslab. IN NS ns.alpha.dnslab."
    tdns-cliv2 agent zone delrr --zone whisky.dnslab. --rr "whisky.dnslab. 300 IN DNSKEY 257 3 13 <base64>"
flags: |
      --force       Bypass dedup check and always send transaction
      --rr string   DNS record to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone dsync
use: dsync
short: Prefix command, not useable by itself
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone dsync bootstrap-sig0-key
use: bootstrap-sig0-key
short: Send dsync bootstrap command to tdns-auth
flags: |
  -a, --algorithm string   Algorithm to use for the new SIG(0) key (default "ED25519")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone dsync publish
use: publish
short: Send dsync publish-dsync-rrset command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone dsync roll-sig0-key
use: roll-sig0-key
short: Send dsync rollover command to tdns-auth
flags: |
  -a, --algorithm string   Algorithm to use for the new SIG(0) key (default "ED25519")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone dsync status
use: status
short: Send dsync status command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone dsync unpublish
use: unpublish
short: Send dsync unpublish-dsync-rrset command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone edits
use: edits
short: Agent edit/sync status commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone edits list
use: list
short: Show synchronized data and tracking state
long: |
  Display the agent's SynchedDataEngine state showing contributions from all peer agents.
  
  Without --zone: summary view sorted by zone → source agent → RRtype → RRs
  With --zone:    detailed per-RR tracking state and outbound queue status
  
  Example:
    tdns-cliv2 agent zone edits list
    tdns-cliv2 agent zone edits list --zone example.com
flags: |
      --zone string   Show detailed status for a specific zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent zone list
use: list
short: List configured zones
flags: |
  -f, --file      Show zone input file
  -N, --notify    Show zone downstream notify addresses
  -P, --primary   Show zone primary nameserver
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone mplist
use: mplist
short: List multi-provider zones with HSYNCPARAM details
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone readfake
use: readfake
short: Create a fake zone from a compiled in string
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone reload
use: reload
short: Request re-loading a zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone update
use: update
short: Create and ultimately send a DNS UPDATE msg for zone auth data
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone update create
use: create
short: Create and ultimately send a DNS UPDATE msg for zone auth data
long: |
  Will query for details about the DNS UPDATE via (add|del|show|set-ttl) commands.
  When the message is complete it may be signed and sent by the 'send' command. After a
  message has been send the loop will start again with a new, empty message to create.
  Loop ends on the command "QUIT"
  
  The zone to update is mandatory to specify on the command line with the --zone flag.
flags: |
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent zone write
use: write
short: Write a zone to disk
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor
use: auditor
short: Interact with the MP auditor via API
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor config
use: config
short: Commands to reload config, reload zones, etc
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor config reload
use: reload
short: Send config reload command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor config reload-zones
use: reload-zones
short: Send reload-zones command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor config status
use: status
short: Send config status command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor daemon
use: daemon
short: Only useful via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor daemon api
use: api
short: request a statusd api summary
long: |
  The daemon api queries the statusd for the provided API
  and prints that out in a (hopefully) comprehensible fashion.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor daemon reload
use: reload
short: Reload config from file
long: |
  Reload config from file (the assumption is that something in the config has changed).
  Right now this doesn't do much, but later on various services will be able to restart.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor daemon restart
use: restart
short: Stop and then start the management daemon
flags: |
      --clear string   Truncate the specified log file before starting
      --update         Update the server binary from /tmp/{binary} to /usr/local/libexec/ before starting
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor daemon start
use: start
short: Start the axfr-statusd daemon
long: |
  Start the axfr-statusd daemon. If it was already running, then this is a no-op.
flags: |
  -W, --maxwait int   Max seconds to wait until declaring start to have failed (default 5)
      --slurp         Slurp stdout/stderr for errors (debug tool only)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor daemon status
use: status
short: Query for the status of the management daemon
long: |
  Query for the status of the management daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor daemon stop
use: stop
short: Stop the management daemon
long: |
  Stop the management daemon. If it was not running, then this is a no-op.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor debug
use: debug
short: Debug commands against the configured daemon
flags: |
      --qname string   qname of rrset to examine
      --qtype string   qtype of rrset to examine
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor debug lav
use: lav
short: Lookup and validate a child RRset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor debug rrset
use: rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor debug show-rrsetcache
use: show-rrsetcache
short: List cached RRsets
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor debug show-ta
use: show-ta
short: List known DNSSEC trust anchors
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor debug sig0
use: sig0
short: SIG(0) key helper commands (local)
flags: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
  -r, --rrtype string      rrtype to use for SIG(0)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor debug sig0 generate
use: generate
inherited: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
      --config string      config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug              debug output
  -H, --headers            show headers
  -Z, --pzone string       parent zone name
      --qname string       qname of rrset to examine
      --qtype string       qtype of rrset to examine
  -r, --rrtype string      rrtype to use for SIG(0)
  -v, --verbose            verbose output
  -z, --zone string        zone name

=== tdns-mpcli auditor debug validate-rrset
use: validate-rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor distrib
use: distrib
short: Manage distributions
long: |
  Commands for managing distributions created by this auditor, including listing distributions and purging completed distributions.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor distrib list
use: list
short: List all distributions from this auditor
flags: |
  -v, --verbose   Verbose output (show full details)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli auditor distrib peers
use: peers
short: List all known peer agents with working keys
long: |
  Show all recipient agents that this auditor has established keys with and can send distributions to.
flags: |
  -v, --verbose   Verbose output (show full details)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli auditor distrib purge
use: purge [--force]
short: Delete distributions
long: |
  Delete distributions from the database. By default, only completed distributions are deleted. Use --force to delete ALL distributions regardless of status.
flags: |
      --force   Delete ALL distributions (not just completed ones)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor eventlog
use: eventlog
short: Audit event log commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor eventlog clear
use: clear
short: Clear audit events
flags: |
      --all                 clear all events
      --older-than string   clear events older than duration (e.g. 24h)
  -z, --zone string         clear events for zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor eventlog list
use: list
short: List audit events
flags: |
      --last int       number of events to show (default 50)
      --since string   events since (RFC3339)
  -z, --zone string    filter by zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor gossip
use: gossip
short: Gossip protocol commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor gossip state
use: state
short: Show gossip state matrix for a zone
long: |
  Display the N×N state matrix for the provider group serving a zone.
  Each row is a reporting peer; each column shows that reporter's
  view of another peer's state. A healthy group shows OPERATIONAL
  in every non-diagonal cell.
flags: |
  -z, --zone string   Zone name (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore
use: keystore
short: Prefix command to access different features of the keystore
long: |
  The keystore holds SIG(0) and DNSSEC key pairs.
  The CLI contains functions for listing, adding, deleting, and
  changing the state of keys.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec
use: dnssec
short: Prefix command, only usable via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec add
use: add
short: Add a new DNSSEC key pair to the keystore
long: |
  Add a new SIG(0) key pair to the keystore. Required arguments are the name of the file
  containing either the private or the public SIG(0) key and the name of the zone.
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to add DNSSEC key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec algorithms
use: algorithms
short: List the DNSSEC algorithms the server supports
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec auto-rollover
use: auto-rollover
short: Manage and inspect automated KSK rollover (scheduled + manual-ASAP)
long: |
  Subcommands operate on local keystore state for a zone:
  
    when      — compute the earliest safe rollover moment (no state change)
    asap      — schedule a manual rollover at that earliest moment
    cancel    — clear a pending manual rollover request
    status    — print phase + per-key state for the zone
    reset     — clear last_rollover_error on one key after operator action
    unstick   — skip the softfail-delay and probe the parent on the next tick
    validate  — re-parse policy from YAML and report which §4 invariants pass/fail
flags: |
      --ksk   Render only the KSK section (status / when); ignored by other subcommands
      --zsk   Render only the ZSK section (status / when); ignored by other subcommands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec auto-rollover asap
use: asap
short: Schedule a manual KSK rollover at the earliest safe moment
long: |
  Asks the daemon to compute ComputeEarliestRollover and persist
  manual_rollover_* on the zone row. The rollover worker fires
  AtomicRollover when t_earliest is reached. Rejects the request if a
  rollover is already in progress or the pipeline has no standby SEP key.
  
  Online-only: scheduling against a stopped daemon is meaningless
  (the manual_rollover_* row would never be read).
flags: |
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli auditor keystore dnssec auto-rollover cancel
use: cancel
short: Cancel a pending manual KSK rollover request
long: |
  Asks the daemon to clear manual_rollover_requested_at and
  manual_rollover_earliest on the zone row. Has no effect on rollovers
  that have already fired or on scheduled (lifetime-driven) rollovers.
  
  Online-only: cancelling against a stopped daemon is meaningless (the
  manual_rollover_* row isn't being read by anything).
flags: |
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli auditor keystore dnssec auto-rollover reset
use: reset
short: Clear last_rollover_error for one key (after operator intervention)
long: |
  Asks the daemon to clear the last_rollover_error column on a single
  key's RolloverKeyState row. Use after diagnosing and fixing a
  hard-failed rollover so status output isn't misleading.
  
  Default mode talks to the daemon's API server. Use --offline to write
  directly to the keystore file when the daemon is down (postmortem
  use). The CLI checks the daemon-sentinel row in the keystore and
  refuses to run --offline if it sees a live daemon process; pass
  --force to override (you must ensure the daemon is genuinely
  stopped first).
flags: |
      --force         With --offline: override the daemon-alive check
      --keyid int     Key ID to reset (RFC 4034 keytag)
      --offline       Write directly to keystore file (postmortem use; daemon is down)
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli auditor keystore dnssec auto-rollover status
use: status
short: Print rollover state for a zone (KSK and ZSK)
long: |
  Prints rollover state for the zone with an OK / ACTIVE / SOFTFAIL
  headline and per-key tables for KSKs and ZSKs.
  
  Default mode talks to the daemon's API server (no daemon config needed
  on the CLI host). Use --offline to render against the keystore file
  when the daemon is down — that requires --config with the daemon's
  config file so the CLI can find db.file and the zone's policy.
  
  Use --ksk or --zsk to print only the KSK block or only the ZSK block
  (the two flags are mutually exclusive). These flags are inherited
  from the auto-rollover parent and accepted by every subcommand for
  consistency.
  
  The DS range line lists SEP keyids (same numbering as the KSK table and
  as DS digest key tags at the parent).
  
  Use -v / --verbose to show rollover_index spans behind the keyid lists
  and the policy summary.
flags: |
      --offline       Render against keystore file (postmortem use; daemon is down)
  -v, --verbose       Show full last_error text and policy summary
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli auditor keystore dnssec auto-rollover unstick
use: unstick
short: Skip the softfail-delay and probe the parent on the next tick
long: |
  Asks the daemon to clear next_push_at on the zone row so the
  rollover engine fires a probe UPDATE on its very next tick instead of
  waiting out the rest of the softfail-delay window. Operator override
  for "I just fixed the parent and want to retry now."
  
  Operationally optional: the engine polls the parent continuously
  regardless of softfail-delay, so a parent fix is auto-detected within
  confirm-poll-max even without 'unstick'. Use only to skip the wait.
  
  Default mode talks to the daemon's API server. Use --offline to write
  directly to the keystore file when the daemon is down (postmortem
  use). The CLI checks the daemon sentinel via refuseIfDaemonAlive
  and refuses to run if a live daemon is detected; --force overrides
  the check for cases where the sentinel is stale.
  
  Hardfail_count and last_softfail_* are preserved so status output
  still shows the most recent failure context. The counter resets to 0
  on the next successful confirmed observation.
  
  Differs from 'reset' (which clears last_rollover_error for one keyid).
flags: |
      --force         With --offline: override the daemon-alive check
      --offline       Write directly to keystore file (postmortem use; daemon is down)
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli auditor keystore dnssec auto-rollover validate
use: validate
short: Validate a zone's rollover policy against §4 cache-flush invariants
long: |
  Re-parses the YAML config the daemon is running and runs every §4
  cache-flush invariant check (E5, E10, E11) against the dnssecpolicy
  attached to the named zone. Reports PASS / FAIL / WARN per invariant
  with operator-actionable suggestions.
  
  Online (default): contacts the daemon for its config-file path and the
  zone's active policy name, then reads the YAML directly. Useful for
  checking a candidate change before reload.
  
  Offline (--serverconfig PATH): skips the daemon and reads PATH. The
  zone's policy must be either supplied via --policy or inferable from
  the YAML's zones: block (when --zone matches a configured zone).
  
  DS_TTL handling: the runtime engine uses the parent's observed DS
  RRset TTL. validate doesn't have that observation, so it uses the
  ttls.parent-ds policy override if set, OR --parent-ds-ttl <duration> if
  supplied. Without either, E10/E11 are skipped (and the report says
  so).
flags: |
      --parent-ds-ttl string   Hypothetical parent DS RRset TTL (e.g. 1h) for E10/E11; overrides ttls.parent-ds
      --policy string          Override the dnssecpolicy name to validate (offline mode only)
      --serverconfig string    Read this YAML file instead of asking the daemon (offline)
  -z, --zone string            Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli auditor keystore dnssec auto-rollover when
use: when
short: Compute the earliest moment a KSK rollover could safely fire (no state change)
long: |
  Asks the running daemon to compute ComputeEarliestRollover (§8.5) and
  prints the result. Side-effect free; does not request a rollover. Use
  'auto-rollover asap' to actually schedule one.
  
  Default mode talks to the daemon's API server (no daemon config needed
  on the CLI host). Use --offline to compute locally against the keystore
  file when the daemon is down — this requires --config with the daemon's
  config file so the CLI can find db.file and the zone's policy.
flags: |
      --offline       Compute locally against keystore file (postmortem use; daemon is down)
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli auditor keystore dnssec clear
use: clear
short: Permanently delete all DNSSEC keys for a zone (KeyStateWorker will regenerate as needed)
flags: |
      --force         Skip confirmation prompt
  -z, --zone string   Zone to clear all DNSSEC keys for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec delete
use: delete
short: Delete DNSSEC key pair from TDNSD keystore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec ds-push
use: ds-push
short: Compute DS RRset from keystore and push to parent (UPDATE-only in this offline mode)
long: |
  Loads tdns config (same as other CLI commands using -c), opens the local keystore DB,
  and pushes the whole DS RRset to the parent. Requires imrengine in config.
  
  Offline mode: this CLI invocation builds a stub *ZoneData with no rollover policy
  attached, so PushDSRRsetForRollover falls through to the legacy single-scheme
  UPDATE path (whole-DS replacement, signed with the zone's active SIG(0) key).
  The auto / prefer-* / force-* dsync-scheme-preference values are honored only
  inside the daemon's rollover engine, where the policy is loaded from
  dnssecpolicies. To exercise NOTIFY pushes, run the daemon and let
  RolloverAutomatedTick drive the push.
  
  Use --dry-run to print the DS set and the UPDATE without sending.
flags: |
      --dry-run       Print DS RRset and UPDATE only; do not send
  -z, --zone string   Child zone (owner of DS RRset)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec export
use: export
short: Export a DNSSEC key pair from the keystore as BIND-style .private/.key files
long: |
  Write the DNSSEC key pair for (zone, keyid) to two files in BIND filename
  convention: K<zone>+<alg-num>+<keyid>.private (PKCS#8 PEM) and .key (zone-file
  DNSKEY RR). The resulting pair is directly consumable by 'keystore dnssec import'.
flags: |
      --keyid int       Key ID of key to export
  -o, --outdir string   Directory to write .private and .key files to (default ".")
  -z, --zone string     Zone the key belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec gen-ds
use: gen-ds
short: Generate DS records for a zone's KSK(s) from the keystore
long: |
  Generate DS (Delegation Signer) records for a zone's KSK (Key Signing Key) DNSKEY records stored in the keystore. The command queries the keystore for DNSKEY records for the specified zone, filters for KSKs (keys with the SEP bit set), and generates DS records using SHA-256 and SHA-384 digest algorithms. If --keyid is not specified, DS records are generated for all KSKs in the zone.
flags: |
      --keyid int     Key ID of specific KSK to generate DS for (optional, if not specified, generates for all KSKs)
  -z, --zone string   Zone to generate DS records for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec generate
use: generate
short: Generate a new DNSSEC key pair and add it to the keystore
flags: |
  -a, --algorithm string   Algorithm to use for DNSSEC key generation (use the 'algorithms' subcommand to list what the server supports)
      --keytype string     Key type to generate (KSK|ZSK|CSK)
      --state string       Inital key state (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec import
use: import
short: Add a new DNSSEC key pair to the keystore
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to import DNSSEC key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec list
use: list
short: List all DNSSEC key pairs in the keystore
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec policy
use: policy
short: DNSSEC policy utilities
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec policy validate
use: validate
short: Validate a YAML fragment with a top-level dnssecpolicies: block
long: |
  Reads a YAML file that contains the same dnssecpolicies: structure as tdns-auth
  config (policy names as keys, algorithm / ksk / zsk / csk / optional rollover+ttls+clamping).
  Runs the same validation as runtime config load. Exits non-zero on any error.
flags: |
  -f, --file string   YAML file path
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec purge
use: purge
short: Delete keys in 'removed' state, keeping the 3 most recent per zone
long: |
  Delete keys in 'removed' state from the keystore, keeping the 3
  most recent per zone (by insert order). Use --zone all to apply to
  every zone with removed keys at once.
  
  Dry-run by default: prints the keys that would be deleted and exits
  without modifying anything. Pass --force to actually delete.
flags: |
      --force         Actually delete; otherwise dry-run
  -z, --zone string   Zone to purge ('all' for every zone)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec query-parent
use: query-parent
short: Query configured parent-agent for child DS (poll until match or timeout)
long: |
  Uses rollover.parent-agent from the zone's dnssec policy (addr:port), or --parent-agent.
  Queries that address over TCP for the zone's DS RRset and compares to the keystore-derived
  expected set (ComputeTargetDSSetForZone, §7.5). Default poll schedule uses policy confirm-* timings,
  or 2s / 60s / 1h when those are unset.
  
  --once performs a single query and exits (no backoff loop).
flags: |
      --once                  Single TCP query; do not poll
      --parent-agent string   Override policy rollover.parent-agent (host:port)
  -z, --zone string           Child zone (owner of DS RRset)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec rollover
use: rollover
short: Perform a manual DNSSEC key rollover (standby→active, active→retired)
flags: |
      --keytype string   Key type to roll over (ZSK|KSK) (default "ZSK")
  -z, --zone string      Zone to perform rollover for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec setstate
use: setstate
short: Set the state of and existing DNSSEC key pair in the TDNSD keystore
flags: |
      --keyid int      Key ID of key to delete
      --state string   New statei of key
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore sig0
use: sig0
short: Prefix command, only usable via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore sig0 add
use: add
short: Add a new SIG(0) key pair to the keystore
long: |
  Add a new SIG(0) key pair to the keystore. Required arguments are the name of the file
  containing either the private or the public SIG(0) key and the name of the zone.
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to add SIG(0) key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore sig0 algorithms
use: algorithms
short: List the SIG(0) algorithms the server supports
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore sig0 delete
use: delete
short: Delete SIG(0) key pair from TDNSD keystore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore sig0 export
use: export
short: Export a SIG(0) key pair from the keystore as BIND-style .private/.key files
long: |
  Write the SIG(0) key pair for (zone, keyid) to two files in BIND filename
  convention: K<zone>+<alg-num>+<keyid>.private (PKCS#8 PEM) and .key (zone-file
  KEY RR). The resulting pair is directly consumable by commands accepting
  --key <basename.private>.
flags: |
      --keyid int       Key ID of key to export
  -o, --outdir string   Directory to write .private and .key files to (default ".")
  -z, --zone string     Zone the key belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore sig0 generate
use: generate
short: Generate a new SIG(0) key pair and add it to the keystore
flags: |
  -a, --algorithm string   Algorithm to use for SIG(0) key generation (use the 'algorithms' subcommand to list what the server supports)
      --state string       Inital key state (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore sig0 import
use: import
short: Add a new SIG(0) key pair to the keystore
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to import SIG(0) key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore sig0 list
use: list
short: List all SIG(0) key pairs in the keystore
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore sig0 setstate
use: setstate
short: Set the state of and existing SIG(0) key pair in the TDNSD keystore
flags: |
      --keyid int      Key ID of key to delete
      --state string   New state of key (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor observations
use: observations
short: Show anomalies/observations detected by the auditor
flags: |
  -z, --zone string   filter by zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor peer
use: peer
short: Peer management commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor peer apiping
use: apiping
short: Ping a peer via HTTPS API
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor peer list
use: list
short: List all known peer agents
long: |
  Show all peer agents that this auditor has discovered and
  established communication with. Displays both API and DNS transports
  independently with their current state, plus leader-election status
  for every provider group the auditor observes.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor peer ping
use: ping
short: Ping a peer via DNS CHUNK
long: |
  Send a DNS CHUNK ping to a peer and report the result.
  The --id flag specifies the peer identity (e.g. agent.beta.dnslab.
  or combiner.dnslab.).
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor peer reset
use: reset
short: Reset peer (agent/auditor only; no-op elsewhere)
long: |
  Reset a peer to initial NEEDED state. Flushes all IMR
  cache entries for the peer's discovery names and restarts
  discovery from scratch. Use this when a peer is stuck in UNKNOWN
  or KNOWN state. Only applicable to roles that use dynamic
  HSYNC3-driven discovery (agent, auditor); a no-op on
  static-peer roles (signer, combiner).
flags: |
      --id string   Peer identity to reset (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor peer zones
use: zones
short: List shared zones for each peer agent
long: |
  Show which zones are shared with each peer agent.
  Displays agent identity and their shared zones in a compact format.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor ping
use: ping
short: Send an API ping request and present the response
flags: |
  -c, --count int   #pings to send
  -n, --newapi      use new api client
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor stop
use: stop
short: Send stop command to the daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor truststore
use: truststore
short: Prefix command to access different features of the truststore
long: |
  The truststore is where SIG(0) public keys for child zones are kept.
  The CLI contains functions for listing trusted SIG(0) keys, adding and
  deleting child keys as well as changing the trust state of individual keys.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor truststore sig0
use: sig0
short: Prefix command, only usable via sub-commands
flags: |
  -c, --child string   Name of child SIG(0) key
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor truststore sig0 add
use: add
short: Add a new SIG(0) public key to the truststore
flags: |
  -s, --src string   Source for SIG(0) public key, a file name or 'dns'
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor truststore sig0 delete
use: delete
short: Delete a SIG(0) public key from the truststore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor truststore sig0 list
use: list
short: List all child SIG(0) public key in the keystore
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor truststore sig0 trust
use: trust
short: Declare a child SIG(0) public key in the keystore as trusted
flags: |
      --keyid int   Keyid of child SIG(0) key to change trust for
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor truststore sig0 untrust
use: untrust
short: Declare a child SIG(0) public key in the keystore as untrusted
flags: |
      --keyid int   Keyid of child SIG(0) key to change trust for
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor web
use: web
short: Auditor web-dashboard commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor web user
use: user
short: Manage web dashboard users
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor web user create
use: create
short: Create a new web dashboard user (prompts for password)
flags: |
      --name string   username (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor web user delete
use: delete
short: Delete a web dashboard user
flags: |
      --name string   username (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor web user list
use: list
short: List configured web dashboard users
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor web user reload
use: reload
short: Tell the running auditor to re-read its users file
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone
use: zone
short: Prefix command, not usable by itself
flags: |
  -F, --force   force operation
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone bump
use: bump
short: Bump SOA serial and epoch (if any) in tdns-auth version of zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dsync
use: dsync
short: Prefix command, not useable by itself
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dsync bootstrap-sig0-key
use: bootstrap-sig0-key
short: Send dsync bootstrap command
flags: |
  -a, --algorithm string   Algorithm for the new SIG(0) key (use the 'algorithms' subcommand to list what the server supports)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dsync publish
use: publish
short: Send dsync publish-dsync-rrset command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dsync roll-sig0-key
use: roll-sig0-key
short: Send dsync rollover command
flags: |
  -a, --algorithm string   Algorithm for the new SIG(0) key (use the 'algorithms' subcommand to list what the server supports)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dsync status
use: status
short: Send dsync status command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dsync unpublish
use: unpublish
short: Send dsync unpublish-dsync-rrset command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone freeze
use: freeze
short: Freeze a zone (i.e. stop accepting DDNS updates to the zone data)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone list
use: list
short: List configured zones
flags: |
  -f, --file      Show zone input file
  -N, --notify    Show zone downstream notify addresses
  -P, --primary   Show zone primary nameserver
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone mplist
use: mplist
short: List multi-provider zones with HSYNCPARAM details
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone nsec
use: nsec
short: Prefix command, not usable by itself
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone nsec generate
use: generate
short: Generate NSEC records for a zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone nsec show
use: show
short: Show the NSEC chain for a zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone readfake
use: readfake
short: Create a fake zone from a compiled in string
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone reload
use: reload
short: Request re-loading a zone
flags: |
  -e, --error            wait for reload to complete and report any parse errors
      --timeout string   how long to wait for reload when --error is set (e.g. 10s, 2m) (default "10s")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone resign
use: resign
short: Re-sign zone from scratch with currently-active keys (drops all existing RRSIGs)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone sign
use: sign
short: Request signing of a zone (additive: cover gaps with active keys)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone thaw
use: thaw
short: Thaw a zone (i.e. accept DDNS updates to the zone data again)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone update
use: update
short: Create and ultimately send a DNS UPDATE msg for zone auth data
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone update create
use: create
short: Create and ultimately send a DNS UPDATE msg for zone auth data
long: |
  Will query for details about the DNS UPDATE via (add|del|show|set-ttl) commands.
  When the message is complete it may be signed and sent by the 'send' command. After a
  message has been send the loop will start again with a new, empty message to create.
  Loop ends on the command "QUIT"
  
  The zone to update is mandatory to specify on the command line with the --zone flag.
flags: |
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone write
use: write
short: Write a zone to disk
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zones
use: zones
short: List audited zones with provider summaries
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner
use: combiner
short: TDNS Combiner commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner add-data
use: add-data [file]
short: Add local data to a zone passing through the combiner
long: |
  Add local data to a zone passing through the combiner.
  
  Zone can be specified via --zone flag. The file should contain one RR per line.
  
  Example contents (for a zone named "example.com"):
    example.com. 86400 IN NS ns1.provider.com.
    example.com. 86400 IN NS ns2.service.net.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner config
use: config
short: Runtime introspection of the combiner's multi-provider config; config and zone reload
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner config reload
use: reload
short: Send config reload command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner config reload-zones
use: reload-zones
short: Send reload-zones command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner config status
use: status
short: Show the running combiner's effective multi-provider config
long: |
  Show the running combiner's effective multi-provider config.
  
  Use -v to include the agent-identity list, protected namespaces,
  sync-api listen addresses, and provider zones.
  
  Sensitive paths (private key locations, JOSE pubkey paths) are
  intentionally not exposed via this endpoint.
flags: |
  -v, --verbose   include agent identities, protected namespaces, listen addresses, provider zones
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli combiner daemon
use: daemon
short: Only useful via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner daemon api
use: api
short: request a statusd api summary
long: |
  The daemon api queries the statusd for the provided API
  and prints that out in a (hopefully) comprehensible fashion.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner daemon reload
use: reload
short: Reload config from file
long: |
  Reload config from file (the assumption is that something in the config has changed).
  Right now this doesn't do much, but later on various services will be able to restart.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner daemon restart
use: restart
short: Stop and then start the management daemon
flags: |
      --clear string   Truncate the specified log file before starting
      --update         Update the server binary from /tmp/{binary} to /usr/local/libexec/ before starting
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner daemon start
use: start
short: Start the axfr-statusd daemon
long: |
  Start the axfr-statusd daemon. If it was already running, then this is a no-op.
flags: |
  -W, --maxwait int   Max seconds to wait until declaring start to have failed (default 5)
      --slurp         Slurp stdout/stderr for errors (debug tool only)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner daemon status
use: status
short: Query for the status of the management daemon
long: |
  Query for the status of the management daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner daemon stop
use: stop
short: Stop the management daemon
long: |
  Stop the management daemon. If it was not running, then this is a no-op.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner debug
use: debug
short: Debug commands against the configured daemon
flags: |
      --qname string   qname of rrset to examine
      --qtype string   qtype of rrset to examine
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner debug lav
use: lav
short: Lookup and validate a child RRset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner debug rrset
use: rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner debug show-rrsetcache
use: show-rrsetcache
short: List cached RRsets
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner debug show-ta
use: show-ta
short: List known DNSSEC trust anchors
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner debug sig0
use: sig0
short: SIG(0) key helper commands (local)
flags: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
  -r, --rrtype string      rrtype to use for SIG(0)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner debug sig0 generate
use: generate
inherited: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
      --config string      config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug              debug output
  -H, --headers            show headers
  -Z, --pzone string       parent zone name
      --qname string       qname of rrset to examine
      --qtype string       qtype of rrset to examine
  -r, --rrtype string      rrtype to use for SIG(0)
  -v, --verbose            verbose output
  -z, --zone string        zone name

=== tdns-mpcli combiner debug validate-rrset
use: validate-rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner distrib
use: distrib
short: Manage distributions
long: |
  Commands for managing distributions created by this combiner, including listing distributions and purging completed distributions.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner distrib list
use: list
short: List all distributions from this combiner
flags: |
  -v, --verbose   Verbose output (show full details)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli combiner distrib peers
use: peers
short: List all known peer agents with working keys
long: |
  Show all recipient agents that this combiner has established keys with and can send distributions to.
flags: |
  -v, --verbose   Verbose output (show full details)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli combiner distrib purge
use: purge [--force]
short: Delete distributions
long: |
  Delete distributions from the database. By default, only completed distributions are deleted. Use --force to delete ALL distributions regardless of status.
flags: |
      --force   Delete ALL distributions (not just completed ones)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner gossip
use: gossip
short: Gossip protocol commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner gossip state
use: state
short: Show gossip state matrix for a zone
long: |
  Display the N×N state matrix for the provider group serving a zone.
  Each row is a reporting peer; each column shows that reporter's
  view of another peer's state. A healthy group shows OPERATIONAL
  in every non-diagonal cell.
flags: |
  -z, --zone string   Zone name (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner keys
use: keys
short: JOSE keypair for secure CHUNK (generate, show)
long: |
  Generate a JOSE keypair or display the public key. Uses the server config file (agent or combiner) to get long_term_jose_priv_key path, or --server-config.
flags: |
      --server-config string   path to agent/combiner config file (overrides apiservers.*.config_file)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner keys generate
use: generate
short: Generate JOSE keypair and write to config path or -output
flags: |
  -o, --output string   path for generated private key (overrides config)
inherited: |
      --config string          config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug                  debug output
  -H, --headers                show headers
  -Z, --pzone string           parent zone name
      --server-config string   path to agent/combiner config file (overrides apiservers.*.config_file)
  -v, --verbose                verbose output
  -z, --zone string            zone name

=== tdns-mpcli combiner keys show
use: show
short: Print public key (JWK) from configured long_term_jose_priv_key
inherited: |
      --config string          config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug                  debug output
  -H, --headers                show headers
  -Z, --pzone string           parent zone name
      --server-config string   path to agent/combiner config file (overrides apiservers.*.config_file)
  -v, --verbose                verbose output
  -z, --zone string            zone name

=== tdns-mpcli combiner list-data
use: list-data
short: List local data added to a zone in the combiner
long: |
  List local data added to a zone in the combiner. Zone can be specified via --zone flag.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner peer
use: peer
short: Commands to the combiner regarding peers
long: |
  Commands that instruct the combiner to perform an action toward a peer agent (e.g. ping, resync).
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner peer apiping
use: apiping
short: Ping a peer via HTTPS API
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner peer list
use: list
short: List all known peer agents
long: |
  Show all peer agents that this combiner knows about.
  Displays both DNS and API transports independently with their current state.
  
  Example:
    tdns-cliv2 combiner peer list
    tdns-cliv2 combiner peer list --verbose
flags: |
      --verbose   Show detailed per-peer statistics
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli combiner peer ping
use: ping
short: Ping a peer via DNS CHUNK
long: |
  Send a DNS CHUNK ping to a peer and report the result.
  The --id flag specifies the peer identity (e.g. agent.beta.dnslab.
  or combiner.dnslab.).
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner peer reset
use: reset
short: Reset peer (agent/auditor only; no-op elsewhere)
long: |
  Reset a peer to initial NEEDED state. Flushes all IMR
  cache entries for the peer's discovery names and restarts
  discovery from scratch. Use this when a peer is stuck in UNKNOWN
  or KNOWN state. Only applicable to roles that use dynamic
  HSYNC3-driven discovery (agent, auditor); a no-op on
  static-peer roles (signer, combiner).
flags: |
      --id string   Peer identity to reset (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner peer resync
use: resync
short: Ask agents to re-send all zone data to the combiner
long: |
  Send an RFI SYNC to configured agents, requesting them to re-send all their
  local zone data. Useful after combiner restart when in-memory agent contributions
  are lost.
  
  By default, sends to all agents for all zones. Use --zone and --agent to narrow scope.
  
  Example:
    tdns-cliv2 combiner peer resync
    tdns-cliv2 combiner peer resync --zone=whisky.dnslab.
    tdns-cliv2 combiner peer resync --agent=agent.alpha.dnslab.
flags: |
      --agent string   Resync only this agent (default: all agents)
      --zone string    Resync only this zone (default: all zones)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner ping
use: ping
short: Send an API ping request and present the response
flags: |
  -c, --count int   #pings to send
  -n, --newapi      use new api client
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner remove-data
use: remove-data [file]
short: Remove local data from a zone passing through the combiner
long: |
  Remove local data from a zone passing through the combiner.
  
  Zone can be specified via --zone flag. The file should contain one RR per line.
  
  Example contents (for a zone named "example.com"):
    example.com. 86400 IN NS ns1.provider.com.
    example.com. 86400 IN NS ns2.service.net.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner router
use: router
short: DNS message router introspection commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner router describe
use: describe
short: Show detailed router state
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner router list
use: list
short: List all registered message handlers
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner router metrics
use: metrics
short: Show router metrics (use --detailed for per-peer breakdown)
flags: |
      --detailed   Show per-peer breakdown
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner router reset
use: reset
short: Reset router metrics
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner router walk
use: walk
short: Walk all handlers with visitor pattern
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner show-combiner-data
use: show-combiner-data
short: Show the combiner's local data store (merged + per-agent)
long: |
  Display the combiner's CombinerData (merged view) and AgentContributions
  (per-agent breakdown) for all zones or a specific zone.
  
  Example:
    tdns-cliv2 combiner show-combiner-data
    tdns-cliv2 combiner show-combiner-data --zone whisky.dnslab.
flags: |
      --zone string   Filter by specific zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner stop
use: stop
short: Send stop command to the daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner transaction
use: transaction
short: Transaction diagnostics
long: |
  Commands for diagnosing transaction errors on the combiner.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner transaction errors
use: errors
short: Show recent transaction errors
long: |
  Show errors from recent CHUNK NOTIFY processing. Use --last to filter by time window (default: 30m).
flags: |
      --last string   Time window for error listing (e.g. 30m, 2h, 1h30m) (default "30m")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner transaction errors details
use: details
short: Show details for a specific transaction error
long: |
  Look up a specific distribution ID in the error journal and show full details.
flags: |
      --distid string   Distribution ID to look up
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner zone
use: zone
short: Combiner zone management commands
flags: |
  -F, --force   Force operation
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner zone bump
use: bump
short: Bump SOA serial for a zone on the combiner
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner zone edits
use: edits
short: Manage pending, approved and rejected edits
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner zone edits approve
use: approve
short: Approve a pending edit
flags: |
      --edit int      Edit ID to approve
      --zone string   Zone the edit belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner zone edits clear
use: clear
short: Clear edit tables (default: all; use --pending/--approved/--rejected/--current to select)
flags: |
      --approved      Clear approved edits
      --current       Clear current contributions
      --pending       Clear pending edits
      --rejected      Clear rejected edits
      --zone string   Scope to zone (default: all zones)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner zone edits list
use: list
short: List edits for a zone (default: current contributions; use --pending/--approved/--rejected)
flags: |
      --approved      Show approved edits (transaction history)
      --pending       Show pending edits
      --rejected      Show rejected edits
      --zone string   Zone to list edits for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner zone edits purge
use: purge
short: Remove all contributions attributed to a specific origin from a zone
long: |
  Remove all contributions for a given origin (sender ID) from
  the combiner's view of a zone. Used to clean up ghost or stale
  contributions left over from earlier code versions or
  mis-configured peers — e.g. an entry attributed to a bare
  "combiner" string instead of the FQDN "combiner.X.dnslab.".
  
  This is destructive: there is no per-RR undo. The combiner's
  in-memory AgentContributions map, the persisted
  CombinerContributions DB rows, and the rebuilt CombinerData are
  all updated. CombineWithLocalChanges runs after the rebuild so
  the served zone reflects the change.
flags: |
      --origin string   Origin (sender ID) whose contributions to remove (required)
      --zone string     Zone to purge contributions from (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner zone edits reapply
use: reapply
short: Reload contributions from DB and re-apply to zone data
flags: |
      --zone string   Zone to reapply contributions for (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner zone edits reject
use: reject
short: Reject a pending edit
flags: |
      --edit int        Edit ID to reject
      --reason string   Reason for rejection (required)
      --zone string     Zone the edit belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner zone list
use: list
short: List configured zones
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner zone mplist
use: mplist
short: List multi-provider zones with HSYNCPARAM details
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner zone reload
use: reload
short: Request re-loading a zone on the combiner
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli completion
use: completion
short: Generate the autocompletion script for the specified shell
long: |
  Generate the autocompletion script for tdns-mpcli for the specified shell.
  See each sub-command's help for details on how to use the generated script.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli completion bash
use: bash
short: Generate the autocompletion script for bash
long: |
  Generate the autocompletion script for the bash shell.
  
  This script depends on the 'bash-completion' package.
  If it is not installed already, you can install it via your OS's package manager.
  
  To load completions in your current shell session:
  
  	source <(tdns-mpcli completion bash)
  
  To load completions for every new session, execute once:
  
  #### Linux:
  
  	tdns-mpcli completion bash > /etc/bash_completion.d/tdns-mpcli
  
  #### macOS:
  
  	tdns-mpcli completion bash > $(brew --prefix)/etc/bash_completion.d/tdns-mpcli
  
  You will need to start a new shell for this setup to take effect.
flags: |
      --no-descriptions   disable completion descriptions
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli completion fish
use: fish
short: Generate the autocompletion script for fish
long: |
  Generate the autocompletion script for the fish shell.
  
  To load completions in your current shell session:
  
  	tdns-mpcli completion fish | source
  
  To load completions for every new session, execute once:
  
  	tdns-mpcli completion fish > ~/.config/fish/completions/tdns-mpcli.fish
  
  You will need to start a new shell for this setup to take effect.
flags: |
      --no-descriptions   disable completion descriptions
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli completion powershell
use: powershell
short: Generate the autocompletion script for powershell
long: |
  Generate the autocompletion script for powershell.
  
  To load completions in your current shell session:
  
  	tdns-mpcli completion powershell | Out-String | Invoke-Expression
  
  To load completions for every new session, add the output of the above command
  to your powershell profile.
flags: |
      --no-descriptions   disable completion descriptions
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli completion zsh
use: zsh
short: Generate the autocompletion script for zsh
long: |
  Generate the autocompletion script for the zsh shell.
  
  If shell completion is not already enabled in your environment you will need
  to enable it.  You can execute the following once:
  
  	echo "autoload -U compinit; compinit" >> ~/.zshrc
  
  To load completions in your current shell session:
  
  	source <(tdns-mpcli completion zsh)
  
  To load completions for every new session, execute once:
  
  #### Linux:
  
  	tdns-mpcli completion zsh > "${fpath[1]}/_tdns-mpcli"
  
  #### macOS:
  
  	tdns-mpcli completion zsh > $(brew --prefix)/share/zsh/site-functions/_tdns-mpcli
  
  You will need to start a new shell for this setup to take effect.
flags: |
      --no-descriptions   disable completion descriptions
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli configure
use: configure
short: Interactive bootstrap for tdns-mp config files
long: |
  Interview the user and emit coordinated configs for
  tdns-mpagent, tdns-mpsigner, tdns-mpcombiner and tdns-mpcli
  under /etc/tdns/.
  
  Safe to re-run: existing values become the prompt defaults.
  Live servers must be explicitly confirmed before their
  config is replaced.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli help
use: help [command]
short: Help about any command
long: |
  Help provides help for any command in the application.
  Simply type tdns-mpcli help [path to command] for full details.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer
use: signer
short: Interact with the MP signer via API
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth
use: auth
short: Interact with tdns-auth (authoritative) via API
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth daemon
use: daemon
short: Only useful via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth daemon api
use: api
short: request a statusd api summary
long: |
  The daemon api queries the statusd for the provided API
  and prints that out in a (hopefully) comprehensible fashion.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth daemon reload
use: reload
short: Reload config from file
long: |
  Reload config from file (the assumption is that something in the config has changed).
  Right now this doesn't do much, but later on various services will be able to restart.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth daemon restart
use: restart
short: Stop and then start the management daemon
flags: |
      --clear string   Truncate the specified log file before starting
      --update         Update the server binary from /tmp/{binary} to /usr/local/libexec/ before starting
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth daemon start
use: start
short: Start the axfr-statusd daemon
long: |
  Start the axfr-statusd daemon. If it was already running, then this is a no-op.
flags: |
  -W, --maxwait int   Max seconds to wait until declaring start to have failed (default 5)
      --slurp         Slurp stdout/stderr for errors (debug tool only)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth daemon status
use: status
short: Query for the status of the management daemon
long: |
  Query for the status of the management daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth daemon stop
use: stop
short: Stop the management daemon
long: |
  Stop the management daemon. If it was not running, then this is a no-op.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr
use: imr
short: IMR (Internal Recursive Resolver) cache commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr dump-tuning
use: dump-tuning
short: Show effective IMR tuning values (backoff policy, family, discovery, etc.)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr dump-zone-backoffs
use: dump-zone-backoffs [zone]
short: Show zone-scoped lame-delegation backoffs (per zone, per address)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr flush
use: flush <qname>
short: Flush IMR cache entries at and below qname
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr query
use: query <qname> <qtype>
short: Query the IMR cache (cache-only, no external queries)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr reset
use: reset
short: Flush entire IMR cache and re-prime (preserves root NS)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr show
use: show
short: Show IMR cache entries related to agent discovery
flags: |
      --id string   Agent identity to show cache entries for (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth ping
use: ping
short: Send an API ping request and present the response
flags: |
  -c, --count int   #pings to send
  -n, --newapi      use new api client
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone
use: zone
short: Prefix command, not usable by itself
flags: |
  -F, --force   force operation
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone bump
use: bump
short: Bump SOA serial and epoch (if any) in tdns-auth version of zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dsync
use: dsync
short: Prefix command, not useable by itself
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dsync bootstrap-sig0-key
use: bootstrap-sig0-key
short: Send dsync bootstrap command
flags: |
  -a, --algorithm string   Algorithm for the new SIG(0) key (use the 'algorithms' subcommand to list what the server supports)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dsync publish
use: publish
short: Send dsync publish-dsync-rrset command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dsync roll-sig0-key
use: roll-sig0-key
short: Send dsync rollover command
flags: |
  -a, --algorithm string   Algorithm for the new SIG(0) key (use the 'algorithms' subcommand to list what the server supports)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dsync status
use: status
short: Send dsync status command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dsync unpublish
use: unpublish
short: Send dsync unpublish-dsync-rrset command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone freeze
use: freeze
short: Freeze a zone (i.e. stop accepting DDNS updates to the zone data)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone list
use: list
short: List configured zones
flags: |
  -f, --file      Show zone input file
  -N, --notify    Show zone downstream notify addresses
  -P, --primary   Show zone primary nameserver
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone nsec
use: nsec
short: Prefix command, not usable by itself
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone nsec generate
use: generate
short: Generate NSEC records for a zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone nsec show
use: show
short: Show the NSEC chain for a zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone readfake
use: readfake
short: Create a fake zone from a compiled in string
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone reload
use: reload
short: Request re-loading a zone
flags: |
  -e, --error            wait for reload to complete and report any parse errors
      --timeout string   how long to wait for reload when --error is set (e.g. 10s, 2m) (default "10s")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone resign
use: resign
short: Re-sign zone from scratch with currently-active keys (drops all existing RRSIGs)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone sign
use: sign
short: Request signing of a zone (additive: cover gaps with active keys)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone thaw
use: thaw
short: Thaw a zone (i.e. accept DDNS updates to the zone data again)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone update
use: update
short: Create and ultimately send a DNS UPDATE msg for zone auth data
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone update create
use: create
short: Create and ultimately send a DNS UPDATE msg for zone auth data
long: |
  Will query for details about the DNS UPDATE via (add|del|show|set-ttl) commands.
  When the message is complete it may be signed and sent by the 'send' command. After a
  message has been send the loop will start again with a new, empty message to create.
  Loop ends on the command "QUIT"
  
  The zone to update is mandatory to specify on the command line with the --zone flag.
flags: |
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone write
use: write
short: Write a zone to disk
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer config
use: config
short: Commands to reload config, reload zones, etc
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer config reload
use: reload
short: Send config reload command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer config reload-zones
use: reload-zones
short: Send reload-zones command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer config status
use: status
short: Send config status command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer daemon
use: daemon
short: Only useful via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer daemon api
use: api
short: request a statusd api summary
long: |
  The daemon api queries the statusd for the provided API
  and prints that out in a (hopefully) comprehensible fashion.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer daemon reload
use: reload
short: Reload config from file
long: |
  Reload config from file (the assumption is that something in the config has changed).
  Right now this doesn't do much, but later on various services will be able to restart.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer daemon restart
use: restart
short: Stop and then start the management daemon
flags: |
      --clear string   Truncate the specified log file before starting
      --update         Update the server binary from /tmp/{binary} to /usr/local/libexec/ before starting
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer daemon start
use: start
short: Start the axfr-statusd daemon
long: |
  Start the axfr-statusd daemon. If it was already running, then this is a no-op.
flags: |
  -W, --maxwait int   Max seconds to wait until declaring start to have failed (default 5)
      --slurp         Slurp stdout/stderr for errors (debug tool only)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer daemon status
use: status
short: Query for the status of the management daemon
long: |
  Query for the status of the management daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer daemon stop
use: stop
short: Stop the management daemon
long: |
  Stop the management daemon. If it was not running, then this is a no-op.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer debug
use: debug
short: Debug commands against the configured daemon
flags: |
      --qname string   qname of rrset to examine
      --qtype string   qtype of rrset to examine
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer debug lav
use: lav
short: Lookup and validate a child RRset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer debug rrset
use: rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer debug show-rrsetcache
use: show-rrsetcache
short: List cached RRsets
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer debug show-ta
use: show-ta
short: List known DNSSEC trust anchors
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer debug sig0
use: sig0
short: SIG(0) key helper commands (local)
flags: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
  -r, --rrtype string      rrtype to use for SIG(0)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer debug sig0 generate
use: generate
inherited: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
      --config string      config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug              debug output
  -H, --headers            show headers
  -Z, --pzone string       parent zone name
      --qname string       qname of rrset to examine
      --qtype string       qtype of rrset to examine
  -r, --rrtype string      rrtype to use for SIG(0)
  -v, --verbose            verbose output
  -z, --zone string        zone name

=== tdns-mpcli signer debug validate-rrset
use: validate-rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer gossip
use: gossip
short: Gossip protocol commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer gossip state
use: state
short: Show gossip state matrix for a zone
long: |
  Display the N×N state matrix for the provider group serving a zone.
  Each row is a reporting peer; each column shows that reporter's
  view of another peer's state. A healthy group shows OPERATIONAL
  in every non-diagonal cell.
flags: |
  -z, --zone string   Zone name (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer jwt
use: jwt
short: JWT inspection and manipulation commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer jwt inspect
use: inspect [file|-]
short: Inspect JWT from CHUNK or JWK RDATA from JWK records
long: |
  Inspect JWT-wrapped payloads from CHUNK records, or decode JWK RDATA from JWK records.
  
  Reads DNS wire or text (dig-style) from stdin (or file). If the response contains a CHUNK RR,
  decodes and inspects the JWT (with optional verify/decrypt). If it contains a JWK RR,
  base64-decodes the RDATA and prints the JWK as pretty JSON.
  
  Examples:
    # Inspect CHUNK (JWT)
    dog node.dist123.kdc. CHUNK | tdns-cli jwt inspect -
  
    # Inspect JWK (pretty-print JSON)
    dog dns.agent.provider. JWK | tdns-cli jwt inspect -
  
    # With signature verification (CHUNK only)
    dog node.dist123.kdc. CHUNK | tdns-cli jwt inspect - --verify --verify-key /path/to/kdc.jose.pub
  
  Input format auto-detection:
    - DNS wire format (from dog/dig): CHUNK → JWT; JWK → base64-decode and pretty JSON
    - DNS text format (zone file / dig style): same
    - Base64-encoded JWT (starts with eyJ...)
    - Raw CHUNK RDATA
flags: |
      --backend string       Crypto backend: jose, hpke, auto (default: auto) (default "auto")
      --decrypt              Decrypt JWE payload (requires --decrypt-key)
      --decrypt-key string   Private key file for decryption (JWK JSON for JOSE, hex for HPKE)
      --output string        Output format: text, json (default: text) (default "text")
  -v, --verbose              Verbose output (show all headers)
      --verify               Verify JWS signature (requires --verify-key)
      --verify-key string    Public key file for signature verification (JWK JSON or PEM)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli signer keys
use: keys
short: Generate long-term keypairs for agent/combiner (JOSE)
long: |
  Generate JOSE keypairs used by tdns-agent and tdns-mpcombiner for
  authenticated NOTIFY(CHUNK) and API traffic. Use one keypair per party:
    - Agent:  agent.jose.private (+ optional agent.jose.pub for combiner config)
    - Combiner: combiner.jose.private (+ optional combiner.jose.pub for agent config)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keys generate
use: generate [--jose] [--jose-outfile <path>] [--jose-pubfile <path>]
short: Generate JOSE keypair (P-256)
long: |
  Generate a JOSE keypair (P-256) for agent or combiner.
  
  Must specify --jose and --jose-outfile (private key path).
  
    --jose              Generate new JOSE keypair
    --jose-outfile      Path for JOSE private key file (required if --jose)
    --jose-pubfile      Optional: path to write public key (JWK) for the peer
                        e.g. agent.jose.pub on combiner, combiner.jose.pub on agent
  
  Examples:
    tdns-cli keys generate --jose --jose-outfile /etc/tdns/agent.jose.private --jose-pubfile /etc/tdns/agent.jose.pub
    tdns-cli keys generate --jose --jose-outfile /etc/tdns/combiner.jose.private --jose-pubfile /etc/tdns/combiner.jose.pub
  
  Then configure:
    - Agent:  long_term_jose_priv_key: /etc/tdns/agent.jose.private
             combiner.long_term_jose_pub_key: /etc/tdns/combiner.jose.pub
    - Combiner: long_term_jose_priv_key: /etc/tdns/combiner.jose.private
                agent_peer.long_term_jose_pub_key: /etc/tdns/agent.jose.pub
flags: |
      --jose                  Generate new JOSE keypair
      --jose-outfile string   Output path for JOSE private key (default: ./agent.jose.private)
      --jose-pubfile string   Optional: output path for JOSE public key (for peer config)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore
use: keystore
short: Prefix command to access different features of the keystore
long: |
  The keystore holds SIG(0) and DNSSEC key pairs.
  The CLI contains functions for listing, adding, deleting, and
  changing the state of keys.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore dnssec
use: dnssec
short: Prefix command, only usable via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore dnssec add
use: add
short: Add a new DNSSEC key pair to the keystore
long: |
  Add a new SIG(0) key pair to the keystore. Required arguments are the name of the file
  containing either the private or the public SIG(0) key and the name of the zone.
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to add DNSSEC key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec algorithms
use: algorithms
short: List the DNSSEC algorithms the server supports
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore dnssec clear
use: clear
short: Permanently delete all DNSSEC keys for a zone (KeyStateWorker will regenerate as needed)
flags: |
      --force         Skip confirmation prompt
  -z, --zone string   Zone to clear all DNSSEC keys for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec delete
use: delete
short: Delete DNSSEC key pair from TDNSD keystore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore dnssec export
use: export
short: Export a DNSSEC key pair from the keystore as BIND-style .private/.key files
long: |
  Write the DNSSEC key pair for (zone, keyid) to two files in BIND filename
  convention: K<zone>+<alg-num>+<keyid>.private (PKCS#8 PEM) and .key (zone-file
  DNSKEY RR). The resulting pair is directly consumable by 'keystore dnssec import'.
flags: |
      --keyid int       Key ID of key to export
  -o, --outdir string   Directory to write .private and .key files to (default ".")
  -z, --zone string     Zone the key belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec gen-ds
use: gen-ds
short: Generate DS records for a zone's KSK(s) from the keystore
long: |
  Generate DS (Delegation Signer) records for a zone's KSK (Key Signing Key) DNSKEY records stored in the keystore. The command queries the keystore for DNSKEY records for the specified zone, filters for KSKs (keys with the SEP bit set), and generates DS records using SHA-256 and SHA-384 digest algorithms. If --keyid is not specified, DS records are generated for all KSKs in the zone.
flags: |
      --keyid int     Key ID of specific KSK to generate DS for (optional, if not specified, generates for all KSKs)
  -z, --zone string   Zone to generate DS records for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec generate
use: generate
short: Generate a new DNSSEC key pair and add it to the keystore
flags: |
  -a, --algorithm string   Algorithm to use for DNSSEC key generation (use the 'algorithms' subcommand to list what the server supports)
      --keytype string     Key type to generate (KSK|ZSK|CSK)
      --state string       Inital key state (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore dnssec import
use: import
short: Add a new DNSSEC key pair to the keystore
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to import DNSSEC key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec list
use: list
short: List all DNSSEC key pairs in the keystore
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore dnssec purge
use: purge
short: Delete keys in 'removed' state, keeping the 3 most recent per zone
long: |
  Delete keys in 'removed' state from the keystore, keeping the 3
  most recent per zone (by insert order). Use --zone all to apply to
  every zone with removed keys at once.
  
  Dry-run by default: prints the keys that would be deleted and exits
  without modifying anything. Pass --force to actually delete.
flags: |
      --force         Actually delete; otherwise dry-run
  -z, --zone string   Zone to purge ('all' for every zone)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec rollover
use: rollover
short: Perform a manual DNSSEC key rollover (standby→active, active→retired)
flags: |
      --keytype string   Key type to roll over (ZSK|KSK) (default "ZSK")
  -z, --zone string      Zone to perform rollover for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec setstate
use: setstate
short: Set the state of and existing DNSSEC key pair in the TDNSD keystore
flags: |
      --keyid int      Key ID of key to delete
      --state string   New statei of key
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore sig0
use: sig0
short: Prefix command, only usable via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore sig0 add
use: add
short: Add a new SIG(0) key pair to the keystore
long: |
  Add a new SIG(0) key pair to the keystore. Required arguments are the name of the file
  containing either the private or the public SIG(0) key and the name of the zone.
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to add SIG(0) key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore sig0 algorithms
use: algorithms
short: List the SIG(0) algorithms the server supports
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore sig0 delete
use: delete
short: Delete SIG(0) key pair from TDNSD keystore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore sig0 export
use: export
short: Export a SIG(0) key pair from the keystore as BIND-style .private/.key files
long: |
  Write the SIG(0) key pair for (zone, keyid) to two files in BIND filename
  convention: K<zone>+<alg-num>+<keyid>.private (PKCS#8 PEM) and .key (zone-file
  KEY RR). The resulting pair is directly consumable by commands accepting
  --key <basename.private>.
flags: |
      --keyid int       Key ID of key to export
  -o, --outdir string   Directory to write .private and .key files to (default ".")
  -z, --zone string     Zone the key belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore sig0 generate
use: generate
short: Generate a new SIG(0) key pair and add it to the keystore
flags: |
  -a, --algorithm string   Algorithm to use for SIG(0) key generation (use the 'algorithms' subcommand to list what the server supports)
      --state string       Inital key state (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore sig0 import
use: import
short: Add a new SIG(0) key pair to the keystore
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to import SIG(0) key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore sig0 list
use: list
short: List all SIG(0) key pairs in the keystore
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore sig0 setstate
use: setstate
short: Set the state of and existing SIG(0) key pair in the TDNSD keystore
flags: |
      --keyid int      Key ID of key to delete
      --state string   New state of key (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer peer
use: peer
short: Peer management commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer peer apiping
use: apiping
short: Ping a peer via HTTPS API
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer peer ping
use: ping
short: Ping a peer via DNS CHUNK
long: |
  Send a DNS CHUNK ping to a peer and report the result.
  The --id flag specifies the peer identity (e.g. agent.beta.dnslab.
  or combiner.dnslab.).
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer peer reset
use: reset
short: Reset peer (agent/auditor only; no-op elsewhere)
long: |
  Reset a peer to initial NEEDED state. Flushes all IMR
  cache entries for the peer's discovery names and restarts
  discovery from scratch. Use this when a peer is stuck in UNKNOWN
  or KNOWN state. Only applicable to roles that use dynamic
  HSYNC3-driven discovery (agent, auditor); a no-op on
  static-peer roles (signer, combiner).
flags: |
      --id string   Peer identity to reset (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer ping
use: ping
short: Send an API ping request and present the response
flags: |
  -c, --count int   #pings to send
  -n, --newapi      use new api client
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer report
use: report <qname>
short: Send a report and (optionally) discover DSYNC via the internal resolver (imr)
flags: |
  -D, --details string   Report details
      --ede int          Manual override of EDE code (513-523 locally defined)
      --port int         Manual override of DSYNC port
  -S, --sender string    Report sender
      --target string    Manual override of DSYNC target
  -T, --tsig             TSIG sign the report (default true)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer router
use: router
short: DNS message router introspection commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer router describe
use: describe
short: Show detailed router state
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer router list
use: list
short: List all registered message handlers
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer router metrics
use: metrics
short: Show router metrics (use --detailed for per-peer breakdown)
flags: |
      --detailed   Show per-peer breakdown
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer router reset
use: reset
short: Reset router metrics
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer router walk
use: walk
short: Walk all handlers with visitor pattern
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer stop
use: stop
short: Send stop command to the daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer truststore
use: truststore
short: Prefix command to access different features of the truststore
long: |
  The truststore is where SIG(0) public keys for child zones are kept.
  The CLI contains functions for listing trusted SIG(0) keys, adding and
  deleting child keys as well as changing the trust state of individual keys.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer truststore sig0
use: sig0
short: Prefix command, only usable via sub-commands
flags: |
  -c, --child string   Name of child SIG(0) key
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer truststore sig0 add
use: add
short: Add a new SIG(0) public key to the truststore
flags: |
  -s, --src string   Source for SIG(0) public key, a file name or 'dns'
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer truststore sig0 delete
use: delete
short: Delete a SIG(0) public key from the truststore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer truststore sig0 list
use: list
short: List all child SIG(0) public key in the keystore
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer truststore sig0 trust
use: trust
short: Declare a child SIG(0) public key in the keystore as trusted
flags: |
      --keyid int   Keyid of child SIG(0) key to change trust for
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer truststore sig0 untrust
use: untrust
short: Declare a child SIG(0) public key in the keystore as untrusted
flags: |
      --keyid int   Keyid of child SIG(0) key to change trust for
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone
use: zone
short: Prefix command, not usable by itself
flags: |
  -F, --force   force operation
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone bump
use: bump
short: Bump SOA serial and epoch (if any) in tdns-auth version of zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dsync
use: dsync
short: Prefix command, not useable by itself
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dsync bootstrap-sig0-key
use: bootstrap-sig0-key
short: Send dsync bootstrap command
flags: |
  -a, --algorithm string   Algorithm for the new SIG(0) key (use the 'algorithms' subcommand to list what the server supports)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dsync publish
use: publish
short: Send dsync publish-dsync-rrset command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dsync roll-sig0-key
use: roll-sig0-key
short: Send dsync rollover command
flags: |
  -a, --algorithm string   Algorithm for the new SIG(0) key (use the 'algorithms' subcommand to list what the server supports)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dsync status
use: status
short: Send dsync status command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dsync unpublish
use: unpublish
short: Send dsync unpublish-dsync-rrset command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone freeze
use: freeze
short: Freeze a zone (i.e. stop accepting DDNS updates to the zone data)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone list
use: list
short: List configured zones
flags: |
  -f, --file      Show zone input file
  -N, --notify    Show zone downstream notify addresses
  -P, --primary   Show zone primary nameserver
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone mplist
use: mplist
short: List multi-provider zones with HSYNCPARAM details
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone nsec
use: nsec
short: Prefix command, not usable by itself
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone nsec generate
use: generate
short: Generate NSEC records for a zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone nsec show
use: show
short: Show the NSEC chain for a zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone readfake
use: readfake
short: Create a fake zone from a compiled in string
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone reload
use: reload
short: Request re-loading a zone
flags: |
  -e, --error            wait for reload to complete and report any parse errors
      --timeout string   how long to wait for reload when --error is set (e.g. 10s, 2m) (default "10s")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone resign
use: resign
short: Re-sign zone from scratch with currently-active keys (drops all existing RRSIGs)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone sign
use: sign
short: Request signing of a zone (additive: cover gaps with active keys)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone thaw
use: thaw
short: Thaw a zone (i.e. accept DDNS updates to the zone data again)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone update
use: update
short: Create and ultimately send a DNS UPDATE msg for zone auth data
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone update create
use: create
short: Create and ultimately send a DNS UPDATE msg for zone auth data
long: |
  Will query for details about the DNS UPDATE via (add|del|show|set-ttl) commands.
  When the message is complete it may be signed and sent by the 'send' command. After a
  message has been send the loop will start again with a new, empty message to create.
  Loop ends on the command "QUIT"
  
  The zone to update is mandatory to specify on the command line with the --zone flag.
flags: |
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone write
use: write
short: Write a zone to disk
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli version
use: version
short: Print the version of the app, more or less verbosely
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

