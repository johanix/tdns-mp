=== tdns-mpcli
use: tdns-mpcli
short: tdns-mpcli is the CLI tool for tdns multi-provider applications
flags: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent
use: agent
short: TDNS Agent commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent config
use: config
short: Commands to reload config, reload zones, etc
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent config check
use: check [config-file]
short: Validate the tdns-agent config and correlate it with the running daemon
long: |
  Validate a tdns-agent configuration file for completeness and internal
  consistency, and (unless --offline) correlate it against the running daemon to
  report drift between the file on disk and what the server actually loaded.
  
  Target config resolution:
    1. an explicit path (positional arg or --serverconfig)
    2. otherwise the compiled-in default (/etc/tdns/tdns-agent.yaml)
  
  Exit status is non-zero if any check FAILs (WARNs do not fail the run).
flags: |
      --offline               do not contact the daemon; run static checks only
      --serverconfig string   path to the tdns-agent config file to check (default: ask the daemon, else the configured/compiled-in default)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent config mwe
use: mwe
short: Generate a minimal working example config (+ certs, zones) for tdns-agent
long: |
  Generate a Minimal Working Example configuration for tdns-agent.
  
  Writes a single self-contained YAML to <dir>/tdns-agent.yaml (or, if that
  file already exists, to <dir>/tdns-agent.yaml.mwe so nothing is
  clobbered). Also generates, unless already present:
  
    - a self-signed cert/key pair under <dir>/certs (for the API server and the
      encrypted DNS transports)
    - two secondary zone templates and a commented-out secondary zone
  
  tdns-agent is secondary-only and never signs, so the generated config declares
  no zones and no dnssec: block; the agent starts clean and you add secondaries
  by uncommenting the example.
  
  The DNS engine listens on IPv4 and IPv6 localhost; extra listen addresses are
  shown commented out. The generated tree is designed to pass
  `tdns-cli agent config check`.
flags: |
      --dir string   base directory for the generated config, certs and zones (default "/etc/tdns")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent config reload
use: reload
short: Send config reload command to tdns-agent
flags: |
      --confirm   apply even if the reload would strand a signed zone by a same-name DNSSEC policy algorithm change
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent config reload-tsig
use: reload-tsig
short: Reconcile keys.tsig into the TSIG keystore (config reload-tsig)
long: |
  Re-read keys.tsig from the config file and reconcile into the DB-backed
  TSIG keystore. Secret conflicts are withheld by default; use --force to
  overwrite all conflicts, or --interactive to prompt per conflict.
flags: |
      --force         overwrite all secret/algorithm conflicts with keys.tsig
      --interactive   prompt per conflict before overwriting
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent config reload-zones
use: reload-zones
short: Send reload-zones command to tdns-agent
flags: |
      --confirm   apply even if the reload would strand a signed zone by a same-name DNSSEC policy algorithm change
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent config status
use: status
short: Send config status command to tdns-agent
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent daemon
use: daemon
short: Only useful via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent daemon api
use: api
short: request an API summary from the tdns-agent daemon
long: |
  The daemon api command queries tdns-agent for the provided API
  and prints that out in a (hopefully) comprehensible fashion.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent daemon reload
use: reload
short: Reload config from file
long: |
  Reload config from file (the assumption is that something in the config has changed).
  Right now this doesn't do much, but later on various services will be able to restart.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent daemon restart
use: restart
short: Stop and then start the management daemon
flags: |
      --clear string   Truncate the specified log file before starting
      --update         Update the server binary from /tmp/{binary} to /usr/local/libexec/ before starting
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent daemon start
use: start
short: Start the tdns-agent daemon
long: |
  Start the tdns-agent daemon. If it was already running, then this is a no-op.
flags: |
  -W, --maxwait int   Max seconds to wait until declaring start to have failed (default 5)
      --slurp         Slurp stdout/stderr for errors (debug tool only)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent daemon status
use: status
short: Query for the status of the management daemon
long: |
  Query for the status of the management daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent daemon stop
use: stop
short: Stop the management daemon
long: |
  Stop the management daemon. If it was not running, then this is a no-op.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug
use: debug
short: Debug commands against the configured daemon
flags: |
      --qname string   qname of rrset to examine
      --qtype string   qtype of rrset to examine
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug agentregistry
use: agentregistry
short: Test the agent registry
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug dump-agentregistry
use: dump-agentregistry
short: Dump the agent registry
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug dump-zonedatarepo
use: dump-zonedatarepo
short: Dump the zone data repo (deprecated: use show-synced-data)
hidden: true
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug lav
use: lav
short: Lookup and validate a child RRset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug queue-status
use: queue-status
short: Show reliable message queue status and pending messages
flags: |
  -v, --verbose   Verbose output (show full details for each message)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -z, --zone string     zone name

=== tdns-mpcli agent debug resync
use: resync
short: Re-send all local changes to combiner and remote agents
long: |
  Re-send all locally stored synced data for a zone to the combiner and
  all remote agents. Use this when the combiner or remote agents have lost
  state and need to be brought back in sync.
  
  Example:
    tdns-cliv2 agent debug resync --zone whisky.dnslab.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug rrset
use: rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug send-notify
use: send-notify
short: Tell agent to send a NOTIFY message to the other agents
flags: |
      --RR string       DNS record to send
  -I, --id string       agent identity to claim
  -R, --rrtype string   RR type sent notify for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug send-rfi
use: send-rfi
short: Tell agent to send an RFI message to another agent
flags: |
  -I, --id string        agent identity to claim
      --rfi string       RFI type (CONFIG|SYNC|AUDIT|EDITS)
      --subtype string   RFI subtype for CONFIG (upstream|downstream|sig0key)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug send-sync-to
use: send-sync-to <RR> [<RR>...]
short: Send a SYNC message to a remote agent (real transport)
long: |
  Create and send a real SYNC message to a specified remote agent.
  Uses the actual transport (CHUNK NOTIFY + fallback).
  RRs are validated before sending.
  
  Example:
    tdns-cliv2 debug agent send-sync-to \
      --to agent.provider-b.example.com. \
      --zone example.com. \
      "example.com. 3600 IN NS ns1.provider-a.example.com." \
      "example.com. 3600 IN NS ns2.provider-a.example.com."
flags: |
      --to string     Target agent ID
      --zone string   Zone name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output

=== tdns-mpcli agent debug show-combiner-data
use: show-combiner-data
short: Show combiner's local modifications store
long: |
  Display the combiner's stored local modifications that are applied to zones.
  Data is sorted by: zone → RRtype → RRs
  
  Example:
    tdns-cliv2 debug agent show-combiner-data
    tdns-cliv2 debug agent show-combiner-data --zone example.com
flags: |
      --zone string   Filter by specific zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output

=== tdns-mpcli agent debug show-key-inventory
use: show-key-inventory
short: Show DNSKEY inventory received from signer (KEYSTATE)
long: |
  Display the last KEYSTATE inventory received from the signer for a zone.
  Shows all keys reported by the signer's KeyDB with their state
  (created, published, standby, active, retired, foreign).
  
  Keys marked "foreign" are from other providers' signers.
  
  Example:
    tdns-cliv2 agent debug show-key-inventory --zone whisky.dnslab.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug show-rrsetcache
use: show-rrsetcache
short: List cached RRsets
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug show-synced-data
use: show-synced-data
short: Show synchronized data from peer agents (moved to: agent zone edits list)
flags: |
      --zone string   Filter by specific zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output

=== tdns-mpcli agent debug show-ta
use: show-ta
short: List known DNSSEC trust anchors
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug sig0
use: sig0
short: SIG(0) key helper commands (local)
flags: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
  -r, --rrtype string      rrtype to use for SIG(0)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug sig0 generate
use: generate
inherited: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
      --config string      config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug              debug output
  -H, --headers            show headers
  -Z, --pzone string       parent zone name
      --qname string       qname of rrset to examine
      --qtype string       qtype of rrset to examine
  -r, --rrtype string      rrtype to use for SIG(0)
  -v, --verbose            verbose output
  -z, --zone string        zone name

=== tdns-mpcli agent debug sync-state
use: sync-state
short: Show sync state for a zone
long: |
  Display the current synchronization state for a zone.
  
  Example:
    tdns-cliv2 debug agent sync-state --zone example.com
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug validate-rrset
use: validate-rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent debug zone-txlog
use: zone-txlog
short: Show staged-but-unpublished zone changes
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent discover
use: discover <agent-identity>
short: Trigger DNS discovery for a remote agent
long: |
  Discover a remote agent by querying DNS for its URI, JWK, TLSA, and SVCB records.
  This triggers async discovery which will:
  1. Query DNS for agent metadata (URI, JWK, TLSA, SVCB)
  2. Register the agent in PeerRegistry
  3. Start Hello retry loop if authorized
  4. Progress agent through state machine to OPERATIONAL
  
  Example:
    tdns-cli agent discover agent2.example.com
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent distrib
use: distrib
short: Manage distributions
long: |
  Commands for managing distributions created by this agent, including listing distributions and purging completed distributions.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent distrib discover
use: discover <agent-identity>
short: Discover and register agent contact information via DNS
long: |
  Performs DNS-based discovery of an agent's contact information and registers it.
  
  Looks up URI, KEY, TLSA, and A/AAAA records to discover:
    - API endpoint (_https._tcp.<identity> URI)
    - DNS endpoint (_dns._udp.<identity> URI)
    - Public key (<identity> KEY)
    - TLS certificate (_443._tcp.<identity> TLSA)
    - IP addresses (<identity> A/AAAA)
  
  After discovery, the agent is registered in PeerRegistry and can be used
  with 'distrib op' commands.
  
  Example:
    tdns-cliv2 agent distrib discover agent.delta.dnslab.
    tdns-cliv2 agent distrib discover provider.example.com
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent distrib list
use: list
short: List all distributions from this agent
flags: |
  -v, --verbose   Verbose output (show full details)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli agent distrib op
use: op [operation]
short: Run an operation toward a peer
long: |
  Run an operation toward a peer by identity (e.g. ping to combiner or a peer agent). Use "distrib peers" to list identities. Supported operations: ping (use --dns or --api for combiner ping).
flags: |
      --api         For ping: use HTTPS API ping
      --dns         For ping: use CHUNK-based DNS ping (default)
  -t, --to string   Recipient identity (e.g. combiner, agent.delta.dnslab.)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent distrib peers
use: peers
short: List all known peer agents with working keys
long: |
  Show all recipient agents that this agent has established keys with and can send distributions to.
flags: |
  -v, --verbose   Verbose output (show full details)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli agent distrib purge
use: purge [--force]
short: Delete distributions
long: |
  Delete distributions from the database. By default, only completed distributions are deleted. Use --force to delete ALL distributions regardless of status.
flags: |
      --force   Delete ALL distributions (not just completed ones)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent gossip
use: gossip
short: Gossip protocol commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent gossip state
use: state
short: Show gossip state matrix for a zone
long: |
  Display the N×N state matrix for the provider group serving a zone.
  Each row is a reporting peer; each column shows that reporter's
  view of another peer's state. A healthy group shows OPERATIONAL
  in every non-diagonal cell.
flags: |
  -z, --zone string   Zone name (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent hsync
use: hsync
short: HSYNC state-reporting commands
long: |
  Query HSYNC state stored in the agent's KeyDB and AgentRegistry.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync agentstatus
use: agentstatus
short: Show HSYNC status for an agent
flags: |
      --agentid string   Remote agent identity to show
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync confirmations
use: confirmations
short: Query HSYNC confirmations
long: |
  Display confirmation records for sync operations.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync events
use: events
short: Show HSYNC transport events
long: |
  Display recent transport events for debugging connectivity issues.
flags: |
  -n, --limit int     Maximum number of events to show (default 100)
  -p, --peer string   Filter by peer ID
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync locate
use: locate <agent-identity>
short: Locate and attempt to contact a remote agent
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync metrics
use: metrics
short: Show HSYNC operational metrics
long: |
  Display aggregated operational metrics for HSYNC operations.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync query
use: query
short: Query HSYNC RRset for a zone via DNS
flags: |
      --resolver string   Resolver address for DNS query (e.g. 8.8.8.8:53)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync send-hello
use: send-hello
short: Send a hello message to a remote agent
long: |
  Send a hello message to a remote agent via the running agent server.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync sync-ops
use: sync-ops
short: Show HSYNC sync operations
long: |
  Display sync operations tracked in the database.
  Shows operation details, status, and timestamps.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent hsync zonestatus
use: zonestatus
short: Show HSYNC status for a zone
flags: |
  -T, --transport string   Transport to show, default both api and dns
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent imr
use: imr
short: IMR (Internal Recursive Resolver) cache commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent imr dump-tuning
use: dump-tuning
short: Show effective IMR tuning values (backoff policy, family, discovery, etc.)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent imr dump-zone-backoffs
use: dump-zone-backoffs [zone]
short: Show zone-scoped lame-delegation backoffs (per zone, per address)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent imr flush
use: flush <qname>
short: Flush IMR cache entries at and below qname
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent imr query
use: query <qname> <qtype>
short: Query the IMR cache (cache-only, no external queries)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent imr reset
use: reset
short: Flush entire IMR cache and re-prime (preserves root NS)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent imr show
use: show
short: Show IMR cache entries related to agent discovery
flags: |
      --id string   Agent identity to show cache entries for (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keys
use: keys
short: JOSE keypair for secure CHUNK (generate, show)
long: |
  Generate a JOSE keypair or display the public key. Uses the server config file (agent or combiner) to get long_term_jose_priv_key path, or --server-config.
flags: |
      --server-config string   path to agent/combiner config file (overrides apiservers.*.config_file)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keys generate
use: generate
short: Generate JOSE keypair and write to config path or -output
flags: |
  -o, --output string   path for generated private key (overrides config)
inherited: |
      --config string          config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug                  debug output
  -H, --headers                show headers
  -Z, --pzone string           parent zone name
      --server-config string   path to agent/combiner config file (overrides apiservers.*.config_file)
  -v, --verbose                verbose output
  -z, --zone string            zone name

=== tdns-mpcli agent keys show
use: show
short: Print public key (JWK) from configured long_term_jose_priv_key
inherited: |
      --config string          config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug                  debug output
  -H, --headers                show headers
  -Z, --pzone string           parent zone name
      --server-config string   path to agent/combiner config file (overrides apiservers.*.config_file)
  -v, --verbose                verbose output
  -z, --zone string            zone name

=== tdns-mpcli agent keystore
use: keystore
short: Prefix command to access different features of the keystore
long: |
  The keystore holds SIG(0), DNSSEC, and global TSIG keys.
  The CLI contains functions for listing, adding, deleting, and
  changing the state of keys.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec
use: dnssec
short: Prefix command, only usable via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec add
use: add
short: Add a new DNSSEC key pair to the keystore
long: |
  Add a new SIG(0) key pair to the keystore. Required arguments are the name of the file
  containing either the private or the public SIG(0) key and the name of the zone.
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to add DNSSEC key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec algorithms
use: algorithms
short: List the DNSSEC algorithms the server supports
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec bulk-convert
use: bulk-convert
short: Convert a directory of bind9 DNSSEC keys into one tdns can pre-load
long: |
  Convert bind9-generated DNSSEC keys in --dir into the form keystore.preload reads.
  
  Two things stand between dnssec-keygen output and a pre-loadable directory: the
  private half is in bind's own format rather than PKCS#8 PEM, and there is no
  manifest, without which pre-load refuses the directory outright.
  
  This rewrites each .private in place as PEM, keeping the original alongside as
  .private.orig, and writes the manifest. The .key files are NOT touched: they
  already hold the public RR in exactly the form tdns exports.
  
  Key state comes from bind's .state file (9.16+, dnssec-policy zones), mapped
  onto tdns's states. A key with no .state file has no state to read, and it is
  not guessed -- pass --state to say which one applies. Guessing wrong is how a
  retired key gets published again.
  
  Runs entirely locally: no daemon, no API, no keystore. That is the point --
  these directories are built and committed before the server that serves them
  exists. Re-running is safe; already-converted keys are left alone.
flags: |
      --dir string     Directory of bind9 key files to convert in place
      --no-backup      Do not keep the original bind-format key as .private.orig
      --state string   Key state for keys with no .state file (required only for those)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec bulk-export
use: bulk-export
short: Export DNSSEC keys from the keystore to a directory
long: |
  Export DNSSEC keys to --dest as BIND-convention key files plus a manifest
  that carries what those files cannot (key state, rollover timestamps).
  
  Selection is additive: --zone takes one exact name, --zones takes a name and
  everything below it, and both may be repeated. Subtree matching is on label
  boundaries, so --zones pq.dnslab does not match notpq.dnslab. With no selector at
  all, the whole class is exported.
  
  Re-exporting into a directory MERGES: entries for keys not covered by this
  run are left in place, so several exports can populate one directory.
flags: |
      --dest string         Directory to write keys and manifest to
      --format string       Private key format to write: pem (PKCS#8, as stored) or bind (Private-key-format v1.3) (default "pem")
  -z, --zone stringArray    Zone to include (repeatable)
      --zones stringArray   Include this zone and everything below it (repeatable)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec bulk-import
use: bulk-import
short: Import DNSSEC keys from a directory into the keystore
long: |
  Import every DNSSEC key described by --src/manifest.yaml.
  
  A key that is not in the keystore is inserted. A key that is already there
  with identical content is left alone. A key that is already there with
  DIFFERENT content is reported and SKIPPED: the running keystore outranks a
  file that may be arbitrarily stale, and silently overwriting it is how a stale
  export un-rolls a rolled key.
  
  --force flips that last case to overwrite, which is what you want when
  recovering a keystore you know to be wrong.
flags: |
      --force        Overwrite keystore entries that differ (default: report and skip them)
      --src string   Directory to read keys and manifest from
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec clear
use: clear
short: Permanently delete all DNSSEC keys for a zone (KeyStateWorker will regenerate as needed)
flags: |
      --force         Skip confirmation prompt
  -z, --zone string   Zone to clear all DNSSEC keys for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec delete
use: delete
short: Delete DNSSEC key pair from the keystore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec ds-push
use: ds-push
short: Compute DS RRset from keystore and push to parent (UPDATE-only in this offline mode)
long: |
  Loads tdns config (same as other CLI commands using -c), opens the local keystore DB,
  and pushes the whole DS RRset to the parent. Requires imrengine in config.
  
  Offline mode: this CLI invocation builds a stub *ZoneData with no rollover policy
  attached, so PushDSRRsetForRollover falls through to the legacy single-scheme
  UPDATE path (whole-DS replacement, signed with the zone's active SIG(0) key).
  The auto / prefer-* / force-* dsync-scheme-preference values are honored only
  inside the daemon's rollover engine, where the policy is loaded from
  dnssecpolicies. To exercise NOTIFY pushes, run the daemon and let
  RolloverAutomatedTick drive the push.
  
  Use --dry-run to print the DS set and the UPDATE without sending.
flags: |
      --dry-run       Print DS RRset and UPDATE only; do not send
  -z, --zone string   Child zone (owner of DS RRset)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec export
use: export
short: Export a DNSSEC key pair from the keystore as BIND-style .private/.key files
long: |
  Write the DNSSEC key pair for (zone, keyid) to two files in BIND filename
  convention: K<zone>+<alg-num>+<keyid>.private and .key (zone-file DNSKEY RR).
  The private half is written as PKCS#8 PEM, or as bind's Private-key-format
  v1.3 with --format bind. The resulting pair is directly consumable by
  'keystore dnssec import'.
flags: |
      --format string   Private key format to write: pem (PKCS#8, as stored) or bind (Private-key-format v1.3) (default "pem")
      --keyid int       Key ID of key to export
  -o, --outdir string   Directory to write .private and .key files to (default ".")
  -z, --zone string     Zone the key belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec gen-ds
use: gen-ds
short: Generate DS records for a zone's KSK(s) from the keystore
long: |
  Generate DS (Delegation Signer) records for a zone's KSK (Key Signing Key) DNSKEY records stored in the keystore. The command queries the keystore for DNSKEY records for the specified zone, filters for KSKs (keys with the SEP bit set), and generates DS records using SHA-256 and SHA-384 digest algorithms. If --keyid is not specified, DS records are generated for all KSKs in the zone.
flags: |
      --keyid int     Key ID of specific KSK to generate DS for (optional, if not specified, generates for all KSKs)
  -z, --zone string   Zone to generate DS records for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec generate
use: generate
short: Generate a new DNSSEC key pair and add it to the keystore
flags: |
  -a, --algorithm string   Algorithm to use for DNSSEC key generation (use the 'algorithms' subcommand to list what the server supports)
      --keytype string     Key type to generate (KSK|ZSK|CSK)
      --state string       Inital key state (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec import
use: import
short: Add a new DNSSEC key pair to the keystore
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to import DNSSEC key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec list
use: list
short: List all DNSSEC key pairs in the keystore
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec policies
use: policies
short: List the DNSSEC policies the server loaded (including any in error)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec policy
use: policy
short: DNSSEC policy utilities
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec policy validate
use: validate
short: Validate a YAML fragment with a dnssec.policies: block
long: |
  Reads a YAML file that contains the same dnssec.policies: structure as tdns-auth
  config (policy names as keys, algorithm / ksk / zsk / csk / optional rollover+ttls+clamping).
  Runs the same validation as runtime config load. Exits non-zero on any error.
flags: |
  -f, --file string   YAML file path
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore dnssec policy-cleanup
use: policy-cleanup
short: Remove a zone's retired keys (and their RRSIGs) now, keeping active keys
long: |
  After a DNSSEC policy change, the old keys are retired but kept (with their
  signatures) so the zone stays validatable while the new keys take over —
  leaving the zone briefly double-signed. policy-cleanup collapses that window
  early: it removes the retired keys and strips their RRSIGs immediately,
  keeping the active keys. Unlike 'clear' (which deletes ALL keys and
  regenerates), this only touches retired keys.
  
  Accelerating removal means a resolver still caching only an old (now-removed)
  DNSKEY briefly cannot validate until it re-queries; the active keys already
  serve. Normally you can just wait for the KeyStateWorker to age the retired
  keys out after propagation-delay.
flags: |
      --force         Skip confirmation prompt
  -z, --zone string   Zone to clean up retired keys for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec purge
use: purge
short: Delete keys in 'removed' state, keeping the 3 most recent per zone
long: |
  Delete keys in 'removed' state from the keystore, keeping the 3
  most recent per zone (by insert order). Use --zone all to apply to
  every zone with removed keys at once.
  
  Dry-run by default: prints the keys that would be deleted and exits
  without modifying anything. Pass --force to actually delete.
flags: |
      --force         Actually delete; otherwise dry-run
  -z, --zone string   Zone to purge ('all' for every zone)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec query-parent
use: query-parent
short: Query configured parent-agent for child DS (poll until match or timeout)
long: |
  Uses rollover.parent-agent from the zone's dnssec policy (addr:port), or --parent-agent.
  Queries that address over TCP for the zone's DS RRset and compares to the keystore-derived
  expected set (ComputeTargetDSSetForZone, §7.5). Default poll schedule uses policy confirm-* timings,
  or 2s / 60s / 1h when those are unset.
  
  --once performs a single query and exits (no backoff loop).
flags: |
      --once                  Single TCP query; do not poll
      --parent-agent string   Override policy rollover.parent-agent (host:port)
  -z, --zone string           Child zone (owner of DS RRset)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec rollover
use: rollover
short: Perform a manual DNSSEC key rollover (standby→active, active→retired)
flags: |
      --keytype string   Key type to roll over (ZSK|KSK) (default "ZSK")
  -z, --zone string      Zone to perform rollover for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore dnssec setstate
use: setstate
short: Set the state of and existing DNSSEC key pair in the keystore
flags: |
      --keyid int      Key ID of key to delete
      --state string   New statei of key
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore sig0
use: sig0
short: Prefix command, only usable via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore sig0 add
use: add
short: Add a new SIG(0) key pair to the keystore
long: |
  Add a new SIG(0) key pair to the keystore. Required arguments are the name of the file
  containing either the private or the public SIG(0) key and the name of the zone.
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to add SIG(0) key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore sig0 algorithms
use: algorithms
short: List the SIG(0) algorithms the server supports
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore sig0 bulk-convert
use: bulk-convert
short: Convert a directory of bind9 SIG(0) keys into one tdns can pre-load
long: |
  Convert bind9-generated SIG(0) keys in --dir into the form keystore.preload reads.
  
  Two things stand between dnssec-keygen output and a pre-loadable directory: the
  private half is in bind's own format rather than PKCS#8 PEM, and there is no
  manifest, without which pre-load refuses the directory outright.
  
  This rewrites each .private in place as PEM, keeping the original alongside as
  .private.orig, and writes the manifest. The .key files are NOT touched: they
  already hold the public RR in exactly the form tdns exports.
  
  Key state comes from bind's .state file (9.16+, dnssec-policy zones), mapped
  onto tdns's states. A key with no .state file has no state to read, and it is
  not guessed -- pass --state to say which one applies. Guessing wrong is how a
  retired key gets published again.
  
  Runs entirely locally: no daemon, no API, no keystore. That is the point --
  these directories are built and committed before the server that serves them
  exists. Re-running is safe; already-converted keys are left alone.
flags: |
      --dir string     Directory of bind9 key files to convert in place
      --no-backup      Do not keep the original bind-format key as .private.orig
      --state string   Key state for keys with no .state file (required only for those)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore sig0 bulk-export
use: bulk-export
short: Export SIG(0) keys from the keystore to a directory
long: |
  Export SIG(0) keys to --dest as BIND-convention key files plus a manifest
  that carries what those files cannot (key state, rollover timestamps).
  
  Selection is additive: --zone takes one exact name, --zones takes a name and
  everything below it, and both may be repeated. Subtree matching is on label
  boundaries, so --zones pq.dnslab does not match notpq.dnslab. With no selector at
  all, the whole class is exported.
  
  Re-exporting into a directory MERGES: entries for keys not covered by this
  run are left in place, so several exports can populate one directory.
flags: |
      --dest string         Directory to write keys and manifest to
      --format string       Private key format to write: pem (PKCS#8, as stored) or bind (Private-key-format v1.3) (default "pem")
  -z, --zone stringArray    Zone to include (repeatable)
      --zones stringArray   Include this zone and everything below it (repeatable)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore sig0 bulk-import
use: bulk-import
short: Import SIG(0) keys from a directory into the keystore
long: |
  Import every SIG(0) key described by --src/manifest.yaml.
  
  A key that is not in the keystore is inserted. A key that is already there
  with identical content is left alone. A key that is already there with
  DIFFERENT content is reported and SKIPPED: the running keystore outranks a
  file that may be arbitrarily stale, and silently overwriting it is how a stale
  export un-rolls a rolled key.
  
  --force flips that last case to overwrite, which is what you want when
  recovering a keystore you know to be wrong.
flags: |
      --force        Overwrite keystore entries that differ (default: report and skip them)
      --src string   Directory to read keys and manifest from
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore sig0 delete
use: delete
short: Delete SIG(0) key pair from the keystore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore sig0 export
use: export
short: Export a SIG(0) key pair from the keystore as BIND-style .private/.key files
long: |
  Write the SIG(0) key pair for (zone, keyid) to two files in BIND filename
  convention: K<zone>+<alg-num>+<keyid>.private and .key (zone-file KEY RR).
  The private half is written as PKCS#8 PEM, or as bind's Private-key-format
  v1.3 with --format bind. The resulting pair is directly consumable by commands
  accepting --key <basename.private>.
flags: |
      --format string   Private key format to write: pem (PKCS#8, as stored) or bind (Private-key-format v1.3) (default "pem")
      --keyid int       Key ID of key to export
  -o, --outdir string   Directory to write .private and .key files to (default ".")
  -z, --zone string     Zone the key belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore sig0 generate
use: generate
short: Generate a new SIG(0) key pair and add it to the keystore
flags: |
  -a, --algorithm string   Algorithm to use for SIG(0) key generation (use the 'algorithms' subcommand to list what the server supports)
      --state string       Inital key state (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore sig0 import
use: import
short: Add a new SIG(0) key pair to the keystore
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to import SIG(0) key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent keystore sig0 list
use: list
short: List all SIG(0) key pairs in the keystore
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore sig0 setstate
use: setstate
short: Set the state of and existing SIG(0) key pair in the keystore
flags: |
      --keyid int      Key ID of key to delete
      --state string   New state of key (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore tsig
use: tsig
short: Manage global TSIG keys in the keystore
long: |
  Global TSIG keystore (no --zone). Keys are DB-backed with origin=api
  for keys created here; config keys are managed via keys.tsig.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore tsig add
use: add
short: Add a TSIG key with a known secret
flags: |
      --algorithm string     HMAC algorithm (default "hmac-sha256")
      --force                Overwrite on secret/algorithm conflict
      --name string          TSIG key name
      --owner string         Owner label (default api) (default "api")
      --secret string        Inline TSIG secret (base64). WARNING: visible in shell history / process list; prefer --secret-file
      --secret-file string   File containing the base64 TSIG secret; preferred over --secret
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore tsig bulk-export
use: bulk-export
short: Export TSIG keys from the keystore to a directory
long: |
  Export TSIG keys to --dest as BIND-convention key files plus a manifest
  that carries what those files cannot (key state, rollover timestamps).
  
  Selection is additive: --key takes one exact name, --keys takes a name and
  everything below it, and both may be repeated. Subtree matching is on label
  boundaries, so --keys pq.dnslab does not match notpq.dnslab. With no selector at
  all, the whole class is exported.
  
  Re-exporting into a directory MERGES: entries for keys not covered by this
  run are left in place, so several exports can populate one directory.
flags: |
      --dest string        Directory to write keys and manifest to
      --key stringArray    TSIG key name to include (repeatable)
      --keys stringArray   Include this name and everything below it (repeatable)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore tsig bulk-import
use: bulk-import
short: Import TSIG keys from a directory into the keystore
long: |
  Import every TSIG key described by --src/manifest.yaml.
  
  A key that is not in the keystore is inserted. A key that is already there
  with identical content is left alone. A key that is already there with
  DIFFERENT content is reported and SKIPPED: the running keystore outranks a
  file that may be arbitrarily stale, and silently overwriting it is how a stale
  export un-rolls a rolled key.
  
  --force flips that last case to overwrite, which is what you want when
  recovering a keystore you know to be wrong.
flags: |
      --force        Overwrite keystore entries that differ (default: report and skip them)
      --src string   Directory to read keys and manifest from
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore tsig delete
use: delete
short: Delete an api-origin TSIG key
flags: |
      --name string   TSIG key name
  -y, --yes           Skip confirmation prompt
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore tsig export
use: export <keyname>
short: Print a TSIG key's secret (default) or a full BIND/NSD key block, to stdout
long: |
  Print a TSIG key's base64 secret to stdout with NO trailing newline and
  nothing else, so it can be captured inline in another command (e.g. via
  shell backticks or $(...)) to TSIG-sign a dog query or transfer:
  
      dog @srv -y name.:$(tdns-cli auth keystore tsig export name) zone. axfr
  
  With --bind or --nsd, print a complete BIND9 or NSD key block instead
  (still on stdout). Errors go to stderr so stdout stays clean for capture.
flags: |
      --bind   Output a complete BIND9 key { ... } block
      --nsd    Output a complete NSD key: block
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore tsig generate
use: generate
short: Generate a new TSIG key and add it to the keystore
flags: |
      --algorithm string   HMAC algorithm (default "hmac-sha256")
      --name string        TSIG key name
      --owner string       Owner label (default api) (default "api")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore tsig import
use: import
short: Import TSIG keys from a BIND or NSD config snippet
long: |
  Scan a config file for TSIG key blocks (not a full config parser).
  Default: import new keys and skip conflicts. --force overwrites all conflicts;
  --interactive prompts per conflict (two-phase round-trip).
flags: |
  -f, --file string     File containing TSIG key declarations
      --force           Overwrite all secret/algorithm conflicts
      --format string   Key syntax: bind or nsd (default "bind")
      --interactive     Prompt per conflict before overwriting
      --owner string    Owner label for imported keys (default "api")
  -v, --verbose         List per-key disposition
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli agent keystore tsig list
use: list
short: List TSIG keys (no secrets)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore tsig purge
use: purge
short: Delete unreferenced api-origin TSIG keys owned by api
long: |
  Dry-run by default: lists purge candidates (origin=api, owner=api,
  zero zone references) and deletes nothing. Pass --force to delete all
  candidates, or --interactive to prompt per key.
flags: |
      --force         Actually delete; otherwise dry-run
      --interactive   Prompt per purge candidate
  -y, --yes           Skip confirmation when used with --force
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent keystore tsig setowner
use: setowner
short: Change owner on an api-origin TSIG key
flags: |
      --name string    TSIG key name
      --owner string   New owner label
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent local
use: local
short: TDNS Agent local commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent local config
use: config
short: Show details of the local agent config
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent local zonedata
use: zonedata
short: TDNS Agent local zone data commands (adding or removing local data about zones)
flags: |
      --RR string   DNS record to add
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent local zonedata add-rr
use: add-rr
short: Add a new local DNS record for an existing zone
inherited: |
      --RR string       DNS record to add
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent local zonedata remove-rr
use: remove-rr
short: Remove a local DNS record for an existing zone
inherited: |
      --RR string       DNS record to add
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent peer
use: peer
short: Peer agent commands (list, ping, zones, resync)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent peer apiping
use: apiping
short: Ping a peer via HTTPS API
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent peer list
use: list
short: List all known peer agents
long: |
  Show all peer agents that this agent has discovered and established communication with.
  Displays both API and DNS transports independently with their current state.
  
  This shows all peers regardless of transport type - both API (TLS) and DNS (JOSE) transports
  are displayed as separate entries to show their independent states.
flags: |
  -v, --verbose   Verbose output (show full details)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli agent peer ping
use: ping
short: Ping a peer via DNS CHUNK
long: |
  Send a DNS CHUNK ping to a peer and report the result.
  The --id flag specifies the peer identity (e.g. agent.beta.dnslab.
  or combiner.dnslab.).
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent peer reset
use: reset
short: Reset peer (agent/auditor only; no-op elsewhere)
long: |
  Reset a peer to initial NEEDED state. Flushes all IMR
  cache entries for the peer's discovery names and restarts
  discovery from scratch. Use this when a peer is stuck in UNKNOWN
  or KNOWN state. Only applicable to roles that use dynamic
  HSYNC3-driven discovery (agent, auditor); a no-op on
  static-peer roles (signer, combiner).
flags: |
      --id string   Peer identity to reset (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent peer resync
use: resync
short: Re-synchronize zone data with peers and combiner
long: |
  Re-synchronize zone data between this agent, its peers, and the combiner.
  
  Modes:
    --push   Re-send all local data to combiner and remote agents
    --pull   Request all remote agents to re-send their data (RFI SYNC)
    --full   Both push and pull (default if no flag specified)
flags: |
      --full   Both push and pull (default)
      --pull   Request peers to re-send their data (RFI SYNC)
      --push   Re-send local data to combiner and peers
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent peer zone
use: zone --zone <zonename>
short: List peer agents for a specific zone
long: |
  Show which peer agents share a specific zone with us.
  Displays all agents that have the specified zone in their shared zones list.
flags: |
  -z, --zone string   Zone name to list agents for (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent peer zones
use: zones
short: List shared zones for each peer agent
long: |
  Show which zones are shared with each peer agent.
  Displays agent identity and their shared zones in a compact format.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent ping
use: ping
short: Send an API ping request and present the response
flags: |
  -c, --count int   #pings to send
  -n, --newapi      use new api client
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent router
use: router
short: DNS message router introspection commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent router describe
use: describe
short: Show detailed router state
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent router list
use: list
short: List all registered message handlers
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent router metrics
use: metrics
short: Show router metrics (use --detailed for per-peer breakdown)
flags: |
      --detailed   Show per-peer breakdown
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent router reset
use: reset
short: Reset router metrics
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent router walk
use: walk
short: Walk all handlers with visitor pattern
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent stop
use: stop
short: Send stop command to the daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent transaction
use: transaction
short: Transaction diagnostics
long: |
  Commands for diagnosing open transactions, pending confirmations, and errors.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent transaction errors
use: errors
short: Show recent transaction errors (combiner only)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent transaction open
use: open
short: Show open transactions
long: |
  Show transactions that have not yet been confirmed.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent transaction open incoming
use: incoming
short: Show open incoming transactions (remote syncs awaiting combiner)
long: |
  Show incoming remote syncs that have been forwarded to the combiner but not yet confirmed.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent transaction open outgoing
use: outgoing
short: Show open outgoing transactions
long: |
  Show outgoing transactions that have not yet been confirmed by the receiver.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent truststore
use: truststore
short: Prefix command to access different features of the truststore
long: |
  The truststore is where SIG(0) public keys for child zones are kept.
  The CLI contains functions for listing trusted SIG(0) keys, adding and
  deleting child keys as well as changing the trust state of individual keys.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent truststore sig0
use: sig0
short: Prefix command, only usable via sub-commands
flags: |
  -c, --child string   Name of child SIG(0) key
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent truststore sig0 add
use: add
short: Add a new SIG(0) public key to the truststore
flags: |
  -s, --src string   Source for SIG(0) public key, a file name or 'dns'
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent truststore sig0 delete
use: delete
short: Delete a SIG(0) public key from the truststore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent truststore sig0 list
use: list
short: List all child SIG(0) public key in the keystore
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent truststore sig0 trust
use: trust
short: Declare a child SIG(0) public key in the keystore as trusted
flags: |
      --keyid int   Keyid of child SIG(0) key to change trust for
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent truststore sig0 untrust
use: untrust
short: Declare a child SIG(0) public key in the keystore as untrusted
flags: |
      --keyid int   Keyid of child SIG(0) key to change trust for
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone
use: zone
short: Agent zone management commands
flags: |
  -F, --force   force operation
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone addrr
use: addrr
short: Add a resource record to a zone and sync with peers and combiner
long: |
  Add a resource record to the specified zone. Supported RR types: NS, DNSKEY, CDS, CSYNC, KEY.
  The RR type is inferred from the --rr argument.
  
  This will:
  - Add the RR to the local synced data store
  - Send sync operations to all remote agents for this zone
  - Send the update to the combiner
  
  Examples:
    tdns-cliv2 agent zone addrr --zone whisky.dnslab. --rr "whisky.dnslab. IN NS ns.alpha.dnslab."
    tdns-cliv2 agent zone addrr --zone whisky.dnslab. --rr "whisky.dnslab. 300 IN DNSKEY 257 3 13 <base64>"
flags: |
      --force       Bypass dedup check and always send transaction
      --rr string   DNS record to add
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone bump
use: bump
short: Bump the SOA serial of an agent-served zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone delrr
use: delrr
short: Delete a resource record from a zone and sync with peers and combiner
long: |
  Delete a resource record from the specified zone. Supported RR types: NS, DNSKEY, CDS, CSYNC, KEY.
  The RR type is inferred from the --rr argument.
  
  This will:
  - Remove the RR from the local synced data store
  - Send sync operations to all remote agents for this zone
  - Send the update to the combiner
  
  Examples:
    tdns-cliv2 agent zone delrr --zone whisky.dnslab. --rr "whisky.dnslab. IN NS ns.alpha.dnslab."
    tdns-cliv2 agent zone delrr --zone whisky.dnslab. --rr "whisky.dnslab. 300 IN DNSKEY 257 3 13 <base64>"
flags: |
      --force       Bypass dedup check and always send transaction
      --rr string   DNS record to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone dsync
use: dsync
short: Prefix command, not useable by itself
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone dsync bootstrap-sig0-key
use: bootstrap-sig0-key
short: Send dsync bootstrap command to tdns-auth
flags: |
  -a, --algorithm string   Algorithm to use for the new SIG(0) key (default "ED25519")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone dsync publish
use: publish
short: Send dsync publish-dsync-rrset command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone dsync roll-sig0-key
use: roll-sig0-key
short: Send dsync rollover command to tdns-auth
flags: |
  -a, --algorithm string   Algorithm to use for the new SIG(0) key (default "ED25519")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone dsync status
use: status
short: Send dsync status command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone dsync unpublish
use: unpublish
short: Send dsync unpublish-dsync-rrset command to tdns-auth
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone edits
use: edits
short: Agent edit/sync status commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone edits list
use: list
short: Show synchronized data and tracking state
long: |
  Display the agent's SynchedDataEngine state showing contributions from all peer agents.
  
  Without --zone: summary view sorted by zone → source agent → RRtype → RRs
  With --zone:    detailed per-RR tracking state and outbound queue status
  
  Example:
    tdns-cliv2 agent zone edits list
    tdns-cliv2 agent zone edits list --zone example.com
flags: |
      --zone string   Show detailed status for a specific zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent zone list
use: list
short: List configured zones
flags: |
  -f, --file      Show zone input file
  -N, --notify    Show zone downstream notify addresses
  -P, --primary   Show zone primary nameserver
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone mplist
use: mplist
short: List multi-provider zones with HSYNCPARAM details
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone readfake
use: readfake
short: Create a fake zone from a compiled in string
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone reload
use: reload
short: Request re-loading a zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone update
use: update
short: Create and ultimately send a DNS UPDATE msg for zone auth data
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli agent zone update create
use: create
short: Create and ultimately send a DNS UPDATE msg for zone auth data
long: |
  Will query for details about the DNS UPDATE via (add|del|show|set-ttl) commands.
  When the message is complete it may be signed and sent by the 'send' command. After a
  message has been send the loop will start again with a new, empty message to create.
  Loop ends on the command "QUIT"
  
  The zone to update is mandatory to specify on the command line with the --zone flag.
flags: |
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli agent zone write
use: write
short: Write a zone to disk
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor
use: auditor
short: Interact with the MP auditor via API
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor config
use: config
short: Commands to reload config, reload zones, etc
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor config check
use: check [config-file]
short: Validate the "auditor" daemon config and correlate it with the running daemon
long: |
  Validate a "auditor" daemon configuration file for completeness and internal
  consistency, and (unless --offline) correlate it against the running daemon to
  report drift between the file on disk and what the server actually loaded.
  
  Target config resolution:
    1. an explicit path (positional arg or --serverconfig)
    2. otherwise the config-file: recorded for "auditor" in the CLI config, else the compiled-in default for its app type
  
  Exit status is non-zero if any check FAILs (WARNs do not fail the run).
flags: |
      --offline               do not contact the daemon; run static checks only
      --serverconfig string   path to the "auditor" daemon config file to check (default: ask the daemon, else the configured/compiled-in default)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor config mwe
use: mwe
short: Generate a minimal working example config (+ certs, zones) for tdns-auditor
long: |
  Generate a Minimal Working Example configuration for tdns-auditor.
  
  Writes a single self-contained YAML to <dir>/tdns-auditor.yaml (or, if that
  file already exists, to <dir>/tdns-auditor.yaml.mwe so nothing is
  clobbered). Also generates, unless already present:
  
    - a self-signed cert/key pair under <dir>/certs (for the API server and the
      encrypted DNS transports)
    - two example primary zones — one unsigned, one signed — using two different
      zone templates, with generated zone files under <dir>/zones
    - a commented-out secondary zone using a third template
  
  The DNS engine listens on IPv4 and IPv6 localhost; extra listen addresses are
  shown commented out. The generated tree is designed to pass
  `tdns-cli auditor config check`.
flags: |
      --dir string   base directory for the generated config, certs and zones (default "/etc/tdns")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor config reload
use: reload
short: Send config reload command to tdns-auditor
flags: |
      --confirm   apply even if the reload would strand a signed zone by a same-name DNSSEC policy algorithm change
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor config reload-tsig
use: reload-tsig
short: Reconcile keys.tsig into the TSIG keystore (config reload-tsig)
long: |
  Re-read keys.tsig from the config file and reconcile into the DB-backed
  TSIG keystore. Secret conflicts are withheld by default; use --force to
  overwrite all conflicts, or --interactive to prompt per conflict.
flags: |
      --force         overwrite all secret/algorithm conflicts with keys.tsig
      --interactive   prompt per conflict before overwriting
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor config reload-zones
use: reload-zones
short: Send reload-zones command to tdns-auditor
flags: |
      --confirm   apply even if the reload would strand a signed zone by a same-name DNSSEC policy algorithm change
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor config status
use: status
short: Send config status command to tdns-auditor
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor daemon
use: daemon
short: Only useful via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor daemon api
use: api
short: request an API summary from the tdns-auditor daemon
long: |
  The daemon api command queries tdns-auditor for the provided API
  and prints that out in a (hopefully) comprehensible fashion.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor daemon reload
use: reload
short: Reload config from file
long: |
  Reload config from file (the assumption is that something in the config has changed).
  Right now this doesn't do much, but later on various services will be able to restart.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor daemon restart
use: restart
short: Stop and then start the management daemon
flags: |
      --clear string   Truncate the specified log file before starting
      --update         Update the server binary from /tmp/{binary} to /usr/local/libexec/ before starting
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor daemon start
use: start
short: Start the tdns-auditor daemon
long: |
  Start the tdns-auditor daemon. If it was already running, then this is a no-op.
flags: |
  -W, --maxwait int   Max seconds to wait until declaring start to have failed (default 5)
      --slurp         Slurp stdout/stderr for errors (debug tool only)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor daemon status
use: status
short: Query for the status of the management daemon
long: |
  Query for the status of the management daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor daemon stop
use: stop
short: Stop the management daemon
long: |
  Stop the management daemon. If it was not running, then this is a no-op.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor debug
use: debug
short: Debug commands against the configured daemon
flags: |
      --qname string   qname of rrset to examine
      --qtype string   qtype of rrset to examine
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor debug lav
use: lav
short: Lookup and validate a child RRset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor debug rrset
use: rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor debug show-rrsetcache
use: show-rrsetcache
short: List cached RRsets
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor debug show-ta
use: show-ta
short: List known DNSSEC trust anchors
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor debug sig0
use: sig0
short: SIG(0) key helper commands (local)
flags: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
  -r, --rrtype string      rrtype to use for SIG(0)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor debug sig0 generate
use: generate
inherited: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
      --config string      config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug              debug output
  -H, --headers            show headers
  -Z, --pzone string       parent zone name
      --qname string       qname of rrset to examine
      --qtype string       qtype of rrset to examine
  -r, --rrtype string      rrtype to use for SIG(0)
  -v, --verbose            verbose output
  -z, --zone string        zone name

=== tdns-mpcli auditor debug validate-rrset
use: validate-rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor debug zone-txlog
use: zone-txlog
short: Show staged-but-unpublished zone changes
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor distrib
use: distrib
short: Manage distributions
long: |
  Commands for managing distributions created by this auditor, including listing distributions and purging completed distributions.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor distrib list
use: list
short: List all distributions from this auditor
flags: |
  -v, --verbose   Verbose output (show full details)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli auditor distrib peers
use: peers
short: List all known peer agents with working keys
long: |
  Show all recipient agents that this auditor has established keys with and can send distributions to.
flags: |
  -v, --verbose   Verbose output (show full details)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli auditor distrib purge
use: purge [--force]
short: Delete distributions
long: |
  Delete distributions from the database. By default, only completed distributions are deleted. Use --force to delete ALL distributions regardless of status.
flags: |
      --force   Delete ALL distributions (not just completed ones)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor eventlog
use: eventlog
short: Audit event log commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor eventlog clear
use: clear
short: Clear audit events
flags: |
      --all                 clear all events
      --older-than string   clear events older than duration (e.g. 24h)
  -z, --zone string         clear events for zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor eventlog list
use: list
short: List audit events
flags: |
      --last int       number of events to show (default 50)
      --since string   events since (RFC3339)
  -z, --zone string    filter by zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor gossip
use: gossip
short: Gossip protocol commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor gossip state
use: state
short: Show gossip state matrix for a zone
long: |
  Display the N×N state matrix for the provider group serving a zone.
  Each row is a reporting peer; each column shows that reporter's
  view of another peer's state. A healthy group shows OPERATIONAL
  in every non-diagonal cell.
flags: |
  -z, --zone string   Zone name (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore
use: keystore
short: Prefix command to access different features of the keystore
long: |
  The keystore holds SIG(0), DNSSEC, and global TSIG keys.
  The CLI contains functions for listing, adding, deleting, and
  changing the state of keys.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec
use: dnssec
short: Prefix command, only usable via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec add
use: add
short: Add a new DNSSEC key pair to the keystore
long: |
  Add a new SIG(0) key pair to the keystore. Required arguments are the name of the file
  containing either the private or the public SIG(0) key and the name of the zone.
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to add DNSSEC key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec algorithms
use: algorithms
short: List the DNSSEC algorithms the server supports
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec bulk-convert
use: bulk-convert
short: Convert a directory of bind9 DNSSEC keys into one tdns can pre-load
long: |
  Convert bind9-generated DNSSEC keys in --dir into the form keystore.preload reads.
  
  Two things stand between dnssec-keygen output and a pre-loadable directory: the
  private half is in bind's own format rather than PKCS#8 PEM, and there is no
  manifest, without which pre-load refuses the directory outright.
  
  This rewrites each .private in place as PEM, keeping the original alongside as
  .private.orig, and writes the manifest. The .key files are NOT touched: they
  already hold the public RR in exactly the form tdns exports.
  
  Key state comes from bind's .state file (9.16+, dnssec-policy zones), mapped
  onto tdns's states. A key with no .state file has no state to read, and it is
  not guessed -- pass --state to say which one applies. Guessing wrong is how a
  retired key gets published again.
  
  Runs entirely locally: no daemon, no API, no keystore. That is the point --
  these directories are built and committed before the server that serves them
  exists. Re-running is safe; already-converted keys are left alone.
flags: |
      --dir string     Directory of bind9 key files to convert in place
      --no-backup      Do not keep the original bind-format key as .private.orig
      --state string   Key state for keys with no .state file (required only for those)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec bulk-export
use: bulk-export
short: Export DNSSEC keys from the keystore to a directory
long: |
  Export DNSSEC keys to --dest as BIND-convention key files plus a manifest
  that carries what those files cannot (key state, rollover timestamps).
  
  Selection is additive: --zone takes one exact name, --zones takes a name and
  everything below it, and both may be repeated. Subtree matching is on label
  boundaries, so --zones pq.dnslab does not match notpq.dnslab. With no selector at
  all, the whole class is exported.
  
  Re-exporting into a directory MERGES: entries for keys not covered by this
  run are left in place, so several exports can populate one directory.
flags: |
      --dest string         Directory to write keys and manifest to
      --format string       Private key format to write: pem (PKCS#8, as stored) or bind (Private-key-format v1.3) (default "pem")
  -z, --zone stringArray    Zone to include (repeatable)
      --zones stringArray   Include this zone and everything below it (repeatable)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec bulk-import
use: bulk-import
short: Import DNSSEC keys from a directory into the keystore
long: |
  Import every DNSSEC key described by --src/manifest.yaml.
  
  A key that is not in the keystore is inserted. A key that is already there
  with identical content is left alone. A key that is already there with
  DIFFERENT content is reported and SKIPPED: the running keystore outranks a
  file that may be arbitrarily stale, and silently overwriting it is how a stale
  export un-rolls a rolled key.
  
  --force flips that last case to overwrite, which is what you want when
  recovering a keystore you know to be wrong.
flags: |
      --force        Overwrite keystore entries that differ (default: report and skip them)
      --src string   Directory to read keys and manifest from
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec clear
use: clear
short: Permanently delete all DNSSEC keys for a zone (KeyStateWorker will regenerate as needed)
flags: |
      --force         Skip confirmation prompt
  -z, --zone string   Zone to clear all DNSSEC keys for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec delete
use: delete
short: Delete DNSSEC key pair from the keystore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec ds-push
use: ds-push
short: Compute DS RRset from keystore and push to parent (UPDATE-only in this offline mode)
long: |
  Loads tdns config (same as other CLI commands using -c), opens the local keystore DB,
  and pushes the whole DS RRset to the parent. Requires imrengine in config.
  
  Offline mode: this CLI invocation builds a stub *ZoneData with no rollover policy
  attached, so PushDSRRsetForRollover falls through to the legacy single-scheme
  UPDATE path (whole-DS replacement, signed with the zone's active SIG(0) key).
  The auto / prefer-* / force-* dsync-scheme-preference values are honored only
  inside the daemon's rollover engine, where the policy is loaded from
  dnssecpolicies. To exercise NOTIFY pushes, run the daemon and let
  RolloverAutomatedTick drive the push.
  
  Use --dry-run to print the DS set and the UPDATE without sending.
flags: |
      --dry-run       Print DS RRset and UPDATE only; do not send
  -z, --zone string   Child zone (owner of DS RRset)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec export
use: export
short: Export a DNSSEC key pair from the keystore as BIND-style .private/.key files
long: |
  Write the DNSSEC key pair for (zone, keyid) to two files in BIND filename
  convention: K<zone>+<alg-num>+<keyid>.private and .key (zone-file DNSKEY RR).
  The private half is written as PKCS#8 PEM, or as bind's Private-key-format
  v1.3 with --format bind. The resulting pair is directly consumable by
  'keystore dnssec import'.
flags: |
      --format string   Private key format to write: pem (PKCS#8, as stored) or bind (Private-key-format v1.3) (default "pem")
      --keyid int       Key ID of key to export
  -o, --outdir string   Directory to write .private and .key files to (default ".")
  -z, --zone string     Zone the key belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec gen-ds
use: gen-ds
short: Generate DS records for a zone's KSK(s) from the keystore
long: |
  Generate DS (Delegation Signer) records for a zone's KSK (Key Signing Key) DNSKEY records stored in the keystore. The command queries the keystore for DNSKEY records for the specified zone, filters for KSKs (keys with the SEP bit set), and generates DS records using SHA-256 and SHA-384 digest algorithms. If --keyid is not specified, DS records are generated for all KSKs in the zone.
flags: |
      --keyid int     Key ID of specific KSK to generate DS for (optional, if not specified, generates for all KSKs)
  -z, --zone string   Zone to generate DS records for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec generate
use: generate
short: Generate a new DNSSEC key pair and add it to the keystore
flags: |
  -a, --algorithm string   Algorithm to use for DNSSEC key generation (use the 'algorithms' subcommand to list what the server supports)
      --keytype string     Key type to generate (KSK|ZSK|CSK)
      --state string       Inital key state (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec import
use: import
short: Add a new DNSSEC key pair to the keystore
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to import DNSSEC key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec list
use: list
short: List all DNSSEC key pairs in the keystore
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec policies
use: policies
short: List the DNSSEC policies the server loaded (including any in error)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec policy
use: policy
short: DNSSEC policy utilities
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec policy validate
use: validate
short: Validate a YAML fragment with a dnssec.policies: block
long: |
  Reads a YAML file that contains the same dnssec.policies: structure as tdns-auth
  config (policy names as keys, algorithm / ksk / zsk / csk / optional rollover+ttls+clamping).
  Runs the same validation as runtime config load. Exits non-zero on any error.
flags: |
  -f, --file string   YAML file path
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore dnssec policy-cleanup
use: policy-cleanup
short: Remove a zone's retired keys (and their RRSIGs) now, keeping active keys
long: |
  After a DNSSEC policy change, the old keys are retired but kept (with their
  signatures) so the zone stays validatable while the new keys take over —
  leaving the zone briefly double-signed. policy-cleanup collapses that window
  early: it removes the retired keys and strips their RRSIGs immediately,
  keeping the active keys. Unlike 'clear' (which deletes ALL keys and
  regenerates), this only touches retired keys.
  
  Accelerating removal means a resolver still caching only an old (now-removed)
  DNSKEY briefly cannot validate until it re-queries; the active keys already
  serve. Normally you can just wait for the KeyStateWorker to age the retired
  keys out after propagation-delay.
flags: |
      --force         Skip confirmation prompt
  -z, --zone string   Zone to clean up retired keys for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec purge
use: purge
short: Delete keys in 'removed' state, keeping the 3 most recent per zone
long: |
  Delete keys in 'removed' state from the keystore, keeping the 3
  most recent per zone (by insert order). Use --zone all to apply to
  every zone with removed keys at once.
  
  Dry-run by default: prints the keys that would be deleted and exits
  without modifying anything. Pass --force to actually delete.
flags: |
      --force         Actually delete; otherwise dry-run
  -z, --zone string   Zone to purge ('all' for every zone)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec query-parent
use: query-parent
short: Query configured parent-agent for child DS (poll until match or timeout)
long: |
  Uses rollover.parent-agent from the zone's dnssec policy (addr:port), or --parent-agent.
  Queries that address over TCP for the zone's DS RRset and compares to the keystore-derived
  expected set (ComputeTargetDSSetForZone, §7.5). Default poll schedule uses policy confirm-* timings,
  or 2s / 60s / 1h when those are unset.
  
  --once performs a single query and exits (no backoff loop).
flags: |
      --once                  Single TCP query; do not poll
      --parent-agent string   Override policy rollover.parent-agent (host:port)
  -z, --zone string           Child zone (owner of DS RRset)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec rollover
use: rollover
short: Perform a manual DNSSEC key rollover (standby→active, active→retired)
flags: |
      --keytype string   Key type to roll over (ZSK|KSK) (default "ZSK")
  -z, --zone string      Zone to perform rollover for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore dnssec setstate
use: setstate
short: Set the state of and existing DNSSEC key pair in the keystore
flags: |
      --keyid int      Key ID of key to delete
      --state string   New statei of key
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore sig0
use: sig0
short: Prefix command, only usable via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore sig0 add
use: add
short: Add a new SIG(0) key pair to the keystore
long: |
  Add a new SIG(0) key pair to the keystore. Required arguments are the name of the file
  containing either the private or the public SIG(0) key and the name of the zone.
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to add SIG(0) key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore sig0 algorithms
use: algorithms
short: List the SIG(0) algorithms the server supports
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore sig0 bulk-convert
use: bulk-convert
short: Convert a directory of bind9 SIG(0) keys into one tdns can pre-load
long: |
  Convert bind9-generated SIG(0) keys in --dir into the form keystore.preload reads.
  
  Two things stand between dnssec-keygen output and a pre-loadable directory: the
  private half is in bind's own format rather than PKCS#8 PEM, and there is no
  manifest, without which pre-load refuses the directory outright.
  
  This rewrites each .private in place as PEM, keeping the original alongside as
  .private.orig, and writes the manifest. The .key files are NOT touched: they
  already hold the public RR in exactly the form tdns exports.
  
  Key state comes from bind's .state file (9.16+, dnssec-policy zones), mapped
  onto tdns's states. A key with no .state file has no state to read, and it is
  not guessed -- pass --state to say which one applies. Guessing wrong is how a
  retired key gets published again.
  
  Runs entirely locally: no daemon, no API, no keystore. That is the point --
  these directories are built and committed before the server that serves them
  exists. Re-running is safe; already-converted keys are left alone.
flags: |
      --dir string     Directory of bind9 key files to convert in place
      --no-backup      Do not keep the original bind-format key as .private.orig
      --state string   Key state for keys with no .state file (required only for those)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore sig0 bulk-export
use: bulk-export
short: Export SIG(0) keys from the keystore to a directory
long: |
  Export SIG(0) keys to --dest as BIND-convention key files plus a manifest
  that carries what those files cannot (key state, rollover timestamps).
  
  Selection is additive: --zone takes one exact name, --zones takes a name and
  everything below it, and both may be repeated. Subtree matching is on label
  boundaries, so --zones pq.dnslab does not match notpq.dnslab. With no selector at
  all, the whole class is exported.
  
  Re-exporting into a directory MERGES: entries for keys not covered by this
  run are left in place, so several exports can populate one directory.
flags: |
      --dest string         Directory to write keys and manifest to
      --format string       Private key format to write: pem (PKCS#8, as stored) or bind (Private-key-format v1.3) (default "pem")
  -z, --zone stringArray    Zone to include (repeatable)
      --zones stringArray   Include this zone and everything below it (repeatable)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore sig0 bulk-import
use: bulk-import
short: Import SIG(0) keys from a directory into the keystore
long: |
  Import every SIG(0) key described by --src/manifest.yaml.
  
  A key that is not in the keystore is inserted. A key that is already there
  with identical content is left alone. A key that is already there with
  DIFFERENT content is reported and SKIPPED: the running keystore outranks a
  file that may be arbitrarily stale, and silently overwriting it is how a stale
  export un-rolls a rolled key.
  
  --force flips that last case to overwrite, which is what you want when
  recovering a keystore you know to be wrong.
flags: |
      --force        Overwrite keystore entries that differ (default: report and skip them)
      --src string   Directory to read keys and manifest from
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore sig0 delete
use: delete
short: Delete SIG(0) key pair from the keystore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore sig0 export
use: export
short: Export a SIG(0) key pair from the keystore as BIND-style .private/.key files
long: |
  Write the SIG(0) key pair for (zone, keyid) to two files in BIND filename
  convention: K<zone>+<alg-num>+<keyid>.private and .key (zone-file KEY RR).
  The private half is written as PKCS#8 PEM, or as bind's Private-key-format
  v1.3 with --format bind. The resulting pair is directly consumable by commands
  accepting --key <basename.private>.
flags: |
      --format string   Private key format to write: pem (PKCS#8, as stored) or bind (Private-key-format v1.3) (default "pem")
      --keyid int       Key ID of key to export
  -o, --outdir string   Directory to write .private and .key files to (default ".")
  -z, --zone string     Zone the key belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore sig0 generate
use: generate
short: Generate a new SIG(0) key pair and add it to the keystore
flags: |
  -a, --algorithm string   Algorithm to use for SIG(0) key generation (use the 'algorithms' subcommand to list what the server supports)
      --state string       Inital key state (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore sig0 import
use: import
short: Add a new SIG(0) key pair to the keystore
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to import SIG(0) key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor keystore sig0 list
use: list
short: List all SIG(0) key pairs in the keystore
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore sig0 setstate
use: setstate
short: Set the state of and existing SIG(0) key pair in the keystore
flags: |
      --keyid int      Key ID of key to delete
      --state string   New state of key (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore tsig
use: tsig
short: Manage global TSIG keys in the keystore
long: |
  Global TSIG keystore (no --zone). Keys are DB-backed with origin=api
  for keys created here; config keys are managed via keys.tsig.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore tsig add
use: add
short: Add a TSIG key with a known secret
flags: |
      --algorithm string     HMAC algorithm (default "hmac-sha256")
      --force                Overwrite on secret/algorithm conflict
      --name string          TSIG key name
      --owner string         Owner label (default api) (default "api")
      --secret string        Inline TSIG secret (base64). WARNING: visible in shell history / process list; prefer --secret-file
      --secret-file string   File containing the base64 TSIG secret; preferred over --secret
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore tsig bulk-export
use: bulk-export
short: Export TSIG keys from the keystore to a directory
long: |
  Export TSIG keys to --dest as BIND-convention key files plus a manifest
  that carries what those files cannot (key state, rollover timestamps).
  
  Selection is additive: --key takes one exact name, --keys takes a name and
  everything below it, and both may be repeated. Subtree matching is on label
  boundaries, so --keys pq.dnslab does not match notpq.dnslab. With no selector at
  all, the whole class is exported.
  
  Re-exporting into a directory MERGES: entries for keys not covered by this
  run are left in place, so several exports can populate one directory.
flags: |
      --dest string        Directory to write keys and manifest to
      --key stringArray    TSIG key name to include (repeatable)
      --keys stringArray   Include this name and everything below it (repeatable)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore tsig bulk-import
use: bulk-import
short: Import TSIG keys from a directory into the keystore
long: |
  Import every TSIG key described by --src/manifest.yaml.
  
  A key that is not in the keystore is inserted. A key that is already there
  with identical content is left alone. A key that is already there with
  DIFFERENT content is reported and SKIPPED: the running keystore outranks a
  file that may be arbitrarily stale, and silently overwriting it is how a stale
  export un-rolls a rolled key.
  
  --force flips that last case to overwrite, which is what you want when
  recovering a keystore you know to be wrong.
flags: |
      --force        Overwrite keystore entries that differ (default: report and skip them)
      --src string   Directory to read keys and manifest from
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore tsig delete
use: delete
short: Delete an api-origin TSIG key
flags: |
      --name string   TSIG key name
  -y, --yes           Skip confirmation prompt
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore tsig export
use: export <keyname>
short: Print a TSIG key's secret (default) or a full BIND/NSD key block, to stdout
long: |
  Print a TSIG key's base64 secret to stdout with NO trailing newline and
  nothing else, so it can be captured inline in another command (e.g. via
  shell backticks or $(...)) to TSIG-sign a dog query or transfer:
  
      dog @srv -y name.:$(tdns-cli auth keystore tsig export name) zone. axfr
  
  With --bind or --nsd, print a complete BIND9 or NSD key block instead
  (still on stdout). Errors go to stderr so stdout stays clean for capture.
flags: |
      --bind   Output a complete BIND9 key { ... } block
      --nsd    Output a complete NSD key: block
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore tsig generate
use: generate
short: Generate a new TSIG key and add it to the keystore
flags: |
      --algorithm string   HMAC algorithm (default "hmac-sha256")
      --name string        TSIG key name
      --owner string       Owner label (default api) (default "api")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore tsig import
use: import
short: Import TSIG keys from a BIND or NSD config snippet
long: |
  Scan a config file for TSIG key blocks (not a full config parser).
  Default: import new keys and skip conflicts. --force overwrites all conflicts;
  --interactive prompts per conflict (two-phase round-trip).
flags: |
  -f, --file string     File containing TSIG key declarations
      --force           Overwrite all secret/algorithm conflicts
      --format string   Key syntax: bind or nsd (default "bind")
      --interactive     Prompt per conflict before overwriting
      --owner string    Owner label for imported keys (default "api")
  -v, --verbose         List per-key disposition
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore tsig list
use: list
short: List TSIG keys (no secrets)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore tsig purge
use: purge
short: Delete unreferenced api-origin TSIG keys owned by api
long: |
  Dry-run by default: lists purge candidates (origin=api, owner=api,
  zero zone references) and deletes nothing. Pass --force to delete all
  candidates, or --interactive to prompt per key.
flags: |
      --force         Actually delete; otherwise dry-run
      --interactive   Prompt per purge candidate
  -y, --yes           Skip confirmation when used with --force
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor keystore tsig setowner
use: setowner
short: Change owner on an api-origin TSIG key
flags: |
      --name string    TSIG key name
      --owner string   New owner label
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor observations
use: observations
short: Show anomalies/observations detected by the auditor
flags: |
  -z, --zone string   filter by zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor peer
use: peer
short: Peer management commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor peer apiping
use: apiping
short: Ping a peer via HTTPS API
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor peer list
use: list
short: List all known peer agents
long: |
  Show all peer agents that this auditor has discovered and
  established communication with. Displays both API and DNS transports
  independently with their current state, plus leader-election status
  for every provider group the auditor observes.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor peer ping
use: ping
short: Ping a peer via DNS CHUNK
long: |
  Send a DNS CHUNK ping to a peer and report the result.
  The --id flag specifies the peer identity (e.g. agent.beta.dnslab.
  or combiner.dnslab.).
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor peer reset
use: reset
short: Reset peer (agent/auditor only; no-op elsewhere)
long: |
  Reset a peer to initial NEEDED state. Flushes all IMR
  cache entries for the peer's discovery names and restarts
  discovery from scratch. Use this when a peer is stuck in UNKNOWN
  or KNOWN state. Only applicable to roles that use dynamic
  HSYNC3-driven discovery (agent, auditor); a no-op on
  static-peer roles (signer, combiner).
flags: |
      --id string   Peer identity to reset (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor peer zones
use: zones
short: List shared zones for each peer agent
long: |
  Show which zones are shared with each peer agent.
  Displays agent identity and their shared zones in a compact format.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor ping
use: ping
short: Send an API ping request and present the response
flags: |
  -c, --count int   #pings to send
  -n, --newapi      use new api client
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor stop
use: stop
short: Send stop command to the daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor truststore
use: truststore
short: Prefix command to access different features of the truststore
long: |
  The truststore is where SIG(0) public keys for child zones are kept.
  The CLI contains functions for listing trusted SIG(0) keys, adding and
  deleting child keys as well as changing the trust state of individual keys.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor truststore sig0
use: sig0
short: Prefix command, only usable via sub-commands
flags: |
  -c, --child string   Name of child SIG(0) key
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor truststore sig0 add
use: add
short: Add a new SIG(0) public key to the truststore
flags: |
  -s, --src string   Source for SIG(0) public key, a file name or 'dns'
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor truststore sig0 delete
use: delete
short: Delete a SIG(0) public key from the truststore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor truststore sig0 list
use: list
short: List all child SIG(0) public key in the keystore
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor truststore sig0 trust
use: trust
short: Declare a child SIG(0) public key in the keystore as trusted
flags: |
      --keyid int   Keyid of child SIG(0) key to change trust for
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor truststore sig0 untrust
use: untrust
short: Declare a child SIG(0) public key in the keystore as untrusted
flags: |
      --keyid int   Keyid of child SIG(0) key to change trust for
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor web
use: web
short: Auditor web-dashboard commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor web user
use: user
short: Manage web dashboard users
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor web user create
use: create
short: Create a new web dashboard user (prompts for password)
flags: |
      --name string   username (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor web user delete
use: delete
short: Delete a web dashboard user
flags: |
      --name string   username (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor web user list
use: list
short: List configured web dashboard users
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor web user reload
use: reload
short: Tell the running auditor to re-read its users file
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone
use: zone
short: Prefix command, not usable by itself
flags: |
  -F, --force   force operation
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone add
use: add
short: Add a dynamic zone at runtime: secondary, or template-constrained primary (persists across restart)
flags: |
      --options strings           Zone options (comma-separated; secondary zones — primaries take options from the template)
      --primaries strings         Primary (upstream) addresses [host:port], comma-separated (secondary zones)
      --primary-key string        Primary TSIG key name applied to all primaries (NOKEY for none) (default "NOKEY")
      --template string           Config template for a primary zone (must carry dynamiczones: true)
      --transfer-src strings      Source address(es) to bind for this zone's outbound transfers (IP only, one per family)
      --tsig-algo string          Inline TSIG algorithm (default hmac-sha256)
      --tsig-name string          Inline TSIG key name; created in keystore if absent and applied to keyless primaries (secondary) or keyless downstreams (primary)
      --tsig-secret string        Inline TSIG secret (base64). WARNING: visible in shell history / process list; prefer --tsig-secret-file
      --tsig-secret-file string   File containing the inline TSIG secret (base64); preferred over --tsig-secret
      --type string               Zone type: secondary (default) or primary (requires --template) (default "secondary")
  -z, --zone string               Zone to add
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone bump
use: bump
short: Bump SOA serial and epoch (if any) in the daemon's version of the zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone childsync
use: childsync
short: Parent-side child delegation sync commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone childsync advert
use: advert
short: Print what the parent primary still lacks of the DSYNC advertisement, as an nsupdate block
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone childsync proxy-status
use: proxy-status
short: Report a childsync-proxy's advertisement and push state
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone childsync publish
use: publish
short: Publish the DSYNC RRset into the zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone childsync reconcile
use: reconcile
short: Reconcile the advertisement and the known children against the served zone now
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone childsync unpublish
use: unpublish
short: Unpublish the DSYNC RRset from the zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone delegation
use: delegation
short: Inspect the delegation data a parent zone holds for its children
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone delegation get
use: get
short: Show what this parent publishes for a child (or list its children)
long: |
  Report the delegation records the parent currently holds for a child zone,
  grouped by owner name and type.
  
  With no --child, list the children this parent has delegation data for -- the
  question that comes first when reconciling an external store against the
  server.
  
  The answer comes from the zone's delegation backend, so it is what the SERVER
  considers current. For a backend that records delegations somewhere other than
  the served zone those differ deliberately, and reading the zone instead would
  quietly give the wrong answer.
flags: |
      --child string   Child zone to report on; omit to list the parent's children
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone delete
use: delete
short: Delete a dynamic (API-managed) zone
flags: |
  -z, --zone string   Zone to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone desc
use: desc
short: Describe a single zone in full, including DNSSEC applied-policy state and policy detail
long: |
  Print a detailed, multi-line record for one zone: everything "zone list -v"
  shows (type, store, options, primaries, notify, error/warning state, effective
  DNSSEC policy and override info) plus two DNSSEC sections not otherwise visible
  from the CLI:
  
    1. the last-applied policy record recorded in the keystore (applied policy
       name, source config|command, and when it was applied); and
    2. the currently-bound policy's detail — mode, KSK/ZSK (or CSK) algorithms,
       key lifetimes and RRSIG validity.
  
  An unsigned zone or an unresolvable policy degrades gracefully rather than
  failing. Does not change anything; read-only.
flags: |
  -z, --zone string   Zone to describe
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone dnssec
use: dnssec
short: Zone DNSSEC operations: signing, policy, and automated rollover
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dnssec auto-rollover
use: auto-rollover
short: Manage and inspect automated KSK rollover (scheduled + manual-ASAP)
long: |
  Subcommands operate on local keystore state for a zone:
  
    when      — compute the earliest safe rollover moment (no state change)
    asap      — schedule a manual rollover at that earliest moment
    cancel    — clear a pending manual rollover request
    status    — print phase + per-key state for the zone
    reset     — clear last_rollover_error on one key after operator action
    unstick   — skip the softfail-delay and probe the parent on the next tick
    validate  — re-parse policy from YAML and report which §4 invariants pass/fail
flags: |
      --ksk   Render only the KSK section (status / when); ignored by other subcommands
      --zsk   Render only the ZSK section (status / when); ignored by other subcommands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dnssec auto-rollover asap
use: asap
short: Schedule a manual KSK rollover at the earliest safe moment
long: |
  Asks the daemon to compute ComputeEarliestRollover and persist
  manual_rollover_* on the zone row. The rollover worker fires
  AtomicRollover when t_earliest is reached. Rejects the request if a
  rollover is already in progress or the pipeline has no standby SEP key.
  
  Online-only: scheduling against a stopped daemon is meaningless
  (the manual_rollover_* row would never be read).
flags: |
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli auditor zone dnssec auto-rollover cancel
use: cancel
short: Cancel a pending manual KSK rollover request
long: |
  Asks the daemon to clear manual_rollover_requested_at and
  manual_rollover_earliest on the zone row. Has no effect on rollovers
  that have already fired or on scheduled (lifetime-driven) rollovers.
  
  Online-only: cancelling against a stopped daemon is meaningless (the
  manual_rollover_* row isn't being read by anything).
flags: |
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli auditor zone dnssec auto-rollover reset
use: reset
short: Clear last_rollover_error for one key (after operator intervention)
long: |
  Asks the daemon to clear the last_rollover_error column on a single
  key's RolloverKeyState row. Use after diagnosing and fixing a
  hard-failed rollover so status output isn't misleading.
  
  Default mode talks to the daemon's API server. Use --offline to write
  directly to the keystore file when the daemon is down (postmortem
  use). The CLI checks the daemon-sentinel row in the keystore and
  refuses to run --offline if it sees a live daemon process; pass
  --force to override (you must ensure the daemon is genuinely
  stopped first).
flags: |
      --force         With --offline: override the daemon-alive check
      --keyid int     Key ID to reset (RFC 4034 keytag)
      --offline       Write directly to keystore file (postmortem use; daemon is down)
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli auditor zone dnssec auto-rollover status
use: status
short: Print rollover state for a zone (KSK and ZSK)
long: |
  Prints rollover state for the zone with an OK / ACTIVE / SOFTFAIL
  headline and per-key tables for KSKs and ZSKs.
  
  Default mode talks to the daemon's API server (no daemon config needed
  on the CLI host). Use --offline to render against the keystore file
  when the daemon is down — that requires --config with the daemon's
  config file so the CLI can find db.file and the zone's policy.
  
  Use --ksk or --zsk to print only the KSK block or only the ZSK block
  (the two flags are mutually exclusive). These flags are inherited
  from the auto-rollover parent and accepted by every subcommand for
  consistency.
  
  The DS range line lists SEP keyids (same numbering as the KSK table and
  as DS digest key tags at the parent).
  
  Use -v / --verbose to show rollover_index spans behind the keyid lists
  and the policy summary.
flags: |
      --offline       Render against keystore file (postmortem use; daemon is down)
  -v, --verbose       Show full last_error text and policy summary
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli auditor zone dnssec auto-rollover unstick
use: unstick
short: Skip the softfail-delay and probe the parent on the next tick
long: |
  Asks the daemon to clear next_push_at on the zone row so the
  rollover engine fires a probe UPDATE on its very next tick instead of
  waiting out the rest of the softfail-delay window. Operator override
  for "I just fixed the parent and want to retry now."
  
  Operationally optional: the engine polls the parent continuously
  regardless of softfail-delay, so a parent fix is auto-detected within
  confirm-poll-max even without 'unstick'. Use only to skip the wait.
  
  Default mode talks to the daemon's API server. Use --offline to write
  directly to the keystore file when the daemon is down (postmortem
  use). The CLI checks the daemon sentinel via refuseIfDaemonAlive
  and refuses to run if a live daemon is detected; --force overrides
  the check for cases where the sentinel is stale.
  
  Hardfail_count and last_softfail_* are preserved so status output
  still shows the most recent failure context. The counter resets to 0
  on the next successful confirmed observation.
  
  Differs from 'reset' (which clears last_rollover_error for one keyid).
flags: |
      --force         With --offline: override the daemon-alive check
      --offline       Write directly to keystore file (postmortem use; daemon is down)
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli auditor zone dnssec auto-rollover validate
use: validate
short: Validate a zone's rollover policy against §4 cache-flush invariants
long: |
  Re-parses the YAML config the daemon is running and runs every §4
  cache-flush invariant check (E5, E10, E11) against the dnssecpolicy
  attached to the named zone. Reports PASS / FAIL / WARN per invariant
  with operator-actionable suggestions.
  
  Online (default): contacts the daemon for its config-file path and the
  zone's active policy name, then reads the YAML directly. Useful for
  checking a candidate change before reload.
  
  Offline (--serverconfig PATH): skips the daemon and reads PATH. The
  zone's policy must be either supplied via --policy or inferable from
  the YAML's zones: block (when --zone matches a configured zone).
  
  DS_TTL handling: the runtime engine uses the parent's observed DS
  RRset TTL. validate doesn't have that observation, so it uses the
  ttls.parent-ds policy override if set, OR --parent-ds-ttl <duration> if
  supplied. Without either, E10/E11 are skipped (and the report says
  so).
flags: |
      --parent-ds-ttl string   Hypothetical parent DS RRset TTL (e.g. 1h) for E10/E11; overrides ttls.parent-ds
      --policy string          Override the dnssecpolicy name to validate (offline mode only)
      --serverconfig string    Read this YAML file instead of asking the daemon (offline)
  -z, --zone string            Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli auditor zone dnssec auto-rollover when
use: when
short: Compute the earliest moment a rollover could safely fire (no state change)
long: |
  Asks the running daemon when the next rollover will fire and the
  earliest it could fire if requested. Side-effect free; does not request
  a rollover. Use 'auto-rollover asap' to actually schedule one.
  
  Reports the KSK schedule by default (parent-DS gated). Use --zsk for the
  ZSK schedule (zone-local, no parent gates — bounded only by standby
  readiness), or --ksk to be explicit. The two flags are mutually exclusive.
  
  Default mode talks to the daemon's API server (no daemon config needed
  on the CLI host). Use --offline to compute locally against the keystore
  file when the daemon is down — this requires --config with the daemon's
  config file so the CLI can find db.file and the zone's policy.
flags: |
      --offline       Compute locally against keystore file (postmortem use; daemon is down)
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli auditor zone dnssec nsec
use: nsec
short: Prefix command, not usable by itself
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dnssec nsec show
use: show
short: Show the NSEC chain for a zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dnssec policy-change
use: policy-change
short: Bind a zone to a new DNSSEC policy for a gradual ZSK algorithm rollover
long: |
  Bind a zone toward a new DNSSEC policy so its ZSK algorithm rolls over
  GRADUALLY. Unlike "zone dnssec policy-set" (which retires the old key
  synchronously — unsafe for an algorithm change), this only sets the
  algorithm of FUTURE-generated ZSKs: the existing FIFO key pipeline drains
  in order, oldest first.
  
  This command does NOT perform the roll. After binding, the algorithm
  rolls on the normal ZSK cadence — OR run
  
    auto-rollover asap -z <zone> --zsk
  
  to promote the next standby now (repeat to accelerate through the
  already-propagated old-alg standbys to the new algorithm).
  
  Requires dnssec.completeness: relaxed. A KSK / CSK / both-role algorithm
  change, or a second policy-change while a roll is in flight, is refused.
flags: |
  -p, --policy string   Target DNSSEC policy name
  -z, --zone string     Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone dnssec policy-reset
use: policy-reset
short: Reset a zone's DNSSEC keys to its config policy, per role (dry-run without --confirm)
long: |
  Force a zone onto its config dnssec_policy: for each key role whose algorithm no
  longer matches config, drop and regenerate that role's keys and re-sign; any
  role whose algorithm is already correct is kept. It also clears the runtime
  override and records the config policy as applied.
  
  This is a break-glass tool for test/lab zones. It exists because an abrupt
  policy switch that changes a KSK/ZSK algorithm is otherwise refused (that needs
  a key rollover that is not built). If the KSK algorithm changes it BREAKS THE
  CHAIN OF TRUST: the parent DS will not match the new KSK until you re-publish it
  (a ZSK-only change keeps the KSK and the DS). NOT for production.
  
  Run WITHOUT --confirm for a DRY RUN that previews what it would do (which roles
  would roll, whether the parent DS would break) and changes nothing; add
  --confirm to apply. A single --zone only (no wildcards, no bulk).
flags: |
      --confirm       Apply the reset; without it the command is a dry-run that only previews what would happen
  -z, --zone string   Zone to reset the DNSSEC policy for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone dnssec policy-set
use: policy-set
short: Set a zone's DNSSEC policy at runtime (persists as an override, not in YAML)
long: |
  Apply a DNSSEC policy to a zone in the running server. The change is stored
  as a per-zone override in the keystore and survives restart, but does NOT
  update the zone's dnssec_policy in the YAML config — update that separately
  to make the new policy the permanent base. If the new policy uses different
  key algorithms, the old keys are retired (their signatures kept until the
  KeyStateWorker removes them) and new keys take over; the zone stays signed
  throughout.
flags: |
  -p, --policy string   DNSSEC policy name to apply
  -z, --zone string     Zone to set the DNSSEC policy for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone dnssec resign
use: resign
short: Re-sign zone from scratch with currently-active keys (drops all existing RRSIGs)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dnssec sign
use: sign
short: Request signing of a zone (additive: cover gaps with active keys)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dsync
use: dsync
short: Prefix command, not useable by itself
hidden: true
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dsync bootstrap-sig0-key
use: bootstrap-sig0-key
short: Send dsync bootstrap command
flags: |
  -a, --algorithm string   Algorithm for the new SIG(0) key (use the 'algorithms' subcommand to list what the server supports)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dsync publish
use: publish
short: Send dsync publish-dsync-rrset command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dsync roll-sig0-key
use: roll-sig0-key
short: Send dsync rollover command
flags: |
  -a, --algorithm string   Algorithm for the new SIG(0) key (use the 'algorithms' subcommand to list what the server supports)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dsync status
use: status
short: Send dsync status command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone dsync unpublish
use: unpublish
short: Send dsync unpublish-dsync-rrset command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone freeze
use: freeze
short: Freeze a zone (i.e. stop accepting DDNS updates to the zone data)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone journal
use: journal
short: Inspect and manage a zone's delta journal
long: |
  The delta journal holds zone changes that have not yet reached the zone file.
  
  It is written when an update is published and replayed over the file on load, so
  a restart loses nothing. "zone sync" folds it into the file and empties it.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone journal list
use: list
short: List the journal's deltas and the records in them
flags: |
      --instructions   Print as replayable ADD/DEL instructions instead of a summary
      --out string     Write the instruction form to this file instead of stdout
  -z, --zone string    Zone name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone journal purge
use: purge
short: Discard the whole journal, saving its content first
long: |
  Discard every delta, after writing what they hold to {zonefile}.{serial}.purged
  as ADD/DEL instructions. Nothing is lost silently: replay what you want back with
  
    tdns-cli ... zone update from-file --file {zonefile}.{serial}.purged --zone <zone> --via api
  
  A journal that would replay cleanly holds changes that exist nowhere else, so
  purging one needs --force. Prefer "zone sync", which folds the same changes into
  the zone file and loses nothing. A journal that would NOT replay needs no flag:
  its changes are already absent from what the zone serves.
flags: |
      --force         Discard a journal that would otherwise replay cleanly
      --out string    Write the instruction form to this file instead of stdout
  -z, --zone string   Zone name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone journal status
use: status
short: Summarise the journal, and whether it will replay on restart
flags: |
  -z, --zone string   Zone name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone journal truncate
use: truncate
short: Drop the journal's tail, keeping the chain through --serial
long: |
  Keep the journal up to and including the delta that ends at --serial, drop the rest.
  
  A journal is a chain: each delta continues where the previous one ended, and a
  chain with a gap cannot be replayed at all. So only a suffix can be removed —
  there is deliberately no way to delete one record from the middle. To undo a
  single record, append a correction with an ordinary "zone update" instead.
flags: |
      --serial uint32   Keep the chain through the delta ending at this serial (required)
  -z, --zone string     Zone name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone list
use: list
short: List configured zones
flags: |
  -f, --file      Show zone input file
  -N, --notify    Show zone downstream notify addresses
  -P, --primary   Show zone primary nameserver
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone list-dynamic
use: list-dynamic
short: List dynamic zones (catalog members + API-managed) and their provisioning state
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone modify
use: modify
short: Modify a dynamic (API-managed) zone's primary or options
flags: |
      --options strings           Zone options (comma-separated)
      --primaries strings         New primary (upstream) addresses [host:port], comma-separated
      --primary-key string        New primary TSIG key name applied to all primaries (NOKEY for none) (default "NOKEY")
      --transfer-src strings      Replace the zone's outbound-transfer source address(es); omit to leave unchanged
      --tsig-algo string          Inline TSIG algorithm (default hmac-sha256)
      --tsig-name string          Inline TSIG key name; created in keystore if absent (existing keys cannot be rotated here)
      --tsig-secret string        Inline TSIG secret (base64). WARNING: visible in shell history / process list; prefer --tsig-secret-file
      --tsig-secret-file string   File containing the inline TSIG secret (base64); preferred over --tsig-secret
  -z, --zone string               Zone to modify
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone mplist
use: mplist
short: List multi-provider zones with HSYNCPARAM details
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone name
use: name
short: Inspect what a zone publishes at a single owner name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone name get
use: get
short: Show the RRsets this zone holds at one name
long: |
  Report the RRsets the zone currently publishes at one owner name, grouped by
  type, in presentation format.
  
  Reports the PUBLISHED view, not staged data: the question is what the server
  serves, and an update still in flight is not yet an answer to it.
  
  A name with nothing at it is an empty report, not an error -- that is what
  provisioning a name for the first time returns, and it has to be
  distinguishable from a read that failed. A name outside the zone IS an error,
  because answering "nothing" there reads as "that name has no records".
  
  RRSIG, NSEC, NSEC3, NSEC3PARAM and ZONEMD are never reported. They are the
  signer's and the zone policy's, and returning them would invite a
  read-modify-write that tries to author them.
flags: |
      --name string   Owner name to report on (required; must be inside the zone)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone parentsync
use: parentsync
short: Child-side parent delegation sync commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone parentsync bootstrap
use: bootstrap
short: Bootstrap the SIG(0) key with the parent
flags: |
      --scheme string   Bootstrap scheme (only "update" is implemented) (default "update")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone parentsync delta
use: delta
short: Compute delta between parent delegation data and child zone data
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone parentsync inquire
use: inquire
short: Inquire the parent about the current SIG(0) key state
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone parentsync inquire update
use: update
short: Deprecated spelling of "parentsync inquire"
hidden: true
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone parentsync roll-key
use: roll-key
short: Roll the SIG(0) key with the parent
flags: |
  -a, --algorithm string   Algorithm for the new SIG(0) key (use the 'algorithms' subcommand to list what the server supports)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone parentsync status
use: status
short: Show parentsync status for the zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone parentsync sync
use: sync
short: Sync delegation data in parent zone via DDNS UPDATE
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone proxy-key
use: proxy-key
short: Show the delegation-sync-proxy UPDATE state and the KEY to publish at the primary
long: |
  For a zone with the delegation-sync-proxy option (a tdns-agent acting as a
  secondary for a DSYNC-unaware primary), report whether the agent can proxy
  DNS UPDATEs to the parent, and print the exact records to serve at the primary
  apex: the agent's KEY RR, an HSYNCPARAM pubkey flag, and the HSYNCPARAM again
  in RFC 3597 form for a primary that cannot parse the private type.
  
  The records are printed in every state, so that what the agent holds can be
  compared against what the primary actually serves — including in ready, where
  the two are supposed to agree. In foreign-key the KEY found at the apex is
  listed alongside them. Read-only: no key is generated by running this.
  States: update-unsupported / ready / foreign-key / waiting-for-key.
flags: |
  -z, --zone string   Zone to report proxy-key state for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone readfake
use: readfake
short: Create a fake zone from a compiled in string
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone reload
use: reload
short: Request re-loading a zone
flags: |
  -e, --error            wait for reload to complete and report any parse errors
      --timeout string   how long to wait for reload when --error is set (e.g. 10s, 2m) (default "10s")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone sync
use: sync
short: Spool the zone out to disk (alias for write, no freeze/thaw)
long: |
  Write the zone's current content to its file, without freezing it.
  
  Identical to "zone write". Both fold any persisted change deltas into the file
  and drop them, so the file becomes the whole truth again.
  
  Use "zone freeze" instead when you want the file to be authoritative and
  updates refused until you thaw.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone thaw
use: thaw
short: Thaw a zone (i.e. accept DDNS updates to the zone data again)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone update
use: update
short: Create and ultimately send a DNS UPDATE msg for zone auth data
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone update addrr
use: addrr
short: Add one or more RRs to a zone
long: |
  Add resource records to a primary zone.
  
    tdns-cli ... zone update addrr --zone alpha.dnslab. --via api \
        --rr "foo.alpha.dnslab. 3600 IN A 1.2.3.4"
flags: |
  -K, --key string       SIG(0) keyfile to use for signing (.private/.key basename)
      --rr stringArray   Resource record in presentation format (repeatable)
  -S, --server string    Server to send the update to (addr:port)
      --signer string    Name of signer (key used to sign the update; defaults to --zone)
      --via string       Transport: "api" or "ddns" (required)
  -z, --zone string      Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone update create
use: create
short: Create and ultimately send a DNS UPDATE msg for zone auth data
long: |
  Will query for details about the DNS UPDATE via (add|del|show|set-ttl) commands.
  When the message is complete it may be signed and sent by the 'send' command. After a
  message has been send the loop will start again with a new, empty message to create.
  Loop ends on the command "QUIT"
  
  The zone to update is mandatory to specify on the command line with the --zone flag.
flags: |
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone update delname
use: delname
short: Delete every RRset at an owner name
long: |
  Delete all records at a name. At the zone apex, SOA and NS are retained --
  deleting those would dismantle the zone rather than a name within it.
  
    tdns-cli ... zone update delname --zone alpha.dnslab. --via api \
        --name foo.alpha.dnslab.
flags: |
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
      --name string     Owner name
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
      --via string      Transport: "api" or "ddns" (required)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone update delrr
use: delrr
short: Delete one specific RR from a zone
long: |
  Delete individual resource records. The RR must be given in full; the TTL is
  ignored, since it is not part of a record's identity.
  
    tdns-cli ... zone update delrr --zone alpha.dnslab. --via api \
        --rr "foo.alpha.dnslab. IN A 1.2.3.4"
flags: |
  -K, --key string       SIG(0) keyfile to use for signing (.private/.key basename)
      --rr stringArray   Resource record in presentation format (repeatable)
  -S, --server string    Server to send the update to (addr:port)
      --signer string    Name of signer (key used to sign the update; defaults to --zone)
      --via string       Transport: "api" or "ddns" (required)
  -z, --zone string      Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone update delrrset
use: delrrset
short: Delete an entire RRset (owner + type) from a zone
long: |
  Delete every record of one type at one owner name.
  
    tdns-cli ... zone update delrrset --zone alpha.dnslab. --via api \
        --name foo.alpha.dnslab. --type A
flags: |
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
      --name string     Owner name
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
      --type string     RR type to delete
      --via string      Transport: "api" or "ddns" (required)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone update from-file
use: from-file
short: Apply an ADD/DEL instruction file to a zone
long: |
  Apply a list of update instructions from a file.
  
    ADD foo.alpha.dnslab. 3600 IN A 1.2.3.4
    DEL bar.alpha.dnslab. IN A 5.6.7.8
  
  This is the format "zone journal purge" and "zone journal list --instructions"
  produce, so a file the server wrote can be edited down to the changes you want
  and fed straight back:
  
    tdns-cli ... zone update from-file --file /var/dns/zones/alpha.2026081704.purged \
        --zone alpha.dnslab. --via api
  
  Comments (";" or "#") and blank lines are ignored. Everything that survives is
  applied as ONE update — there is no half-applied outcome.
flags: |
      --file string     Instruction file to apply (required)
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
      --via string      Transport: "api" or "ddns" (required)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone update replacerrset
use: replacerrset
short: Atomically replace an RRset with the supplied RRs
long: |
  Replace an entire RRset in one transaction. The RRset to replace is inferred
  from the owner and type of the supplied records, so they must all describe the
  same one; mixed owners or types are refused rather than guessed at.
  
  This is a single atomic operation, not a delete followed by an add: the empty
  intermediate RRset is never published and no secondary can observe it.
  
    tdns-cli ... zone update replacerrset --zone alpha.dnslab. --via api \
        --rr "foo.alpha.dnslab. IN A 1.2.3.4" \
        --rr "foo.alpha.dnslab. IN A 2.3.4.5"
  
  To remove an RRset entirely, use delrrset -- replacerrset with no records is an
  error, not a silent delete.
flags: |
  -K, --key string       SIG(0) keyfile to use for signing (.private/.key basename)
      --rr stringArray   Resource record in presentation format (repeatable)
  -S, --server string    Server to send the update to (addr:port)
      --signer string    Name of signer (key used to sign the update; defaults to --zone)
      --via string       Transport: "api" or "ddns" (required)
  -z, --zone string      Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli auditor zone write
use: write
short: Write a zone to disk
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone zonemd
use: zonemd
short: Inspect and verify a zone's ZONEMD (RFC 8976)
long: |
  A zone with the publish-zonemd option carries a message digest of its own
  contents at the apex, recomputed inside every publish.
  
  "status" reports what the zone publishes without recomputing anything.
  "verify" digests the zone and reports whether the published value describes it,
  which is what a recipient of an AXFR will do.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone zonemd status
use: status
short: Show the zone's published ZONEMD and its configuration
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zone zonemd verify
use: verify
short: Recompute the zone's digest and compare it with what is published
long: |
  Digest the zone as it is currently served and compare the result with the
  apex ZONEMD, the way a recipient would.
  
  Costs one full pass over the zone, which for a large zone is not free; that is
  why it is a separate verb from "status".
flags: |
      --ignore-serial   digest against the serial each ZONEMD names rather than the SOA's (a diagnostic for a zone whose serial moved after it was digested, NOT a laxer check)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli auditor zones
use: zones
short: List audited zones with provider summaries
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner
use: combiner
short: TDNS Combiner commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner add-data
use: add-data [file]
short: Add local data to a zone passing through the combiner
long: |
  Add local data to a zone passing through the combiner.
  
  Zone can be specified via --zone flag. The file should contain one RR per line.
  
  Example contents (for a zone named "example.com"):
    example.com. 86400 IN NS ns1.provider.com.
    example.com. 86400 IN NS ns2.service.net.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner config
use: config
short: Runtime introspection of the combiner's multi-provider config; config and zone reload
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner config check
use: check [config-file]
short: Validate the "combiner" daemon config and correlate it with the running daemon
long: |
  Validate a "combiner" daemon configuration file for completeness and internal
  consistency, and (unless --offline) correlate it against the running daemon to
  report drift between the file on disk and what the server actually loaded.
  
  Target config resolution:
    1. an explicit path (positional arg or --serverconfig)
    2. otherwise the config-file: recorded for "combiner" in the CLI config, else the compiled-in default for its app type
  
  Exit status is non-zero if any check FAILs (WARNs do not fail the run).
flags: |
      --offline               do not contact the daemon; run static checks only
      --serverconfig string   path to the "combiner" daemon config file to check (default: ask the daemon, else the configured/compiled-in default)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner config mwe
use: mwe
short: Generate a minimal working example config (+ certs, zones) for tdns-combiner
long: |
  Generate a Minimal Working Example configuration for tdns-combiner.
  
  Writes a single self-contained YAML to <dir>/tdns-combiner.yaml (or, if that
  file already exists, to <dir>/tdns-combiner.yaml.mwe so nothing is
  clobbered). Also generates, unless already present:
  
    - a self-signed cert/key pair under <dir>/certs (for the API server and the
      encrypted DNS transports)
    - two example primary zones — one unsigned, one signed — using two different
      zone templates, with generated zone files under <dir>/zones
    - a commented-out secondary zone using a third template
  
  The DNS engine listens on IPv4 and IPv6 localhost; extra listen addresses are
  shown commented out. The generated tree is designed to pass
  `tdns-cli combiner config check`.
flags: |
      --dir string   base directory for the generated config, certs and zones (default "/etc/tdns")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner config reload
use: reload
short: Send config reload command to tdns-combiner
flags: |
      --confirm   apply even if the reload would strand a signed zone by a same-name DNSSEC policy algorithm change
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner config reload-tsig
use: reload-tsig
short: Reconcile keys.tsig into the TSIG keystore (config reload-tsig)
long: |
  Re-read keys.tsig from the config file and reconcile into the DB-backed
  TSIG keystore. Secret conflicts are withheld by default; use --force to
  overwrite all conflicts, or --interactive to prompt per conflict.
flags: |
      --force         overwrite all secret/algorithm conflicts with keys.tsig
      --interactive   prompt per conflict before overwriting
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner config reload-zones
use: reload-zones
short: Send reload-zones command to tdns-combiner
flags: |
      --confirm   apply even if the reload would strand a signed zone by a same-name DNSSEC policy algorithm change
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner config status
use: status
short: Show the running combiner's effective multi-provider config
long: |
  Show the running combiner's effective multi-provider config.
  
  Use -v to include the agent-identity list, protected namespaces,
  sync-api listen addresses, and provider zones.
  
  Sensitive paths (private key locations, JOSE pubkey paths) are
  intentionally not exposed via this endpoint.
flags: |
  -v, --verbose   include agent identities, protected namespaces, listen addresses, provider zones
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli combiner daemon
use: daemon
short: Only useful via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner daemon api
use: api
short: request an API summary from the tdns-combiner daemon
long: |
  The daemon api command queries tdns-combiner for the provided API
  and prints that out in a (hopefully) comprehensible fashion.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner daemon reload
use: reload
short: Reload config from file
long: |
  Reload config from file (the assumption is that something in the config has changed).
  Right now this doesn't do much, but later on various services will be able to restart.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner daemon restart
use: restart
short: Stop and then start the management daemon
flags: |
      --clear string   Truncate the specified log file before starting
      --update         Update the server binary from /tmp/{binary} to /usr/local/libexec/ before starting
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner daemon start
use: start
short: Start the tdns-combiner daemon
long: |
  Start the tdns-combiner daemon. If it was already running, then this is a no-op.
flags: |
  -W, --maxwait int   Max seconds to wait until declaring start to have failed (default 5)
      --slurp         Slurp stdout/stderr for errors (debug tool only)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner daemon status
use: status
short: Query for the status of the management daemon
long: |
  Query for the status of the management daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner daemon stop
use: stop
short: Stop the management daemon
long: |
  Stop the management daemon. If it was not running, then this is a no-op.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner debug
use: debug
short: Debug commands against the configured daemon
flags: |
      --qname string   qname of rrset to examine
      --qtype string   qtype of rrset to examine
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner debug lav
use: lav
short: Lookup and validate a child RRset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner debug rrset
use: rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner debug show-rrsetcache
use: show-rrsetcache
short: List cached RRsets
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner debug show-ta
use: show-ta
short: List known DNSSEC trust anchors
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner debug sig0
use: sig0
short: SIG(0) key helper commands (local)
flags: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
  -r, --rrtype string      rrtype to use for SIG(0)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner debug sig0 generate
use: generate
inherited: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
      --config string      config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug              debug output
  -H, --headers            show headers
  -Z, --pzone string       parent zone name
      --qname string       qname of rrset to examine
      --qtype string       qtype of rrset to examine
  -r, --rrtype string      rrtype to use for SIG(0)
  -v, --verbose            verbose output
  -z, --zone string        zone name

=== tdns-mpcli combiner debug validate-rrset
use: validate-rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner debug zone-txlog
use: zone-txlog
short: Show staged-but-unpublished zone changes
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner distrib
use: distrib
short: Manage distributions
long: |
  Commands for managing distributions created by this combiner, including listing distributions and purging completed distributions.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner distrib list
use: list
short: List all distributions from this combiner
flags: |
  -v, --verbose   Verbose output (show full details)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli combiner distrib peers
use: peers
short: List all known peer agents with working keys
long: |
  Show all recipient agents that this combiner has established keys with and can send distributions to.
flags: |
  -v, --verbose   Verbose output (show full details)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli combiner distrib purge
use: purge [--force]
short: Delete distributions
long: |
  Delete distributions from the database. By default, only completed distributions are deleted. Use --force to delete ALL distributions regardless of status.
flags: |
      --force   Delete ALL distributions (not just completed ones)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner gossip
use: gossip
short: Gossip protocol commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner gossip state
use: state
short: Show gossip state matrix for a zone
long: |
  Display the N×N state matrix for the provider group serving a zone.
  Each row is a reporting peer; each column shows that reporter's
  view of another peer's state. A healthy group shows OPERATIONAL
  in every non-diagonal cell.
flags: |
  -z, --zone string   Zone name (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner keys
use: keys
short: JOSE keypair for secure CHUNK (generate, show)
long: |
  Generate a JOSE keypair or display the public key. Uses the server config file (agent or combiner) to get long_term_jose_priv_key path, or --server-config.
flags: |
      --server-config string   path to agent/combiner config file (overrides apiservers.*.config_file)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner keys generate
use: generate
short: Generate JOSE keypair and write to config path or -output
flags: |
  -o, --output string   path for generated private key (overrides config)
inherited: |
      --config string          config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug                  debug output
  -H, --headers                show headers
  -Z, --pzone string           parent zone name
      --server-config string   path to agent/combiner config file (overrides apiservers.*.config_file)
  -v, --verbose                verbose output
  -z, --zone string            zone name

=== tdns-mpcli combiner keys show
use: show
short: Print public key (JWK) from configured long_term_jose_priv_key
inherited: |
      --config string          config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug                  debug output
  -H, --headers                show headers
  -Z, --pzone string           parent zone name
      --server-config string   path to agent/combiner config file (overrides apiservers.*.config_file)
  -v, --verbose                verbose output
  -z, --zone string            zone name

=== tdns-mpcli combiner list-data
use: list-data
short: List local data added to a zone in the combiner
long: |
  List local data added to a zone in the combiner. Zone can be specified via --zone flag.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner peer
use: peer
short: Commands to the combiner regarding peers
long: |
  Commands that instruct the combiner to perform an action toward a peer agent (e.g. ping, resync).
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner peer apiping
use: apiping
short: Ping a peer via HTTPS API
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner peer list
use: list
short: List all known peer agents
long: |
  Show all peer agents that this combiner knows about.
  Displays both DNS and API transports independently with their current state.
  
  Example:
    tdns-cliv2 combiner peer list
    tdns-cliv2 combiner peer list --verbose
flags: |
      --verbose   Show detailed per-peer statistics
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli combiner peer ping
use: ping
short: Ping a peer via DNS CHUNK
long: |
  Send a DNS CHUNK ping to a peer and report the result.
  The --id flag specifies the peer identity (e.g. agent.beta.dnslab.
  or combiner.dnslab.).
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner peer reset
use: reset
short: Reset peer (agent/auditor only; no-op elsewhere)
long: |
  Reset a peer to initial NEEDED state. Flushes all IMR
  cache entries for the peer's discovery names and restarts
  discovery from scratch. Use this when a peer is stuck in UNKNOWN
  or KNOWN state. Only applicable to roles that use dynamic
  HSYNC3-driven discovery (agent, auditor); a no-op on
  static-peer roles (signer, combiner).
flags: |
      --id string   Peer identity to reset (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner peer resync
use: resync
short: Ask agents to re-send all zone data to the combiner
long: |
  Send an RFI SYNC to configured agents, requesting them to re-send all their
  local zone data. Useful after combiner restart when in-memory agent contributions
  are lost.
  
  By default, sends to all agents for all zones. Use --zone and --agent to narrow scope.
  
  Example:
    tdns-cliv2 combiner peer resync
    tdns-cliv2 combiner peer resync --zone=whisky.dnslab.
    tdns-cliv2 combiner peer resync --agent=agent.alpha.dnslab.
flags: |
      --agent string   Resync only this agent (default: all agents)
      --zone string    Resync only this zone (default: all zones)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner ping
use: ping
short: Send an API ping request and present the response
flags: |
  -c, --count int   #pings to send
  -n, --newapi      use new api client
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner remove-data
use: remove-data [file]
short: Remove local data from a zone passing through the combiner
long: |
  Remove local data from a zone passing through the combiner.
  
  Zone can be specified via --zone flag. The file should contain one RR per line.
  
  Example contents (for a zone named "example.com"):
    example.com. 86400 IN NS ns1.provider.com.
    example.com. 86400 IN NS ns2.service.net.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner router
use: router
short: DNS message router introspection commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner router describe
use: describe
short: Show detailed router state
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner router list
use: list
short: List all registered message handlers
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner router metrics
use: metrics
short: Show router metrics (use --detailed for per-peer breakdown)
flags: |
      --detailed   Show per-peer breakdown
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner router reset
use: reset
short: Reset router metrics
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner router walk
use: walk
short: Walk all handlers with visitor pattern
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner show-combiner-data
use: show-combiner-data
short: Show the combiner's local data store (merged + per-agent)
long: |
  Display the combiner's CombinerData (merged view) and AgentContributions
  (per-agent breakdown) for all zones or a specific zone.
  
  Example:
    tdns-cliv2 combiner show-combiner-data
    tdns-cliv2 combiner show-combiner-data --zone whisky.dnslab.
flags: |
      --zone string   Filter by specific zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner stop
use: stop
short: Send stop command to the daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner transaction
use: transaction
short: Transaction diagnostics
long: |
  Commands for diagnosing transaction errors on the combiner.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner transaction errors
use: errors
short: Show recent transaction errors
long: |
  Show errors from recent CHUNK NOTIFY processing. Use --last to filter by time window (default: 30m).
flags: |
      --last string   Time window for error listing (e.g. 30m, 2h, 1h30m) (default "30m")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner transaction errors details
use: details
short: Show details for a specific transaction error
long: |
  Look up a specific distribution ID in the error journal and show full details.
flags: |
      --distid string   Distribution ID to look up
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner zone
use: zone
short: Combiner zone management commands
flags: |
  -F, --force   Force operation
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner zone bump
use: bump
short: Bump SOA serial for a zone on the combiner
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner zone edits
use: edits
short: Manage pending, approved and rejected edits
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner zone edits approve
use: approve
short: Approve a pending edit
flags: |
      --edit int      Edit ID to approve
      --zone string   Zone the edit belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner zone edits clear
use: clear
short: Clear edit tables (default: all; use --pending/--approved/--rejected/--current to select)
flags: |
      --approved      Clear approved edits
      --current       Clear current contributions
      --pending       Clear pending edits
      --rejected      Clear rejected edits
      --zone string   Scope to zone (default: all zones)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner zone edits list
use: list
short: List edits for a zone (default: current contributions; use --pending/--approved/--rejected)
flags: |
      --approved      Show approved edits (transaction history)
      --pending       Show pending edits
      --rejected      Show rejected edits
      --zone string   Zone to list edits for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner zone edits purge
use: purge
short: Remove all contributions attributed to a specific origin from a zone
long: |
  Remove all contributions for a given origin (sender ID) from
  the combiner's view of a zone. Used to clean up ghost or stale
  contributions left over from earlier code versions or
  mis-configured peers — e.g. an entry attributed to a bare
  "combiner" string instead of the FQDN "combiner.X.dnslab.".
  
  This is destructive: there is no per-RR undo. The combiner's
  in-memory AgentContributions map, the persisted
  CombinerContributions DB rows, and the rebuilt CombinerData are
  all updated. CombineWithLocalChanges runs after the rebuild so
  the served zone reflects the change.
flags: |
      --origin string   Origin (sender ID) whose contributions to remove (required)
      --zone string     Zone to purge contributions from (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner zone edits reapply
use: reapply
short: Reload contributions from DB and re-apply to zone data
flags: |
      --zone string   Zone to reapply contributions for (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner zone edits reject
use: reject
short: Reject a pending edit
flags: |
      --edit int        Edit ID to reject
      --reason string   Reason for rejection (required)
      --zone string     Zone the edit belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli combiner zone list
use: list
short: List configured zones
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner zone mplist
use: mplist
short: List multi-provider zones with HSYNCPARAM details
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli combiner zone reload
use: reload
short: Request re-loading a zone on the combiner
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           Force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli completion
use: completion
short: Generate the autocompletion script for the specified shell
long: |
  Generate the autocompletion script for tdns-mpcli for the specified shell.
  See each sub-command's help for details on how to use the generated script.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli completion bash
use: bash
short: Generate the autocompletion script for bash
long: |
  Generate the autocompletion script for the bash shell.
  
  This script depends on the 'bash-completion' package.
  If it is not installed already, you can install it via your OS's package manager.
  
  To load completions in your current shell session:
  
  	source <(tdns-mpcli completion bash)
  
  To load completions for every new session, execute once:
  
  #### Linux:
  
  	tdns-mpcli completion bash > /etc/bash_completion.d/tdns-mpcli
  
  #### macOS:
  
  	tdns-mpcli completion bash > $(brew --prefix)/etc/bash_completion.d/tdns-mpcli
  
  You will need to start a new shell for this setup to take effect.
flags: |
      --no-descriptions   disable completion descriptions
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli completion fish
use: fish
short: Generate the autocompletion script for fish
long: |
  Generate the autocompletion script for the fish shell.
  
  To load completions in your current shell session:
  
  	tdns-mpcli completion fish | source
  
  To load completions for every new session, execute once:
  
  	tdns-mpcli completion fish > ~/.config/fish/completions/tdns-mpcli.fish
  
  You will need to start a new shell for this setup to take effect.
flags: |
      --no-descriptions   disable completion descriptions
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli completion powershell
use: powershell
short: Generate the autocompletion script for powershell
long: |
  Generate the autocompletion script for powershell.
  
  To load completions in your current shell session:
  
  	tdns-mpcli completion powershell | Out-String | Invoke-Expression
  
  To load completions for every new session, add the output of the above command
  to your powershell profile.
flags: |
      --no-descriptions   disable completion descriptions
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli completion zsh
use: zsh
short: Generate the autocompletion script for zsh
long: |
  Generate the autocompletion script for the zsh shell.
  
  If shell completion is not already enabled in your environment you will need
  to enable it.  You can execute the following once:
  
  	echo "autoload -U compinit; compinit" >> ~/.zshrc
  
  To load completions in your current shell session:
  
  	source <(tdns-mpcli completion zsh)
  
  To load completions for every new session, execute once:
  
  #### Linux:
  
  	tdns-mpcli completion zsh > "${fpath[1]}/_tdns-mpcli"
  
  #### macOS:
  
  	tdns-mpcli completion zsh > $(brew --prefix)/share/zsh/site-functions/_tdns-mpcli
  
  You will need to start a new shell for this setup to take effect.
flags: |
      --no-descriptions   disable completion descriptions
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli configure
use: configure
short: Interactive bootstrap for tdns-mp config files
long: |
  Interview the user and emit coordinated configs for
  tdns-mpagent, tdns-mpsigner, tdns-mpcombiner and tdns-mpcli
  under /etc/tdns/.
  
  Safe to re-run: existing values become the prompt defaults.
  Live servers must be explicitly confirmed before their
  config is replaced.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli help
use: help [command]
short: Help about any command
long: |
  Help provides help for any command in the application.
  Simply type tdns-mpcli help [path to command] for full details.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer
use: signer
short: Interact with the MP signer via API
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth
use: auth
short: Interact with tdns-auth (authoritative) via API
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth daemon
use: daemon
short: Only useful via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth daemon api
use: api
short: request an API summary from the tdns-auth daemon
long: |
  The daemon api command queries tdns-auth for the provided API
  and prints that out in a (hopefully) comprehensible fashion.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth daemon reload
use: reload
short: Reload config from file
long: |
  Reload config from file (the assumption is that something in the config has changed).
  Right now this doesn't do much, but later on various services will be able to restart.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth daemon restart
use: restart
short: Stop and then start the management daemon
flags: |
      --clear string   Truncate the specified log file before starting
      --update         Update the server binary from /tmp/{binary} to /usr/local/libexec/ before starting
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth daemon start
use: start
short: Start the tdns-auth daemon
long: |
  Start the tdns-auth daemon. If it was already running, then this is a no-op.
flags: |
  -W, --maxwait int   Max seconds to wait until declaring start to have failed (default 5)
      --slurp         Slurp stdout/stderr for errors (debug tool only)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth daemon status
use: status
short: Query for the status of the management daemon
long: |
  Query for the status of the management daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth daemon stop
use: stop
short: Stop the management daemon
long: |
  Stop the management daemon. If it was not running, then this is a no-op.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth dsync-api
use: dsync-api
short: Manage the DSYNC API scheme
long: |
  Operator commands for the DSYNC API scheme: the HTTPS channel a child uses to
  change its delegation when it cannot sign a DNS message and therefore cannot
  use the DSYNC UPDATE scheme.
  
  These commands travel on the management API and require the operator API key.
  The credentials they issue authenticate somewhere else entirely -- the DSYNC
  API listener -- and every one of them is confined by the parent zone's
  updatepolicy.child.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth dsync-api cert-credential
use: cert-credential
short: Manage DSYNC API certificate credentials
long: |
  Create, list, disable and delete client-certificate credentials for the DSYNC
  API listener. These travel on the management API with the operator key, the
  same socket as "credential".
  
    tdns-cli auth dsync-api cert-credential add \
        --zone example. --mech tls-pkix --identity child1.example. \
        --principal child1.example.
  
    tdns-cli auth dsync-api cert-credential add \
        --zone example. --mech tls-pin --cert /path/to/child.crt \
        --principal child1.example.
  
  "list" shows both bearer and certificate credentials (one table). Bearer rows
  display as mechanism "basic".
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth dsync-api cert-credential add
use: add
short: Issue a certificate credential for one identity of one parent zone
flags: |
      --cert string        Leaf certificate file; hashed for tls-pin
      --comment string     Free-text note stored with the credential
      --expires string     Expiry as a duration ("720h") or RFC3339 timestamp; default: never
      --identity string    DNS name (tls-pkix) or pin (tls-pin)
      --mech string        tls-pin or tls-pkix (required)
      --pin string         Literal 44-character standard-encoding base64 SPKI pin (tls-pin)
      --principal string   DNS name the update policy is evaluated against (default: the identity, tls-pkix only)
  -z, --zone string        Parent zone the credential is scoped to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth dsync-api cert-credential delete
use: delete
short: Delete a certificate credential outright
flags: |
      --cert string       Leaf certificate file; hashed for tls-pin
      --identity string   DNS name (tls-pkix) or pin (tls-pin)
      --mech string       tls-pin or tls-pkix (required)
      --pin string        Literal 44-character standard-encoding base64 SPKI pin (tls-pin)
  -z, --zone string       Parent zone the credential is scoped to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth dsync-api cert-credential disable
use: disable
short: Stop a certificate credential working, keeping its record
flags: |
      --cert string       Leaf certificate file; hashed for tls-pin
      --identity string   DNS name (tls-pkix) or pin (tls-pin)
      --mech string       tls-pin or tls-pkix (required)
      --pin string        Literal 44-character standard-encoding base64 SPKI pin (tls-pin)
  -z, --zone string       Parent zone the credential is scoped to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth dsync-api cert-credential enable
use: enable
short: Re-enable a disabled certificate credential
flags: |
      --cert string       Leaf certificate file; hashed for tls-pin
      --identity string   DNS name (tls-pkix) or pin (tls-pin)
      --mech string       tls-pin or tls-pkix (required)
      --pin string        Literal 44-character standard-encoding base64 SPKI pin (tls-pin)
  -z, --zone string       Parent zone the credential is scoped to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth dsync-api cert-credential list
use: list
short: List credentials, for one parent zone or for all (bearer and certificate)
flags: |
  -z, --zone string   Parent zone the credential is scoped to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth dsync-api credential
use: credential
short: Manage DSYNC API credentials
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth dsync-api credential add
use: add
short: Issue a credential for one child of one parent zone
long: |
  Create a <username, key> credential. The key is generated here, shown once, and
  stored only as a hash -- there is no way to retrieve it afterwards. If it is
  lost, delete the credential and issue another.
  
  The username identifies the principal that the parent zone's updatepolicy.child
  is evaluated against, exactly where the SIG(0) signer name goes on the DDNS
  path. Name it after the child zone and the policy needs no explanation:
  
    tdns-cli auth dsync-api credential add --zone example. --user child1.example.
  
  With updatepolicy.child.type "selfsub", that credential may change the allowed
  RR types at or below child1.example. and nowhere else.
  
  Use --principal when the account should have a human-readable name:
  
    tdns-cli auth dsync-api credential add --zone example. \
        --user acme-registrar --principal child1.example.
flags: |
      --comment string     Free-text note stored with the credential
      --expires string     Expiry as a duration ("720h") or RFC3339 timestamp; default: never
      --principal string   DNS name the update policy is evaluated against (default: the username)
  -u, --user string        Username (required)
  -z, --zone string        Parent zone the credential is scoped to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth dsync-api credential delete
use: delete
short: Delete a credential outright
long: |
  Remove the credential and its row. Prefer "disable" when the question "who had
  access, and when" might be asked later: disabling stops the credential working
  and keeps the record.
flags: |
  -u, --user string   Username (required)
  -z, --zone string   Parent zone the credential is scoped to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth dsync-api credential disable
use: disable
short: Stop a credential working, keeping its record
flags: |
  -u, --user string   Username (required)
  -z, --zone string   Parent zone the credential is scoped to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth dsync-api credential enable
use: enable
short: Re-enable a disabled credential
flags: |
  -u, --user string   Username (required)
  -z, --zone string   Parent zone the credential is scoped to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth dsync-api credential list
use: list
short: List credentials, for one parent zone or for all (bearer and certificate)
flags: |
  -z, --zone string   Parent zone the credential is scoped to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth imr
use: imr
short: IMR (Internal Recursive Resolver) cache commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr dump-tuning
use: dump-tuning
short: Show effective IMR tuning values (backoff policy, family, discovery, etc.)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr dump-zone-backoffs
use: dump-zone-backoffs [zone]
short: Show zone-scoped lame-delegation backoffs (per zone, per address)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr flush
use: flush <qname>
short: Flush IMR cache entries at and below qname
flags: |
      --keep-structural   keep structural records (NS, glue); flush only the rest
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr forward
use: forward
short: Inspect and probe the IMR's forward zones
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr forward list
use: list
short: List the configured forward zones and their upstreams
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr forward probe
use: probe [zone]
short: Probe the forward upstreams now (same probe as at startup) and report per upstream
long: |
  Send a recursive SOA query for the forward zone to every one of its
  upstreams (or one zone's) and report the outcome per upstream. The probe updates
  the live reachability state, so a recovered upstream clears its DEGRADED
  error and a newly dead one raises it.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr forward status
use: status
short: Show per-upstream reachability, counters and last success/error
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr query
use: query <qname> <qtype>
short: Query the IMR cache (cache-only, no external queries)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr reset
use: reset
short: Flush entire IMR cache and re-prime (preserves root NS)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr show
use: show
short: Show IMR cache entries related to agent discovery
flags: |
      --id string   Agent identity to show cache entries for (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr stub
use: stub
short: Inspect and probe the IMR's stub zones
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr stub list
use: list
short: List the configured stub zones and their servers
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr stub probe
use: probe [zone]
short: Probe the stub servers now and report per (server, address, transport)
long: |
  Send an RD=0 SOA query for the stub zone to every (server, address,
  advertised transport) tuple and report rcode, AA bit and RTT. Strictly
  report-only: nothing is recorded into the resolver's server state, so a
  probe can never put a stub server into backoff.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth imr stub status
use: status
short: Show per-server transport counters and active backoffs
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth ping
use: ping
short: Send an API ping request and present the response
flags: |
  -c, --count int   #pings to send
  -n, --newapi      use new api client
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone
use: zone
short: Prefix command, not usable by itself
flags: |
  -F, --force   force operation
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone add
use: add
short: Add a dynamic zone at runtime: secondary, or template-constrained primary (persists across restart)
flags: |
      --options strings           Zone options (comma-separated; secondary zones — primaries take options from the template)
      --primaries strings         Primary (upstream) addresses [host:port], comma-separated (secondary zones)
      --primary-key string        Primary TSIG key name applied to all primaries (NOKEY for none) (default "NOKEY")
      --template string           Config template for a primary zone (must carry dynamiczones: true)
      --transfer-src strings      Source address(es) to bind for this zone's outbound transfers (IP only, one per family)
      --tsig-algo string          Inline TSIG algorithm (default hmac-sha256)
      --tsig-name string          Inline TSIG key name; created in keystore if absent and applied to keyless primaries (secondary) or keyless downstreams (primary)
      --tsig-secret string        Inline TSIG secret (base64). WARNING: visible in shell history / process list; prefer --tsig-secret-file
      --tsig-secret-file string   File containing the inline TSIG secret (base64); preferred over --tsig-secret
      --type string               Zone type: secondary (default) or primary (requires --template) (default "secondary")
  -z, --zone string               Zone to add
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone bump
use: bump
short: Bump SOA serial and epoch (if any) in the daemon's version of the zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone childsync
use: childsync
short: Parent-side child delegation sync commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone childsync advert
use: advert
short: Print what the parent primary still lacks of the DSYNC advertisement, as an nsupdate block
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone childsync proxy-status
use: proxy-status
short: Report a childsync-proxy's advertisement and push state
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone childsync publish
use: publish
short: Publish the DSYNC RRset into the zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone childsync reconcile
use: reconcile
short: Reconcile the advertisement and the known children against the served zone now
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone childsync unpublish
use: unpublish
short: Unpublish the DSYNC RRset from the zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone delegation
use: delegation
short: Inspect the delegation data a parent zone holds for its children
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone delegation get
use: get
short: Show what this parent publishes for a child (or list its children)
long: |
  Report the delegation records the parent currently holds for a child zone,
  grouped by owner name and type.
  
  With no --child, list the children this parent has delegation data for -- the
  question that comes first when reconciling an external store against the
  server.
  
  The answer comes from the zone's delegation backend, so it is what the SERVER
  considers current. For a backend that records delegations somewhere other than
  the served zone those differ deliberately, and reading the zone instead would
  quietly give the wrong answer.
flags: |
      --child string   Child zone to report on; omit to list the parent's children
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone delete
use: delete
short: Delete a dynamic (API-managed) zone
flags: |
  -z, --zone string   Zone to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone desc
use: desc
short: Describe a single zone in full, including DNSSEC applied-policy state and policy detail
long: |
  Print a detailed, multi-line record for one zone: everything "zone list -v"
  shows (type, store, options, primaries, notify, error/warning state, effective
  DNSSEC policy and override info) plus two DNSSEC sections not otherwise visible
  from the CLI:
  
    1. the last-applied policy record recorded in the keystore (applied policy
       name, source config|command, and when it was applied); and
    2. the currently-bound policy's detail — mode, KSK/ZSK (or CSK) algorithms,
       key lifetimes and RRSIG validity.
  
  An unsigned zone or an unresolvable policy degrades gracefully rather than
  failing. Does not change anything; read-only.
flags: |
  -z, --zone string   Zone to describe
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone dnssec
use: dnssec
short: Zone DNSSEC operations: signing, policy, and automated rollover
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dnssec auto-rollover
use: auto-rollover
short: Manage and inspect automated KSK rollover (scheduled + manual-ASAP)
long: |
  Subcommands operate on local keystore state for a zone:
  
    when      — compute the earliest safe rollover moment (no state change)
    asap      — schedule a manual rollover at that earliest moment
    cancel    — clear a pending manual rollover request
    status    — print phase + per-key state for the zone
    reset     — clear last_rollover_error on one key after operator action
    unstick   — skip the softfail-delay and probe the parent on the next tick
    validate  — re-parse policy from YAML and report which §4 invariants pass/fail
flags: |
      --ksk   Render only the KSK section (status / when); ignored by other subcommands
      --zsk   Render only the ZSK section (status / when); ignored by other subcommands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dnssec auto-rollover asap
use: asap
short: Schedule a manual KSK rollover at the earliest safe moment
long: |
  Asks the daemon to compute ComputeEarliestRollover and persist
  manual_rollover_* on the zone row. The rollover worker fires
  AtomicRollover when t_earliest is reached. Rejects the request if a
  rollover is already in progress or the pipeline has no standby SEP key.
  
  Online-only: scheduling against a stopped daemon is meaningless
  (the manual_rollover_* row would never be read).
flags: |
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli signer auth zone dnssec auto-rollover cancel
use: cancel
short: Cancel a pending manual KSK rollover request
long: |
  Asks the daemon to clear manual_rollover_requested_at and
  manual_rollover_earliest on the zone row. Has no effect on rollovers
  that have already fired or on scheduled (lifetime-driven) rollovers.
  
  Online-only: cancelling against a stopped daemon is meaningless (the
  manual_rollover_* row isn't being read by anything).
flags: |
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli signer auth zone dnssec auto-rollover reset
use: reset
short: Clear last_rollover_error for one key (after operator intervention)
long: |
  Asks the daemon to clear the last_rollover_error column on a single
  key's RolloverKeyState row. Use after diagnosing and fixing a
  hard-failed rollover so status output isn't misleading.
  
  Default mode talks to the daemon's API server. Use --offline to write
  directly to the keystore file when the daemon is down (postmortem
  use). The CLI checks the daemon-sentinel row in the keystore and
  refuses to run --offline if it sees a live daemon process; pass
  --force to override (you must ensure the daemon is genuinely
  stopped first).
flags: |
      --force         With --offline: override the daemon-alive check
      --keyid int     Key ID to reset (RFC 4034 keytag)
      --offline       Write directly to keystore file (postmortem use; daemon is down)
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli signer auth zone dnssec auto-rollover status
use: status
short: Print rollover state for a zone (KSK and ZSK)
long: |
  Prints rollover state for the zone with an OK / ACTIVE / SOFTFAIL
  headline and per-key tables for KSKs and ZSKs.
  
  Default mode talks to the daemon's API server (no daemon config needed
  on the CLI host). Use --offline to render against the keystore file
  when the daemon is down — that requires --config with the daemon's
  config file so the CLI can find db.file and the zone's policy.
  
  Use --ksk or --zsk to print only the KSK block or only the ZSK block
  (the two flags are mutually exclusive). These flags are inherited
  from the auto-rollover parent and accepted by every subcommand for
  consistency.
  
  The DS range line lists SEP keyids (same numbering as the KSK table and
  as DS digest key tags at the parent).
  
  Use -v / --verbose to show rollover_index spans behind the keyid lists
  and the policy summary.
flags: |
      --offline       Render against keystore file (postmortem use; daemon is down)
  -v, --verbose       Show full last_error text and policy summary
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli signer auth zone dnssec auto-rollover unstick
use: unstick
short: Skip the softfail-delay and probe the parent on the next tick
long: |
  Asks the daemon to clear next_push_at on the zone row so the
  rollover engine fires a probe UPDATE on its very next tick instead of
  waiting out the rest of the softfail-delay window. Operator override
  for "I just fixed the parent and want to retry now."
  
  Operationally optional: the engine polls the parent continuously
  regardless of softfail-delay, so a parent fix is auto-detected within
  confirm-poll-max even without 'unstick'. Use only to skip the wait.
  
  Default mode talks to the daemon's API server. Use --offline to write
  directly to the keystore file when the daemon is down (postmortem
  use). The CLI checks the daemon sentinel via refuseIfDaemonAlive
  and refuses to run if a live daemon is detected; --force overrides
  the check for cases where the sentinel is stale.
  
  Hardfail_count and last_softfail_* are preserved so status output
  still shows the most recent failure context. The counter resets to 0
  on the next successful confirmed observation.
  
  Differs from 'reset' (which clears last_rollover_error for one keyid).
flags: |
      --force         With --offline: override the daemon-alive check
      --offline       Write directly to keystore file (postmortem use; daemon is down)
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli signer auth zone dnssec auto-rollover validate
use: validate
short: Validate a zone's rollover policy against §4 cache-flush invariants
long: |
  Re-parses the YAML config the daemon is running and runs every §4
  cache-flush invariant check (E5, E10, E11) against the dnssecpolicy
  attached to the named zone. Reports PASS / FAIL / WARN per invariant
  with operator-actionable suggestions.
  
  Online (default): contacts the daemon for its config-file path and the
  zone's active policy name, then reads the YAML directly. Useful for
  checking a candidate change before reload.
  
  Offline (--serverconfig PATH): skips the daemon and reads PATH. The
  zone's policy must be either supplied via --policy or inferable from
  the YAML's zones: block (when --zone matches a configured zone).
  
  DS_TTL handling: the runtime engine uses the parent's observed DS
  RRset TTL. validate doesn't have that observation, so it uses the
  ttls.parent-ds policy override if set, OR --parent-ds-ttl <duration> if
  supplied. Without either, E10/E11 are skipped (and the report says
  so).
flags: |
      --parent-ds-ttl string   Hypothetical parent DS RRset TTL (e.g. 1h) for E10/E11; overrides ttls.parent-ds
      --policy string          Override the dnssecpolicy name to validate (offline mode only)
      --serverconfig string    Read this YAML file instead of asking the daemon (offline)
  -z, --zone string            Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli signer auth zone dnssec auto-rollover when
use: when
short: Compute the earliest moment a rollover could safely fire (no state change)
long: |
  Asks the running daemon when the next rollover will fire and the
  earliest it could fire if requested. Side-effect free; does not request
  a rollover. Use 'auto-rollover asap' to actually schedule one.
  
  Reports the KSK schedule by default (parent-DS gated). Use --zsk for the
  ZSK schedule (zone-local, no parent gates — bounded only by standby
  readiness), or --ksk to be explicit. The two flags are mutually exclusive.
  
  Default mode talks to the daemon's API server (no daemon config needed
  on the CLI host). Use --offline to compute locally against the keystore
  file when the daemon is down — this requires --config with the daemon's
  config file so the CLI can find db.file and the zone's policy.
flags: |
      --offline       Compute locally against keystore file (postmortem use; daemon is down)
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli signer auth zone dnssec nsec
use: nsec
short: Prefix command, not usable by itself
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dnssec nsec show
use: show
short: Show the NSEC chain for a zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dnssec policy-change
use: policy-change
short: Bind a zone to a new DNSSEC policy for a gradual ZSK algorithm rollover
long: |
  Bind a zone toward a new DNSSEC policy so its ZSK algorithm rolls over
  GRADUALLY. Unlike "zone dnssec policy-set" (which retires the old key
  synchronously — unsafe for an algorithm change), this only sets the
  algorithm of FUTURE-generated ZSKs: the existing FIFO key pipeline drains
  in order, oldest first.
  
  This command does NOT perform the roll. After binding, the algorithm
  rolls on the normal ZSK cadence — OR run
  
    auto-rollover asap -z <zone> --zsk
  
  to promote the next standby now (repeat to accelerate through the
  already-propagated old-alg standbys to the new algorithm).
  
  Requires dnssec.completeness: relaxed. A KSK / CSK / both-role algorithm
  change, or a second policy-change while a roll is in flight, is refused.
flags: |
  -p, --policy string   Target DNSSEC policy name
  -z, --zone string     Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone dnssec policy-reset
use: policy-reset
short: Reset a zone's DNSSEC keys to its config policy, per role (dry-run without --confirm)
long: |
  Force a zone onto its config dnssec_policy: for each key role whose algorithm no
  longer matches config, drop and regenerate that role's keys and re-sign; any
  role whose algorithm is already correct is kept. It also clears the runtime
  override and records the config policy as applied.
  
  This is a break-glass tool for test/lab zones. It exists because an abrupt
  policy switch that changes a KSK/ZSK algorithm is otherwise refused (that needs
  a key rollover that is not built). If the KSK algorithm changes it BREAKS THE
  CHAIN OF TRUST: the parent DS will not match the new KSK until you re-publish it
  (a ZSK-only change keeps the KSK and the DS). NOT for production.
  
  Run WITHOUT --confirm for a DRY RUN that previews what it would do (which roles
  would roll, whether the parent DS would break) and changes nothing; add
  --confirm to apply. A single --zone only (no wildcards, no bulk).
flags: |
      --confirm       Apply the reset; without it the command is a dry-run that only previews what would happen
  -z, --zone string   Zone to reset the DNSSEC policy for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone dnssec policy-set
use: policy-set
short: Set a zone's DNSSEC policy at runtime (persists as an override, not in YAML)
long: |
  Apply a DNSSEC policy to a zone in the running server. The change is stored
  as a per-zone override in the keystore and survives restart, but does NOT
  update the zone's dnssec_policy in the YAML config — update that separately
  to make the new policy the permanent base. If the new policy uses different
  key algorithms, the old keys are retired (their signatures kept until the
  KeyStateWorker removes them) and new keys take over; the zone stays signed
  throughout.
flags: |
  -p, --policy string   DNSSEC policy name to apply
  -z, --zone string     Zone to set the DNSSEC policy for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone dnssec resign
use: resign
short: Re-sign zone from scratch with currently-active keys (drops all existing RRSIGs)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dnssec sign
use: sign
short: Request signing of a zone (additive: cover gaps with active keys)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dsync
use: dsync
short: Prefix command, not useable by itself
hidden: true
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dsync bootstrap-sig0-key
use: bootstrap-sig0-key
short: Send dsync bootstrap command
flags: |
  -a, --algorithm string   Algorithm for the new SIG(0) key (use the 'algorithms' subcommand to list what the server supports)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dsync publish
use: publish
short: Send dsync publish-dsync-rrset command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dsync roll-sig0-key
use: roll-sig0-key
short: Send dsync rollover command
flags: |
  -a, --algorithm string   Algorithm for the new SIG(0) key (use the 'algorithms' subcommand to list what the server supports)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dsync status
use: status
short: Send dsync status command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone dsync unpublish
use: unpublish
short: Send dsync unpublish-dsync-rrset command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone freeze
use: freeze
short: Freeze a zone (i.e. stop accepting DDNS updates to the zone data)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone journal
use: journal
short: Inspect and manage a zone's delta journal
long: |
  The delta journal holds zone changes that have not yet reached the zone file.
  
  It is written when an update is published and replayed over the file on load, so
  a restart loses nothing. "zone sync" folds it into the file and empties it.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone journal list
use: list
short: List the journal's deltas and the records in them
flags: |
      --instructions   Print as replayable ADD/DEL instructions instead of a summary
      --out string     Write the instruction form to this file instead of stdout
  -z, --zone string    Zone name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone journal purge
use: purge
short: Discard the whole journal, saving its content first
long: |
  Discard every delta, after writing what they hold to {zonefile}.{serial}.purged
  as ADD/DEL instructions. Nothing is lost silently: replay what you want back with
  
    tdns-cli ... zone update from-file --file {zonefile}.{serial}.purged --zone <zone> --via api
  
  A journal that would replay cleanly holds changes that exist nowhere else, so
  purging one needs --force. Prefer "zone sync", which folds the same changes into
  the zone file and loses nothing. A journal that would NOT replay needs no flag:
  its changes are already absent from what the zone serves.
flags: |
      --force         Discard a journal that would otherwise replay cleanly
      --out string    Write the instruction form to this file instead of stdout
  -z, --zone string   Zone name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone journal status
use: status
short: Summarise the journal, and whether it will replay on restart
flags: |
  -z, --zone string   Zone name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone journal truncate
use: truncate
short: Drop the journal's tail, keeping the chain through --serial
long: |
  Keep the journal up to and including the delta that ends at --serial, drop the rest.
  
  A journal is a chain: each delta continues where the previous one ended, and a
  chain with a gap cannot be replayed at all. So only a suffix can be removed —
  there is deliberately no way to delete one record from the middle. To undo a
  single record, append a correction with an ordinary "zone update" instead.
flags: |
      --serial uint32   Keep the chain through the delta ending at this serial (required)
  -z, --zone string     Zone name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone list
use: list
short: List configured zones
flags: |
  -f, --file      Show zone input file
  -N, --notify    Show zone downstream notify addresses
  -P, --primary   Show zone primary nameserver
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone list-dynamic
use: list-dynamic
short: List dynamic zones (catalog members + API-managed) and their provisioning state
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone modify
use: modify
short: Modify a dynamic (API-managed) zone's primary or options
flags: |
      --options strings           Zone options (comma-separated)
      --primaries strings         New primary (upstream) addresses [host:port], comma-separated
      --primary-key string        New primary TSIG key name applied to all primaries (NOKEY for none) (default "NOKEY")
      --transfer-src strings      Replace the zone's outbound-transfer source address(es); omit to leave unchanged
      --tsig-algo string          Inline TSIG algorithm (default hmac-sha256)
      --tsig-name string          Inline TSIG key name; created in keystore if absent (existing keys cannot be rotated here)
      --tsig-secret string        Inline TSIG secret (base64). WARNING: visible in shell history / process list; prefer --tsig-secret-file
      --tsig-secret-file string   File containing the inline TSIG secret (base64); preferred over --tsig-secret
  -z, --zone string               Zone to modify
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone name
use: name
short: Inspect what a zone publishes at a single owner name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone name get
use: get
short: Show the RRsets this zone holds at one name
long: |
  Report the RRsets the zone currently publishes at one owner name, grouped by
  type, in presentation format.
  
  Reports the PUBLISHED view, not staged data: the question is what the server
  serves, and an update still in flight is not yet an answer to it.
  
  A name with nothing at it is an empty report, not an error -- that is what
  provisioning a name for the first time returns, and it has to be
  distinguishable from a read that failed. A name outside the zone IS an error,
  because answering "nothing" there reads as "that name has no records".
  
  RRSIG, NSEC, NSEC3, NSEC3PARAM and ZONEMD are never reported. They are the
  signer's and the zone policy's, and returning them would invite a
  read-modify-write that tries to author them.
flags: |
      --name string   Owner name to report on (required; must be inside the zone)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone parentsync
use: parentsync
short: Child-side parent delegation sync commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone parentsync bootstrap
use: bootstrap
short: Bootstrap the SIG(0) key with the parent
flags: |
      --scheme string   Bootstrap scheme (only "update" is implemented) (default "update")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone parentsync delta
use: delta
short: Compute delta between parent delegation data and child zone data
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone parentsync inquire
use: inquire
short: Inquire the parent about the current SIG(0) key state
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone parentsync inquire update
use: update
short: Deprecated spelling of "parentsync inquire"
hidden: true
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone parentsync roll-key
use: roll-key
short: Roll the SIG(0) key with the parent
flags: |
  -a, --algorithm string   Algorithm for the new SIG(0) key (use the 'algorithms' subcommand to list what the server supports)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone parentsync status
use: status
short: Show parentsync status for the zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone parentsync sync
use: sync
short: Sync delegation data in parent zone via DDNS UPDATE
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone proxy-key
use: proxy-key
short: Show the delegation-sync-proxy UPDATE state and the KEY to publish at the primary
long: |
  For a zone with the delegation-sync-proxy option (a tdns-agent acting as a
  secondary for a DSYNC-unaware primary), report whether the agent can proxy
  DNS UPDATEs to the parent, and print the exact records to serve at the primary
  apex: the agent's KEY RR, an HSYNCPARAM pubkey flag, and the HSYNCPARAM again
  in RFC 3597 form for a primary that cannot parse the private type.
  
  The records are printed in every state, so that what the agent holds can be
  compared against what the primary actually serves — including in ready, where
  the two are supposed to agree. In foreign-key the KEY found at the apex is
  listed alongside them. Read-only: no key is generated by running this.
  States: update-unsupported / ready / foreign-key / waiting-for-key.
flags: |
  -z, --zone string   Zone to report proxy-key state for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone readfake
use: readfake
short: Create a fake zone from a compiled in string
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone reload
use: reload
short: Request re-loading a zone
flags: |
  -e, --error            wait for reload to complete and report any parse errors
      --timeout string   how long to wait for reload when --error is set (e.g. 10s, 2m) (default "10s")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone sync
use: sync
short: Spool the zone out to disk (alias for write, no freeze/thaw)
long: |
  Write the zone's current content to its file, without freezing it.
  
  Identical to "zone write". Both fold any persisted change deltas into the file
  and drop them, so the file becomes the whole truth again.
  
  Use "zone freeze" instead when you want the file to be authoritative and
  updates refused until you thaw.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone thaw
use: thaw
short: Thaw a zone (i.e. accept DDNS updates to the zone data again)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone update
use: update
short: Create and ultimately send a DNS UPDATE msg for zone auth data
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone update addrr
use: addrr
short: Add one or more RRs to a zone
long: |
  Add resource records to a primary zone.
  
    tdns-cli ... zone update addrr --zone alpha.dnslab. --via api \
        --rr "foo.alpha.dnslab. 3600 IN A 1.2.3.4"
flags: |
  -K, --key string       SIG(0) keyfile to use for signing (.private/.key basename)
      --rr stringArray   Resource record in presentation format (repeatable)
  -S, --server string    Server to send the update to (addr:port)
      --signer string    Name of signer (key used to sign the update; defaults to --zone)
      --via string       Transport: "api" or "ddns" (required)
  -z, --zone string      Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone update create
use: create
short: Create and ultimately send a DNS UPDATE msg for zone auth data
long: |
  Will query for details about the DNS UPDATE via (add|del|show|set-ttl) commands.
  When the message is complete it may be signed and sent by the 'send' command. After a
  message has been send the loop will start again with a new, empty message to create.
  Loop ends on the command "QUIT"
  
  The zone to update is mandatory to specify on the command line with the --zone flag.
flags: |
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone update delname
use: delname
short: Delete every RRset at an owner name
long: |
  Delete all records at a name. At the zone apex, SOA and NS are retained --
  deleting those would dismantle the zone rather than a name within it.
  
    tdns-cli ... zone update delname --zone alpha.dnslab. --via api \
        --name foo.alpha.dnslab.
flags: |
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
      --name string     Owner name
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
      --via string      Transport: "api" or "ddns" (required)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone update delrr
use: delrr
short: Delete one specific RR from a zone
long: |
  Delete individual resource records. The RR must be given in full; the TTL is
  ignored, since it is not part of a record's identity.
  
    tdns-cli ... zone update delrr --zone alpha.dnslab. --via api \
        --rr "foo.alpha.dnslab. IN A 1.2.3.4"
flags: |
  -K, --key string       SIG(0) keyfile to use for signing (.private/.key basename)
      --rr stringArray   Resource record in presentation format (repeatable)
  -S, --server string    Server to send the update to (addr:port)
      --signer string    Name of signer (key used to sign the update; defaults to --zone)
      --via string       Transport: "api" or "ddns" (required)
  -z, --zone string      Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone update delrrset
use: delrrset
short: Delete an entire RRset (owner + type) from a zone
long: |
  Delete every record of one type at one owner name.
  
    tdns-cli ... zone update delrrset --zone alpha.dnslab. --via api \
        --name foo.alpha.dnslab. --type A
flags: |
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
      --name string     Owner name
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
      --type string     RR type to delete
      --via string      Transport: "api" or "ddns" (required)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone update from-file
use: from-file
short: Apply an ADD/DEL instruction file to a zone
long: |
  Apply a list of update instructions from a file.
  
    ADD foo.alpha.dnslab. 3600 IN A 1.2.3.4
    DEL bar.alpha.dnslab. IN A 5.6.7.8
  
  This is the format "zone journal purge" and "zone journal list --instructions"
  produce, so a file the server wrote can be edited down to the changes you want
  and fed straight back:
  
    tdns-cli ... zone update from-file --file /var/dns/zones/alpha.2026081704.purged \
        --zone alpha.dnslab. --via api
  
  Comments (";" or "#") and blank lines are ignored. Everything that survives is
  applied as ONE update — there is no half-applied outcome.
flags: |
      --file string     Instruction file to apply (required)
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
      --via string      Transport: "api" or "ddns" (required)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone update replacerrset
use: replacerrset
short: Atomically replace an RRset with the supplied RRs
long: |
  Replace an entire RRset in one transaction. The RRset to replace is inferred
  from the owner and type of the supplied records, so they must all describe the
  same one; mixed owners or types are refused rather than guessed at.
  
  This is a single atomic operation, not a delete followed by an add: the empty
  intermediate RRset is never published and no secondary can observe it.
  
    tdns-cli ... zone update replacerrset --zone alpha.dnslab. --via api \
        --rr "foo.alpha.dnslab. IN A 1.2.3.4" \
        --rr "foo.alpha.dnslab. IN A 2.3.4.5"
  
  To remove an RRset entirely, use delrrset -- replacerrset with no records is an
  error, not a silent delete.
flags: |
  -K, --key string       SIG(0) keyfile to use for signing (.private/.key basename)
      --rr stringArray   Resource record in presentation format (repeatable)
  -S, --server string    Server to send the update to (addr:port)
      --signer string    Name of signer (key used to sign the update; defaults to --zone)
      --via string       Transport: "api" or "ddns" (required)
  -z, --zone string      Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer auth zone write
use: write
short: Write a zone to disk
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone zonemd
use: zonemd
short: Inspect and verify a zone's ZONEMD (RFC 8976)
long: |
  A zone with the publish-zonemd option carries a message digest of its own
  contents at the apex, recomputed inside every publish.
  
  "status" reports what the zone publishes without recomputing anything.
  "verify" digests the zone and reports whether the published value describes it,
  which is what a recipient of an AXFR will do.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone zonemd status
use: status
short: Show the zone's published ZONEMD and its configuration
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer auth zone zonemd verify
use: verify
short: Recompute the zone's digest and compare it with what is published
long: |
  Digest the zone as it is currently served and compare the result with the
  apex ZONEMD, the way a recipient would.
  
  Costs one full pass over the zone, which for a large zone is not free; that is
  why it is a separate verb from "status".
flags: |
      --ignore-serial   digest against the serial each ZONEMD names rather than the SOA's (a diagnostic for a zone whose serial moved after it was digested, NOT a laxer check)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer config
use: config
short: Commands to reload config, reload zones, etc
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer config check
use: check [config-file]
short: Validate the "signer" daemon config and correlate it with the running daemon
long: |
  Validate a "signer" daemon configuration file for completeness and internal
  consistency, and (unless --offline) correlate it against the running daemon to
  report drift between the file on disk and what the server actually loaded.
  
  Target config resolution:
    1. an explicit path (positional arg or --serverconfig)
    2. otherwise the config-file: recorded for "signer" in the CLI config, else the compiled-in default for its app type
  
  Exit status is non-zero if any check FAILs (WARNs do not fail the run).
flags: |
      --offline               do not contact the daemon; run static checks only
      --serverconfig string   path to the "signer" daemon config file to check (default: ask the daemon, else the configured/compiled-in default)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer config mwe
use: mwe
short: Generate a minimal working example config (+ certs, zones) for tdns-signer
long: |
  Generate a Minimal Working Example configuration for tdns-signer.
  
  Writes a single self-contained YAML to <dir>/tdns-signer.yaml (or, if that
  file already exists, to <dir>/tdns-signer.yaml.mwe so nothing is
  clobbered). Also generates, unless already present:
  
    - a self-signed cert/key pair under <dir>/certs (for the API server and the
      encrypted DNS transports)
    - two example primary zones — one unsigned, one signed — using two different
      zone templates, with generated zone files under <dir>/zones
    - a commented-out secondary zone using a third template
  
  The DNS engine listens on IPv4 and IPv6 localhost; extra listen addresses are
  shown commented out. The generated tree is designed to pass
  `tdns-cli signer config check`.
flags: |
      --dir string   base directory for the generated config, certs and zones (default "/etc/tdns")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer config reload
use: reload
short: Send config reload command to tdns-signer
flags: |
      --confirm   apply even if the reload would strand a signed zone by a same-name DNSSEC policy algorithm change
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer config reload-tsig
use: reload-tsig
short: Reconcile keys.tsig into the TSIG keystore (config reload-tsig)
long: |
  Re-read keys.tsig from the config file and reconcile into the DB-backed
  TSIG keystore. Secret conflicts are withheld by default; use --force to
  overwrite all conflicts, or --interactive to prompt per conflict.
flags: |
      --force         overwrite all secret/algorithm conflicts with keys.tsig
      --interactive   prompt per conflict before overwriting
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer config reload-zones
use: reload-zones
short: Send reload-zones command to tdns-signer
flags: |
      --confirm   apply even if the reload would strand a signed zone by a same-name DNSSEC policy algorithm change
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer config status
use: status
short: Send config status command to tdns-signer
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer daemon
use: daemon
short: Only useful via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer daemon api
use: api
short: request an API summary from the tdns-signer daemon
long: |
  The daemon api command queries tdns-signer for the provided API
  and prints that out in a (hopefully) comprehensible fashion.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer daemon reload
use: reload
short: Reload config from file
long: |
  Reload config from file (the assumption is that something in the config has changed).
  Right now this doesn't do much, but later on various services will be able to restart.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer daemon restart
use: restart
short: Stop and then start the management daemon
flags: |
      --clear string   Truncate the specified log file before starting
      --update         Update the server binary from /tmp/{binary} to /usr/local/libexec/ before starting
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer daemon start
use: start
short: Start the tdns-signer daemon
long: |
  Start the tdns-signer daemon. If it was already running, then this is a no-op.
flags: |
  -W, --maxwait int   Max seconds to wait until declaring start to have failed (default 5)
      --slurp         Slurp stdout/stderr for errors (debug tool only)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer daemon status
use: status
short: Query for the status of the management daemon
long: |
  Query for the status of the management daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer daemon stop
use: stop
short: Stop the management daemon
long: |
  Stop the management daemon. If it was not running, then this is a no-op.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer debug
use: debug
short: Debug commands against the configured daemon
flags: |
      --qname string   qname of rrset to examine
      --qtype string   qtype of rrset to examine
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer debug lav
use: lav
short: Lookup and validate a child RRset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer debug rrset
use: rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer debug show-rrsetcache
use: show-rrsetcache
short: List cached RRsets
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer debug show-ta
use: show-ta
short: List known DNSSEC trust anchors
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer debug sig0
use: sig0
short: SIG(0) key helper commands (local)
flags: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
  -r, --rrtype string      rrtype to use for SIG(0)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer debug sig0 generate
use: generate
inherited: |
  -a, --algorithm string   Algorithm to use for SIG(0) (use the 'algorithms' subcommand to list what the server supports)
      --config string      config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug              debug output
  -H, --headers            show headers
  -Z, --pzone string       parent zone name
      --qname string       qname of rrset to examine
      --qtype string       qtype of rrset to examine
  -r, --rrtype string      rrtype to use for SIG(0)
  -v, --verbose            verbose output
  -z, --zone string        zone name

=== tdns-mpcli signer debug validate-rrset
use: validate-rrset
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer debug zone-txlog
use: zone-txlog
short: Show staged-but-unpublished zone changes
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
      --qname string    qname of rrset to examine
      --qtype string    qtype of rrset to examine
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer gossip
use: gossip
short: Gossip protocol commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer gossip state
use: state
short: Show gossip state matrix for a zone
long: |
  Display the N×N state matrix for the provider group serving a zone.
  Each row is a reporting peer; each column shows that reporter's
  view of another peer's state. A healthy group shows OPERATIONAL
  in every non-diagonal cell.
flags: |
  -z, --zone string   Zone name (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer jwt
use: jwt
short: JWT inspection and manipulation commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer jwt inspect
use: inspect [file|-]
short: Inspect JWT from CHUNK or JWK RDATA from JWK records
long: |
  Inspect JWT-wrapped payloads from CHUNK records, or decode JWK RDATA from JWK records.
  
  Reads DNS wire or text (dig-style) from stdin (or file). If the response contains a CHUNK RR,
  decodes and inspects the JWT (with optional verify/decrypt). If it contains a JWK RR,
  base64-decodes the RDATA and prints the JWK as pretty JSON.
  
  Examples:
    # Inspect CHUNK (JWT)
    dog node.dist123.kdc. CHUNK | tdns-cli jwt inspect -
  
    # Inspect JWK (pretty-print JSON)
    dog dns.agent.provider. JWK | tdns-cli jwt inspect -
  
    # With signature verification (CHUNK only)
    dog node.dist123.kdc. CHUNK | tdns-cli jwt inspect - --verify --verify-key /path/to/kdc.jose.pub
  
  Input format auto-detection:
    - DNS wire format (from dog/dig): CHUNK → JWT; JWK → base64-decode and pretty JSON
    - DNS text format (zone file / dig style): same
    - Base64-encoded JWT (starts with eyJ...)
    - Raw CHUNK RDATA
flags: |
      --backend string       Crypto backend: jose, auto (default: auto) (default "auto")
      --decrypt              Decrypt JWE payload (requires --decrypt-key)
      --decrypt-key string   Private key file for decryption (JWK JSON)
      --output string        Output format: text, json (default: text) (default "text")
  -v, --verbose              Verbose output (show all headers)
      --verify               Verify JWS signature (requires --verify-key)
      --verify-key string    Public key file for signature verification (JWK JSON or PEM)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli signer keys
use: keys
short: Generate long-term keypairs for agent/combiner (JOSE)
long: |
  Generate JOSE keypairs used by tdns-agent and tdns-mpcombiner for
  authenticated NOTIFY(CHUNK) and API traffic. Use one keypair per party:
    - Agent:  agent.jose.private (+ optional agent.jose.pub for combiner config)
    - Combiner: combiner.jose.private (+ optional combiner.jose.pub for agent config)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keys generate
use: generate [--jose] [--jose-outfile <path>] [--jose-pubfile <path>]
short: Generate JOSE keypair (P-256)
long: |
  Generate a JOSE keypair (P-256) for agent or combiner.
  
  Must specify --jose and --jose-outfile (private key path).
  
    --jose              Generate new JOSE keypair
    --jose-outfile      Path for JOSE private key file (required if --jose)
    --jose-pubfile      Optional: path to write public key (JWK) for the peer
                        e.g. agent.jose.pub on combiner, combiner.jose.pub on agent
  
  Examples:
    tdns-cli keys generate --jose --jose-outfile /etc/tdns/agent.jose.private --jose-pubfile /etc/tdns/agent.jose.pub
    tdns-cli keys generate --jose --jose-outfile /etc/tdns/combiner.jose.private --jose-pubfile /etc/tdns/combiner.jose.pub
  
  Then configure:
    - Agent:  long_term_jose_priv_key: /etc/tdns/agent.jose.private
             combiner.long_term_jose_pub_key: /etc/tdns/combiner.jose.pub
    - Combiner: long_term_jose_priv_key: /etc/tdns/combiner.jose.private
                agent_peer.long_term_jose_pub_key: /etc/tdns/agent.jose.pub
flags: |
      --jose                  Generate new JOSE keypair
      --jose-outfile string   Output path for JOSE private key (default: ./agent.jose.private)
      --jose-pubfile string   Optional: output path for JOSE public key (for peer config)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore
use: keystore
short: Prefix command to access different features of the keystore
long: |
  The keystore holds SIG(0), DNSSEC, and global TSIG keys.
  The CLI contains functions for listing, adding, deleting, and
  changing the state of keys.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore dnssec
use: dnssec
short: Prefix command, only usable via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore dnssec add
use: add
short: Add a new DNSSEC key pair to the keystore
long: |
  Add a new SIG(0) key pair to the keystore. Required arguments are the name of the file
  containing either the private or the public SIG(0) key and the name of the zone.
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to add DNSSEC key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec algorithms
use: algorithms
short: List the DNSSEC algorithms the server supports
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore dnssec bulk-convert
use: bulk-convert
short: Convert a directory of bind9 DNSSEC keys into one tdns can pre-load
long: |
  Convert bind9-generated DNSSEC keys in --dir into the form keystore.preload reads.
  
  Two things stand between dnssec-keygen output and a pre-loadable directory: the
  private half is in bind's own format rather than PKCS#8 PEM, and there is no
  manifest, without which pre-load refuses the directory outright.
  
  This rewrites each .private in place as PEM, keeping the original alongside as
  .private.orig, and writes the manifest. The .key files are NOT touched: they
  already hold the public RR in exactly the form tdns exports.
  
  Key state comes from bind's .state file (9.16+, dnssec-policy zones), mapped
  onto tdns's states. A key with no .state file has no state to read, and it is
  not guessed -- pass --state to say which one applies. Guessing wrong is how a
  retired key gets published again.
  
  Runs entirely locally: no daemon, no API, no keystore. That is the point --
  these directories are built and committed before the server that serves them
  exists. Re-running is safe; already-converted keys are left alone.
flags: |
      --dir string     Directory of bind9 key files to convert in place
      --no-backup      Do not keep the original bind-format key as .private.orig
      --state string   Key state for keys with no .state file (required only for those)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore dnssec bulk-export
use: bulk-export
short: Export DNSSEC keys from the keystore to a directory
long: |
  Export DNSSEC keys to --dest as BIND-convention key files plus a manifest
  that carries what those files cannot (key state, rollover timestamps).
  
  Selection is additive: --zone takes one exact name, --zones takes a name and
  everything below it, and both may be repeated. Subtree matching is on label
  boundaries, so --zones pq.dnslab does not match notpq.dnslab. With no selector at
  all, the whole class is exported.
  
  Re-exporting into a directory MERGES: entries for keys not covered by this
  run are left in place, so several exports can populate one directory.
flags: |
      --dest string         Directory to write keys and manifest to
      --format string       Private key format to write: pem (PKCS#8, as stored) or bind (Private-key-format v1.3) (default "pem")
  -z, --zone stringArray    Zone to include (repeatable)
      --zones stringArray   Include this zone and everything below it (repeatable)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec bulk-import
use: bulk-import
short: Import DNSSEC keys from a directory into the keystore
long: |
  Import every DNSSEC key described by --src/manifest.yaml.
  
  A key that is not in the keystore is inserted. A key that is already there
  with identical content is left alone. A key that is already there with
  DIFFERENT content is reported and SKIPPED: the running keystore outranks a
  file that may be arbitrarily stale, and silently overwriting it is how a stale
  export un-rolls a rolled key.
  
  --force flips that last case to overwrite, which is what you want when
  recovering a keystore you know to be wrong.
flags: |
      --force        Overwrite keystore entries that differ (default: report and skip them)
      --src string   Directory to read keys and manifest from
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore dnssec clear
use: clear
short: Permanently delete all DNSSEC keys for a zone (KeyStateWorker will regenerate as needed)
flags: |
      --force         Skip confirmation prompt
  -z, --zone string   Zone to clear all DNSSEC keys for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec delete
use: delete
short: Delete DNSSEC key pair from the keystore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore dnssec export
use: export
short: Export a DNSSEC key pair from the keystore as BIND-style .private/.key files
long: |
  Write the DNSSEC key pair for (zone, keyid) to two files in BIND filename
  convention: K<zone>+<alg-num>+<keyid>.private and .key (zone-file DNSKEY RR).
  The private half is written as PKCS#8 PEM, or as bind's Private-key-format
  v1.3 with --format bind. The resulting pair is directly consumable by
  'keystore dnssec import'.
flags: |
      --format string   Private key format to write: pem (PKCS#8, as stored) or bind (Private-key-format v1.3) (default "pem")
      --keyid int       Key ID of key to export
  -o, --outdir string   Directory to write .private and .key files to (default ".")
  -z, --zone string     Zone the key belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec gen-ds
use: gen-ds
short: Generate DS records for a zone's KSK(s) from the keystore
long: |
  Generate DS (Delegation Signer) records for a zone's KSK (Key Signing Key) DNSKEY records stored in the keystore. The command queries the keystore for DNSKEY records for the specified zone, filters for KSKs (keys with the SEP bit set), and generates DS records using SHA-256 and SHA-384 digest algorithms. If --keyid is not specified, DS records are generated for all KSKs in the zone.
flags: |
      --keyid int     Key ID of specific KSK to generate DS for (optional, if not specified, generates for all KSKs)
  -z, --zone string   Zone to generate DS records for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec generate
use: generate
short: Generate a new DNSSEC key pair and add it to the keystore
flags: |
  -a, --algorithm string   Algorithm to use for DNSSEC key generation (use the 'algorithms' subcommand to list what the server supports)
      --keytype string     Key type to generate (KSK|ZSK|CSK)
      --state string       Inital key state (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore dnssec import
use: import
short: Add a new DNSSEC key pair to the keystore
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to import DNSSEC key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec list
use: list
short: List all DNSSEC key pairs in the keystore
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore dnssec policies
use: policies
short: List the DNSSEC policies the server loaded (including any in error)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore dnssec policy-cleanup
use: policy-cleanup
short: Remove a zone's retired keys (and their RRSIGs) now, keeping active keys
long: |
  After a DNSSEC policy change, the old keys are retired but kept (with their
  signatures) so the zone stays validatable while the new keys take over —
  leaving the zone briefly double-signed. policy-cleanup collapses that window
  early: it removes the retired keys and strips their RRSIGs immediately,
  keeping the active keys. Unlike 'clear' (which deletes ALL keys and
  regenerates), this only touches retired keys.
  
  Accelerating removal means a resolver still caching only an old (now-removed)
  DNSKEY briefly cannot validate until it re-queries; the active keys already
  serve. Normally you can just wait for the KeyStateWorker to age the retired
  keys out after propagation-delay.
flags: |
      --force         Skip confirmation prompt
  -z, --zone string   Zone to clean up retired keys for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec purge
use: purge
short: Delete keys in 'removed' state, keeping the 3 most recent per zone
long: |
  Delete keys in 'removed' state from the keystore, keeping the 3
  most recent per zone (by insert order). Use --zone all to apply to
  every zone with removed keys at once.
  
  Dry-run by default: prints the keys that would be deleted and exits
  without modifying anything. Pass --force to actually delete.
flags: |
      --force         Actually delete; otherwise dry-run
  -z, --zone string   Zone to purge ('all' for every zone)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec rollover
use: rollover
short: Perform a manual DNSSEC key rollover (standby→active, active→retired)
flags: |
      --keytype string   Key type to roll over (ZSK|KSK) (default "ZSK")
  -z, --zone string      Zone to perform rollover for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore dnssec setstate
use: setstate
short: Set the state of and existing DNSSEC key pair in the keystore
flags: |
      --keyid int      Key ID of key to delete
      --state string   New statei of key
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore sig0
use: sig0
short: Prefix command, only usable via sub-commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore sig0 add
use: add
short: Add a new SIG(0) key pair to the keystore
long: |
  Add a new SIG(0) key pair to the keystore. Required arguments are the name of the file
  containing either the private or the public SIG(0) key and the name of the zone.
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to add SIG(0) key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore sig0 algorithms
use: algorithms
short: List the SIG(0) algorithms the server supports
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore sig0 bulk-convert
use: bulk-convert
short: Convert a directory of bind9 SIG(0) keys into one tdns can pre-load
long: |
  Convert bind9-generated SIG(0) keys in --dir into the form keystore.preload reads.
  
  Two things stand between dnssec-keygen output and a pre-loadable directory: the
  private half is in bind's own format rather than PKCS#8 PEM, and there is no
  manifest, without which pre-load refuses the directory outright.
  
  This rewrites each .private in place as PEM, keeping the original alongside as
  .private.orig, and writes the manifest. The .key files are NOT touched: they
  already hold the public RR in exactly the form tdns exports.
  
  Key state comes from bind's .state file (9.16+, dnssec-policy zones), mapped
  onto tdns's states. A key with no .state file has no state to read, and it is
  not guessed -- pass --state to say which one applies. Guessing wrong is how a
  retired key gets published again.
  
  Runs entirely locally: no daemon, no API, no keystore. That is the point --
  these directories are built and committed before the server that serves them
  exists. Re-running is safe; already-converted keys are left alone.
flags: |
      --dir string     Directory of bind9 key files to convert in place
      --no-backup      Do not keep the original bind-format key as .private.orig
      --state string   Key state for keys with no .state file (required only for those)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore sig0 bulk-export
use: bulk-export
short: Export SIG(0) keys from the keystore to a directory
long: |
  Export SIG(0) keys to --dest as BIND-convention key files plus a manifest
  that carries what those files cannot (key state, rollover timestamps).
  
  Selection is additive: --zone takes one exact name, --zones takes a name and
  everything below it, and both may be repeated. Subtree matching is on label
  boundaries, so --zones pq.dnslab does not match notpq.dnslab. With no selector at
  all, the whole class is exported.
  
  Re-exporting into a directory MERGES: entries for keys not covered by this
  run are left in place, so several exports can populate one directory.
flags: |
      --dest string         Directory to write keys and manifest to
      --format string       Private key format to write: pem (PKCS#8, as stored) or bind (Private-key-format v1.3) (default "pem")
  -z, --zone stringArray    Zone to include (repeatable)
      --zones stringArray   Include this zone and everything below it (repeatable)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore sig0 bulk-import
use: bulk-import
short: Import SIG(0) keys from a directory into the keystore
long: |
  Import every SIG(0) key described by --src/manifest.yaml.
  
  A key that is not in the keystore is inserted. A key that is already there
  with identical content is left alone. A key that is already there with
  DIFFERENT content is reported and SKIPPED: the running keystore outranks a
  file that may be arbitrarily stale, and silently overwriting it is how a stale
  export un-rolls a rolled key.
  
  --force flips that last case to overwrite, which is what you want when
  recovering a keystore you know to be wrong.
flags: |
      --force        Overwrite keystore entries that differ (default: report and skip them)
      --src string   Directory to read keys and manifest from
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore sig0 delete
use: delete
short: Delete SIG(0) key pair from the keystore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore sig0 export
use: export
short: Export a SIG(0) key pair from the keystore as BIND-style .private/.key files
long: |
  Write the SIG(0) key pair for (zone, keyid) to two files in BIND filename
  convention: K<zone>+<alg-num>+<keyid>.private and .key (zone-file KEY RR).
  The private half is written as PKCS#8 PEM, or as bind's Private-key-format
  v1.3 with --format bind. The resulting pair is directly consumable by commands
  accepting --key <basename.private>.
flags: |
      --format string   Private key format to write: pem (PKCS#8, as stored) or bind (Private-key-format v1.3) (default "pem")
      --keyid int       Key ID of key to export
  -o, --outdir string   Directory to write .private and .key files to (default ".")
  -z, --zone string     Zone the key belongs to
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore sig0 generate
use: generate
short: Generate a new SIG(0) key pair and add it to the keystore
flags: |
  -a, --algorithm string   Algorithm to use for SIG(0) key generation (use the 'algorithms' subcommand to list what the server supports)
      --state string       Inital key state (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore sig0 import
use: import
short: Add a new SIG(0) key pair to the keystore
flags: |
  -f, --file string   Name of file containing either pub or priv SIG(0) data
  -z, --zone string   Zone to import SIG(0) key for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer keystore sig0 list
use: list
short: List all SIG(0) key pairs in the keystore
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore sig0 setstate
use: setstate
short: Set the state of and existing SIG(0) key pair in the keystore
flags: |
      --keyid int      Key ID of key to delete
      --state string   New state of key (created|published|active|retired)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore tsig
use: tsig
short: Manage global TSIG keys in the keystore
long: |
  Global TSIG keystore (no --zone). Keys are DB-backed with origin=api
  for keys created here; config keys are managed via keys.tsig.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore tsig add
use: add
short: Add a TSIG key with a known secret
flags: |
      --algorithm string     HMAC algorithm (default "hmac-sha256")
      --force                Overwrite on secret/algorithm conflict
      --name string          TSIG key name
      --owner string         Owner label (default api) (default "api")
      --secret string        Inline TSIG secret (base64). WARNING: visible in shell history / process list; prefer --secret-file
      --secret-file string   File containing the base64 TSIG secret; preferred over --secret
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore tsig bulk-export
use: bulk-export
short: Export TSIG keys from the keystore to a directory
long: |
  Export TSIG keys to --dest as BIND-convention key files plus a manifest
  that carries what those files cannot (key state, rollover timestamps).
  
  Selection is additive: --key takes one exact name, --keys takes a name and
  everything below it, and both may be repeated. Subtree matching is on label
  boundaries, so --keys pq.dnslab does not match notpq.dnslab. With no selector at
  all, the whole class is exported.
  
  Re-exporting into a directory MERGES: entries for keys not covered by this
  run are left in place, so several exports can populate one directory.
flags: |
      --dest string        Directory to write keys and manifest to
      --key stringArray    TSIG key name to include (repeatable)
      --keys stringArray   Include this name and everything below it (repeatable)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore tsig bulk-import
use: bulk-import
short: Import TSIG keys from a directory into the keystore
long: |
  Import every TSIG key described by --src/manifest.yaml.
  
  A key that is not in the keystore is inserted. A key that is already there
  with identical content is left alone. A key that is already there with
  DIFFERENT content is reported and SKIPPED: the running keystore outranks a
  file that may be arbitrarily stale, and silently overwriting it is how a stale
  export un-rolls a rolled key.
  
  --force flips that last case to overwrite, which is what you want when
  recovering a keystore you know to be wrong.
flags: |
      --force        Overwrite keystore entries that differ (default: report and skip them)
      --src string   Directory to read keys and manifest from
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore tsig delete
use: delete
short: Delete an api-origin TSIG key
flags: |
      --name string   TSIG key name
  -y, --yes           Skip confirmation prompt
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore tsig export
use: export <keyname>
short: Print a TSIG key's secret (default) or a full BIND/NSD key block, to stdout
long: |
  Print a TSIG key's base64 secret to stdout with NO trailing newline and
  nothing else, so it can be captured inline in another command (e.g. via
  shell backticks or $(...)) to TSIG-sign a dog query or transfer:
  
      dog @srv -y name.:$(tdns-cli auth keystore tsig export name) zone. axfr
  
  With --bind or --nsd, print a complete BIND9 or NSD key block instead
  (still on stdout). Errors go to stderr so stdout stays clean for capture.
flags: |
      --bind   Output a complete BIND9 key { ... } block
      --nsd    Output a complete NSD key: block
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore tsig generate
use: generate
short: Generate a new TSIG key and add it to the keystore
flags: |
      --algorithm string   HMAC algorithm (default "hmac-sha256")
      --name string        TSIG key name
      --owner string       Owner label (default api) (default "api")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore tsig import
use: import
short: Import TSIG keys from a BIND or NSD config snippet
long: |
  Scan a config file for TSIG key blocks (not a full config parser).
  Default: import new keys and skip conflicts. --force overwrites all conflicts;
  --interactive prompts per conflict (two-phase round-trip).
flags: |
  -f, --file string     File containing TSIG key declarations
      --force           Overwrite all secret/algorithm conflicts
      --format string   Key syntax: bind or nsd (default "bind")
      --interactive     Prompt per conflict before overwriting
      --owner string    Owner label for imported keys (default "api")
  -v, --verbose         List per-key disposition
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -z, --zone string     zone name

=== tdns-mpcli signer keystore tsig list
use: list
short: List TSIG keys (no secrets)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore tsig purge
use: purge
short: Delete unreferenced api-origin TSIG keys owned by api
long: |
  Dry-run by default: lists purge candidates (origin=api, owner=api,
  zero zone references) and deletes nothing. Pass --force to delete all
  candidates, or --interactive to prompt per key.
flags: |
      --force         Actually delete; otherwise dry-run
      --interactive   Prompt per purge candidate
  -y, --yes           Skip confirmation when used with --force
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer keystore tsig setowner
use: setowner
short: Change owner on an api-origin TSIG key
flags: |
      --name string    TSIG key name
      --owner string   New owner label
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer peer
use: peer
short: Peer management commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer peer apiping
use: apiping
short: Ping a peer via HTTPS API
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer peer ping
use: ping
short: Ping a peer via DNS CHUNK
long: |
  Send a DNS CHUNK ping to a peer and report the result.
  The --id flag specifies the peer identity (e.g. agent.beta.dnslab.
  or combiner.dnslab.).
flags: |
      --id string   Peer identity to ping (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer peer reset
use: reset
short: Reset peer (agent/auditor only; no-op elsewhere)
long: |
  Reset a peer to initial NEEDED state. Flushes all IMR
  cache entries for the peer's discovery names and restarts
  discovery from scratch. Use this when a peer is stuck in UNKNOWN
  or KNOWN state. Only applicable to roles that use dynamic
  HSYNC3-driven discovery (agent, auditor); a no-op on
  static-peer roles (signer, combiner).
flags: |
      --id string   Peer identity to reset (required)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer ping
use: ping
short: Send an API ping request and present the response
flags: |
  -c, --count int   #pings to send
  -n, --newapi      use new api client
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer report
use: report <qname>
short: Send a report and (optionally) discover DSYNC via the internal resolver (imr)
flags: |
  -D, --details string   Report details
      --ede int          Manual override of EDE code (513-523 locally defined)
      --port int         Manual override of DSYNC port
  -S, --sender string    Report sender
      --target string    Manual override of DSYNC target
  -T, --tsig             TSIG sign the report (default true)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer router
use: router
short: DNS message router introspection commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer router describe
use: describe
short: Show detailed router state
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer router list
use: list
short: List all registered message handlers
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer router metrics
use: metrics
short: Show router metrics (use --detailed for per-peer breakdown)
flags: |
      --detailed   Show per-peer breakdown
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer router reset
use: reset
short: Reset router metrics
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer router walk
use: walk
short: Walk all handlers with visitor pattern
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer stop
use: stop
short: Send stop command to the daemon
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer truststore
use: truststore
short: Prefix command to access different features of the truststore
long: |
  The truststore is where SIG(0) public keys for child zones are kept.
  The CLI contains functions for listing trusted SIG(0) keys, adding and
  deleting child keys as well as changing the trust state of individual keys.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer truststore sig0
use: sig0
short: Prefix command, only usable via sub-commands
flags: |
  -c, --child string   Name of child SIG(0) key
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer truststore sig0 add
use: add
short: Add a new SIG(0) public key to the truststore
flags: |
  -s, --src string   Source for SIG(0) public key, a file name or 'dns'
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer truststore sig0 delete
use: delete
short: Delete a SIG(0) public key from the truststore
flags: |
      --keyid int   Key ID of key to delete
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer truststore sig0 list
use: list
short: List all child SIG(0) public key in the keystore
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer truststore sig0 trust
use: trust
short: Declare a child SIG(0) public key in the keystore as trusted
flags: |
      --keyid int   Keyid of child SIG(0) key to change trust for
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer truststore sig0 untrust
use: untrust
short: Declare a child SIG(0) public key in the keystore as untrusted
flags: |
      --keyid int   Keyid of child SIG(0) key to change trust for
inherited: |
  -c, --child string    Name of child SIG(0) key
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone
use: zone
short: Prefix command, not usable by itself
flags: |
  -F, --force   force operation
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone add
use: add
short: Add a dynamic zone at runtime: secondary, or template-constrained primary (persists across restart)
flags: |
      --options strings           Zone options (comma-separated; secondary zones — primaries take options from the template)
      --primaries strings         Primary (upstream) addresses [host:port], comma-separated (secondary zones)
      --primary-key string        Primary TSIG key name applied to all primaries (NOKEY for none) (default "NOKEY")
      --template string           Config template for a primary zone (must carry dynamiczones: true)
      --transfer-src strings      Source address(es) to bind for this zone's outbound transfers (IP only, one per family)
      --tsig-algo string          Inline TSIG algorithm (default hmac-sha256)
      --tsig-name string          Inline TSIG key name; created in keystore if absent and applied to keyless primaries (secondary) or keyless downstreams (primary)
      --tsig-secret string        Inline TSIG secret (base64). WARNING: visible in shell history / process list; prefer --tsig-secret-file
      --tsig-secret-file string   File containing the inline TSIG secret (base64); preferred over --tsig-secret
      --type string               Zone type: secondary (default) or primary (requires --template) (default "secondary")
  -z, --zone string               Zone to add
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone bump
use: bump
short: Bump SOA serial and epoch (if any) in the daemon's version of the zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone childsync
use: childsync
short: Parent-side child delegation sync commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone childsync advert
use: advert
short: Print what the parent primary still lacks of the DSYNC advertisement, as an nsupdate block
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone childsync proxy-status
use: proxy-status
short: Report a childsync-proxy's advertisement and push state
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone childsync publish
use: publish
short: Publish the DSYNC RRset into the zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone childsync reconcile
use: reconcile
short: Reconcile the advertisement and the known children against the served zone now
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone childsync unpublish
use: unpublish
short: Unpublish the DSYNC RRset from the zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone delegation
use: delegation
short: Inspect the delegation data a parent zone holds for its children
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone delegation get
use: get
short: Show what this parent publishes for a child (or list its children)
long: |
  Report the delegation records the parent currently holds for a child zone,
  grouped by owner name and type.
  
  With no --child, list the children this parent has delegation data for -- the
  question that comes first when reconciling an external store against the
  server.
  
  The answer comes from the zone's delegation backend, so it is what the SERVER
  considers current. For a backend that records delegations somewhere other than
  the served zone those differ deliberately, and reading the zone instead would
  quietly give the wrong answer.
flags: |
      --child string   Child zone to report on; omit to list the parent's children
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone delete
use: delete
short: Delete a dynamic (API-managed) zone
flags: |
  -z, --zone string   Zone to delete
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone desc
use: desc
short: Describe a single zone in full, including DNSSEC applied-policy state and policy detail
long: |
  Print a detailed, multi-line record for one zone: everything "zone list -v"
  shows (type, store, options, primaries, notify, error/warning state, effective
  DNSSEC policy and override info) plus two DNSSEC sections not otherwise visible
  from the CLI:
  
    1. the last-applied policy record recorded in the keystore (applied policy
       name, source config|command, and when it was applied); and
    2. the currently-bound policy's detail — mode, KSK/ZSK (or CSK) algorithms,
       key lifetimes and RRSIG validity.
  
  An unsigned zone or an unresolvable policy degrades gracefully rather than
  failing. Does not change anything; read-only.
flags: |
  -z, --zone string   Zone to describe
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone dnssec
use: dnssec
short: Zone DNSSEC operations: signing, policy, and automated rollover
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dnssec auto-rollover
use: auto-rollover
short: Manage and inspect automated KSK rollover (scheduled + manual-ASAP)
long: |
  Subcommands operate on local keystore state for a zone:
  
    when      — compute the earliest safe rollover moment (no state change)
    asap      — schedule a manual rollover at that earliest moment
    cancel    — clear a pending manual rollover request
    status    — print phase + per-key state for the zone
    reset     — clear last_rollover_error on one key after operator action
    unstick   — skip the softfail-delay and probe the parent on the next tick
    validate  — re-parse policy from YAML and report which §4 invariants pass/fail
flags: |
      --ksk   Render only the KSK section (status / when); ignored by other subcommands
      --zsk   Render only the ZSK section (status / when); ignored by other subcommands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dnssec auto-rollover asap
use: asap
short: Schedule a manual KSK rollover at the earliest safe moment
long: |
  Asks the daemon to compute ComputeEarliestRollover and persist
  manual_rollover_* on the zone row. The rollover worker fires
  AtomicRollover when t_earliest is reached. Rejects the request if a
  rollover is already in progress or the pipeline has no standby SEP key.
  
  Online-only: scheduling against a stopped daemon is meaningless
  (the manual_rollover_* row would never be read).
flags: |
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli signer zone dnssec auto-rollover cancel
use: cancel
short: Cancel a pending manual KSK rollover request
long: |
  Asks the daemon to clear manual_rollover_requested_at and
  manual_rollover_earliest on the zone row. Has no effect on rollovers
  that have already fired or on scheduled (lifetime-driven) rollovers.
  
  Online-only: cancelling against a stopped daemon is meaningless (the
  manual_rollover_* row isn't being read by anything).
flags: |
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli signer zone dnssec auto-rollover reset
use: reset
short: Clear last_rollover_error for one key (after operator intervention)
long: |
  Asks the daemon to clear the last_rollover_error column on a single
  key's RolloverKeyState row. Use after diagnosing and fixing a
  hard-failed rollover so status output isn't misleading.
  
  Default mode talks to the daemon's API server. Use --offline to write
  directly to the keystore file when the daemon is down (postmortem
  use). The CLI checks the daemon-sentinel row in the keystore and
  refuses to run --offline if it sees a live daemon process; pass
  --force to override (you must ensure the daemon is genuinely
  stopped first).
flags: |
      --force         With --offline: override the daemon-alive check
      --keyid int     Key ID to reset (RFC 4034 keytag)
      --offline       Write directly to keystore file (postmortem use; daemon is down)
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli signer zone dnssec auto-rollover status
use: status
short: Print rollover state for a zone (KSK and ZSK)
long: |
  Prints rollover state for the zone with an OK / ACTIVE / SOFTFAIL
  headline and per-key tables for KSKs and ZSKs.
  
  Default mode talks to the daemon's API server (no daemon config needed
  on the CLI host). Use --offline to render against the keystore file
  when the daemon is down — that requires --config with the daemon's
  config file so the CLI can find db.file and the zone's policy.
  
  Use --ksk or --zsk to print only the KSK block or only the ZSK block
  (the two flags are mutually exclusive). These flags are inherited
  from the auto-rollover parent and accepted by every subcommand for
  consistency.
  
  The DS range line lists SEP keyids (same numbering as the KSK table and
  as DS digest key tags at the parent).
  
  Use -v / --verbose to show rollover_index spans behind the keyid lists
  and the policy summary.
flags: |
      --offline       Render against keystore file (postmortem use; daemon is down)
  -v, --verbose       Show full last_error text and policy summary
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli signer zone dnssec auto-rollover unstick
use: unstick
short: Skip the softfail-delay and probe the parent on the next tick
long: |
  Asks the daemon to clear next_push_at on the zone row so the
  rollover engine fires a probe UPDATE on its very next tick instead of
  waiting out the rest of the softfail-delay window. Operator override
  for "I just fixed the parent and want to retry now."
  
  Operationally optional: the engine polls the parent continuously
  regardless of softfail-delay, so a parent fix is auto-detected within
  confirm-poll-max even without 'unstick'. Use only to skip the wait.
  
  Default mode talks to the daemon's API server. Use --offline to write
  directly to the keystore file when the daemon is down (postmortem
  use). The CLI checks the daemon sentinel via refuseIfDaemonAlive
  and refuses to run if a live daemon is detected; --force overrides
  the check for cases where the sentinel is stale.
  
  Hardfail_count and last_softfail_* are preserved so status output
  still shows the most recent failure context. The counter resets to 0
  on the next successful confirmed observation.
  
  Differs from 'reset' (which clears last_rollover_error for one keyid).
flags: |
      --force         With --offline: override the daemon-alive check
      --offline       Write directly to keystore file (postmortem use; daemon is down)
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli signer zone dnssec auto-rollover validate
use: validate
short: Validate a zone's rollover policy against §4 cache-flush invariants
long: |
  Re-parses the YAML config the daemon is running and runs every §4
  cache-flush invariant check (E5, E10, E11) against the dnssecpolicy
  attached to the named zone. Reports PASS / FAIL / WARN per invariant
  with operator-actionable suggestions.
  
  Online (default): contacts the daemon for its config-file path and the
  zone's active policy name, then reads the YAML directly. Useful for
  checking a candidate change before reload.
  
  Offline (--serverconfig PATH): skips the daemon and reads PATH. The
  zone's policy must be either supplied via --policy or inferable from
  the YAML's zones: block (when --zone matches a configured zone).
  
  DS_TTL handling: the runtime engine uses the parent's observed DS
  RRset TTL. validate doesn't have that observation, so it uses the
  ttls.parent-ds policy override if set, OR --parent-ds-ttl <duration> if
  supplied. Without either, E10/E11 are skipped (and the report says
  so).
flags: |
      --parent-ds-ttl string   Hypothetical parent DS RRset TTL (e.g. 1h) for E10/E11; overrides ttls.parent-ds
      --policy string          Override the dnssecpolicy name to validate (offline mode only)
      --serverconfig string    Read this YAML file instead of asking the daemon (offline)
  -z, --zone string            Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli signer zone dnssec auto-rollover when
use: when
short: Compute the earliest moment a rollover could safely fire (no state change)
long: |
  Asks the running daemon when the next rollover will fire and the
  earliest it could fire if requested. Side-effect free; does not request
  a rollover. Use 'auto-rollover asap' to actually schedule one.
  
  Reports the KSK schedule by default (parent-DS gated). Use --zsk for the
  ZSK schedule (zone-local, no parent gates — bounded only by standby
  readiness), or --ksk to be explicit. The two flags are mutually exclusive.
  
  Default mode talks to the daemon's API server (no daemon config needed
  on the CLI host). Use --offline to compute locally against the keystore
  file when the daemon is down — this requires --config with the daemon's
  config file so the CLI can find db.file and the zone's policy.
flags: |
      --offline       Compute locally against keystore file (postmortem use; daemon is down)
  -z, --zone string   Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
      --ksk             Render only the KSK section (status / when); ignored by other subcommands
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
      --zsk             Render only the ZSK section (status / when); ignored by other subcommands

=== tdns-mpcli signer zone dnssec nsec
use: nsec
short: Prefix command, not usable by itself
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dnssec nsec show
use: show
short: Show the NSEC chain for a zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dnssec policy-change
use: policy-change
short: Bind a zone to a new DNSSEC policy for a gradual ZSK algorithm rollover
long: |
  Bind a zone toward a new DNSSEC policy so its ZSK algorithm rolls over
  GRADUALLY. Unlike "zone dnssec policy-set" (which retires the old key
  synchronously — unsafe for an algorithm change), this only sets the
  algorithm of FUTURE-generated ZSKs: the existing FIFO key pipeline drains
  in order, oldest first.
  
  This command does NOT perform the roll. After binding, the algorithm
  rolls on the normal ZSK cadence — OR run
  
    auto-rollover asap -z <zone> --zsk
  
  to promote the next standby now (repeat to accelerate through the
  already-propagated old-alg standbys to the new algorithm).
  
  Requires dnssec.completeness: relaxed. A KSK / CSK / both-role algorithm
  change, or a second policy-change while a roll is in flight, is refused.
flags: |
  -p, --policy string   Target DNSSEC policy name
  -z, --zone string     Zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone dnssec policy-reset
use: policy-reset
short: Reset a zone's DNSSEC keys to its config policy, per role (dry-run without --confirm)
long: |
  Force a zone onto its config dnssec_policy: for each key role whose algorithm no
  longer matches config, drop and regenerate that role's keys and re-sign; any
  role whose algorithm is already correct is kept. It also clears the runtime
  override and records the config policy as applied.
  
  This is a break-glass tool for test/lab zones. It exists because an abrupt
  policy switch that changes a KSK/ZSK algorithm is otherwise refused (that needs
  a key rollover that is not built). If the KSK algorithm changes it BREAKS THE
  CHAIN OF TRUST: the parent DS will not match the new KSK until you re-publish it
  (a ZSK-only change keeps the KSK and the DS). NOT for production.
  
  Run WITHOUT --confirm for a DRY RUN that previews what it would do (which roles
  would roll, whether the parent DS would break) and changes nothing; add
  --confirm to apply. A single --zone only (no wildcards, no bulk).
flags: |
      --confirm       Apply the reset; without it the command is a dry-run that only previews what would happen
  -z, --zone string   Zone to reset the DNSSEC policy for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone dnssec policy-set
use: policy-set
short: Set a zone's DNSSEC policy at runtime (persists as an override, not in YAML)
long: |
  Apply a DNSSEC policy to a zone in the running server. The change is stored
  as a per-zone override in the keystore and survives restart, but does NOT
  update the zone's dnssec_policy in the YAML config — update that separately
  to make the new policy the permanent base. If the new policy uses different
  key algorithms, the old keys are retired (their signatures kept until the
  KeyStateWorker removes them) and new keys take over; the zone stays signed
  throughout.
flags: |
  -p, --policy string   DNSSEC policy name to apply
  -z, --zone string     Zone to set the DNSSEC policy for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone dnssec resign
use: resign
short: Re-sign zone from scratch with currently-active keys (drops all existing RRSIGs)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dnssec sign
use: sign
short: Request signing of a zone (additive: cover gaps with active keys)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dsync
use: dsync
short: Prefix command, not useable by itself
hidden: true
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dsync bootstrap-sig0-key
use: bootstrap-sig0-key
short: Send dsync bootstrap command
flags: |
  -a, --algorithm string   Algorithm for the new SIG(0) key (use the 'algorithms' subcommand to list what the server supports)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dsync publish
use: publish
short: Send dsync publish-dsync-rrset command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dsync roll-sig0-key
use: roll-sig0-key
short: Send dsync rollover command
flags: |
  -a, --algorithm string   Algorithm for the new SIG(0) key (use the 'algorithms' subcommand to list what the server supports)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dsync status
use: status
short: Send dsync status command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone dsync unpublish
use: unpublish
short: Send dsync unpublish-dsync-rrset command
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone freeze
use: freeze
short: Freeze a zone (i.e. stop accepting DDNS updates to the zone data)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone journal
use: journal
short: Inspect and manage a zone's delta journal
long: |
  The delta journal holds zone changes that have not yet reached the zone file.
  
  It is written when an update is published and replayed over the file on load, so
  a restart loses nothing. "zone sync" folds it into the file and empties it.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone journal list
use: list
short: List the journal's deltas and the records in them
flags: |
      --instructions   Print as replayable ADD/DEL instructions instead of a summary
      --out string     Write the instruction form to this file instead of stdout
  -z, --zone string    Zone name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone journal purge
use: purge
short: Discard the whole journal, saving its content first
long: |
  Discard every delta, after writing what they hold to {zonefile}.{serial}.purged
  as ADD/DEL instructions. Nothing is lost silently: replay what you want back with
  
    tdns-cli ... zone update from-file --file {zonefile}.{serial}.purged --zone <zone> --via api
  
  A journal that would replay cleanly holds changes that exist nowhere else, so
  purging one needs --force. Prefer "zone sync", which folds the same changes into
  the zone file and loses nothing. A journal that would NOT replay needs no flag:
  its changes are already absent from what the zone serves.
flags: |
      --force         Discard a journal that would otherwise replay cleanly
      --out string    Write the instruction form to this file instead of stdout
  -z, --zone string   Zone name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone journal status
use: status
short: Summarise the journal, and whether it will replay on restart
flags: |
  -z, --zone string   Zone name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone journal truncate
use: truncate
short: Drop the journal's tail, keeping the chain through --serial
long: |
  Keep the journal up to and including the delta that ends at --serial, drop the rest.
  
  A journal is a chain: each delta continues where the previous one ended, and a
  chain with a gap cannot be replayed at all. So only a suffix can be removed —
  there is deliberately no way to delete one record from the middle. To undo a
  single record, append a correction with an ordinary "zone update" instead.
flags: |
      --serial uint32   Keep the chain through the delta ending at this serial (required)
  -z, --zone string     Zone name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone list
use: list
short: List configured zones
flags: |
  -f, --file      Show zone input file
  -N, --notify    Show zone downstream notify addresses
  -P, --primary   Show zone primary nameserver
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone list-dynamic
use: list-dynamic
short: List dynamic zones (catalog members + API-managed) and their provisioning state
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone modify
use: modify
short: Modify a dynamic (API-managed) zone's primary or options
flags: |
      --options strings           Zone options (comma-separated)
      --primaries strings         New primary (upstream) addresses [host:port], comma-separated
      --primary-key string        New primary TSIG key name applied to all primaries (NOKEY for none) (default "NOKEY")
      --transfer-src strings      Replace the zone's outbound-transfer source address(es); omit to leave unchanged
      --tsig-algo string          Inline TSIG algorithm (default hmac-sha256)
      --tsig-name string          Inline TSIG key name; created in keystore if absent (existing keys cannot be rotated here)
      --tsig-secret string        Inline TSIG secret (base64). WARNING: visible in shell history / process list; prefer --tsig-secret-file
      --tsig-secret-file string   File containing the inline TSIG secret (base64); preferred over --tsig-secret
  -z, --zone string               Zone to modify
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone mplist
use: mplist
short: List multi-provider zones with HSYNCPARAM details
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone name
use: name
short: Inspect what a zone publishes at a single owner name
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone name get
use: get
short: Show the RRsets this zone holds at one name
long: |
  Report the RRsets the zone currently publishes at one owner name, grouped by
  type, in presentation format.
  
  Reports the PUBLISHED view, not staged data: the question is what the server
  serves, and an update still in flight is not yet an answer to it.
  
  A name with nothing at it is an empty report, not an error -- that is what
  provisioning a name for the first time returns, and it has to be
  distinguishable from a read that failed. A name outside the zone IS an error,
  because answering "nothing" there reads as "that name has no records".
  
  RRSIG, NSEC, NSEC3, NSEC3PARAM and ZONEMD are never reported. They are the
  signer's and the zone policy's, and returning them would invite a
  read-modify-write that tries to author them.
flags: |
      --name string   Owner name to report on (required; must be inside the zone)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone parentsync
use: parentsync
short: Child-side parent delegation sync commands
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone parentsync bootstrap
use: bootstrap
short: Bootstrap the SIG(0) key with the parent
flags: |
      --scheme string   Bootstrap scheme (only "update" is implemented) (default "update")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone parentsync delta
use: delta
short: Compute delta between parent delegation data and child zone data
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone parentsync inquire
use: inquire
short: Inquire the parent about the current SIG(0) key state
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone parentsync inquire update
use: update
short: Deprecated spelling of "parentsync inquire"
hidden: true
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone parentsync roll-key
use: roll-key
short: Roll the SIG(0) key with the parent
flags: |
  -a, --algorithm string   Algorithm for the new SIG(0) key (use the 'algorithms' subcommand to list what the server supports)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone parentsync status
use: status
short: Show parentsync status for the zone
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone parentsync sync
use: sync
short: Sync delegation data in parent zone via DDNS UPDATE
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone proxy-key
use: proxy-key
short: Show the delegation-sync-proxy UPDATE state and the KEY to publish at the primary
long: |
  For a zone with the delegation-sync-proxy option (a tdns-agent acting as a
  secondary for a DSYNC-unaware primary), report whether the agent can proxy
  DNS UPDATEs to the parent, and print the exact records to serve at the primary
  apex: the agent's KEY RR, an HSYNCPARAM pubkey flag, and the HSYNCPARAM again
  in RFC 3597 form for a primary that cannot parse the private type.
  
  The records are printed in every state, so that what the agent holds can be
  compared against what the primary actually serves — including in ready, where
  the two are supposed to agree. In foreign-key the KEY found at the apex is
  listed alongside them. Read-only: no key is generated by running this.
  States: update-unsupported / ready / foreign-key / waiting-for-key.
flags: |
  -z, --zone string   Zone to report proxy-key state for
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone readfake
use: readfake
short: Create a fake zone from a compiled in string
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone reload
use: reload
short: Request re-loading a zone
flags: |
  -e, --error            wait for reload to complete and report any parse errors
      --timeout string   how long to wait for reload when --error is set (e.g. 10s, 2m) (default "10s")
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone sync
use: sync
short: Spool the zone out to disk (alias for write, no freeze/thaw)
long: |
  Write the zone's current content to its file, without freezing it.
  
  Identical to "zone write". Both fold any persisted change deltas into the file
  and drop them, so the file becomes the whole truth again.
  
  Use "zone freeze" instead when you want the file to be authoritative and
  updates refused until you thaw.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone thaw
use: thaw
short: Thaw a zone (i.e. accept DDNS updates to the zone data again)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone update
use: update
short: Create and ultimately send a DNS UPDATE msg for zone auth data
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone update addrr
use: addrr
short: Add one or more RRs to a zone
long: |
  Add resource records to a primary zone.
  
    tdns-cli ... zone update addrr --zone alpha.dnslab. --via api \
        --rr "foo.alpha.dnslab. 3600 IN A 1.2.3.4"
flags: |
  -K, --key string       SIG(0) keyfile to use for signing (.private/.key basename)
      --rr stringArray   Resource record in presentation format (repeatable)
  -S, --server string    Server to send the update to (addr:port)
      --signer string    Name of signer (key used to sign the update; defaults to --zone)
      --via string       Transport: "api" or "ddns" (required)
  -z, --zone string      Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone update create
use: create
short: Create and ultimately send a DNS UPDATE msg for zone auth data
long: |
  Will query for details about the DNS UPDATE via (add|del|show|set-ttl) commands.
  When the message is complete it may be signed and sent by the 'send' command. After a
  message has been send the loop will start again with a new, empty message to create.
  Loop ends on the command "QUIT"
  
  The zone to update is mandatory to specify on the command line with the --zone flag.
flags: |
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone update delname
use: delname
short: Delete every RRset at an owner name
long: |
  Delete all records at a name. At the zone apex, SOA and NS are retained --
  deleting those would dismantle the zone rather than a name within it.
  
    tdns-cli ... zone update delname --zone alpha.dnslab. --via api \
        --name foo.alpha.dnslab.
flags: |
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
      --name string     Owner name
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
      --via string      Transport: "api" or "ddns" (required)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone update delrr
use: delrr
short: Delete one specific RR from a zone
long: |
  Delete individual resource records. The RR must be given in full; the TTL is
  ignored, since it is not part of a record's identity.
  
    tdns-cli ... zone update delrr --zone alpha.dnslab. --via api \
        --rr "foo.alpha.dnslab. IN A 1.2.3.4"
flags: |
  -K, --key string       SIG(0) keyfile to use for signing (.private/.key basename)
      --rr stringArray   Resource record in presentation format (repeatable)
  -S, --server string    Server to send the update to (addr:port)
      --signer string    Name of signer (key used to sign the update; defaults to --zone)
      --via string       Transport: "api" or "ddns" (required)
  -z, --zone string      Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone update delrrset
use: delrrset
short: Delete an entire RRset (owner + type) from a zone
long: |
  Delete every record of one type at one owner name.
  
    tdns-cli ... zone update delrrset --zone alpha.dnslab. --via api \
        --name foo.alpha.dnslab. --type A
flags: |
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
      --name string     Owner name
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
      --type string     RR type to delete
      --via string      Transport: "api" or "ddns" (required)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone update from-file
use: from-file
short: Apply an ADD/DEL instruction file to a zone
long: |
  Apply a list of update instructions from a file.
  
    ADD foo.alpha.dnslab. 3600 IN A 1.2.3.4
    DEL bar.alpha.dnslab. IN A 5.6.7.8
  
  This is the format "zone journal purge" and "zone journal list --instructions"
  produce, so a file the server wrote can be edited down to the changes you want
  and fed straight back:
  
    tdns-cli ... zone update from-file --file /var/dns/zones/alpha.2026081704.purged \
        --zone alpha.dnslab. --via api
  
  Comments (";" or "#") and blank lines are ignored. Everything that survives is
  applied as ONE update — there is no half-applied outcome.
flags: |
      --file string     Instruction file to apply (required)
  -K, --key string      SIG(0) keyfile to use for signing (.private/.key basename)
  -S, --server string   Server to send the update to (addr:port)
      --signer string   Name of signer (key used to sign the update; defaults to --zone)
      --via string      Transport: "api" or "ddns" (required)
  -z, --zone string     Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone update replacerrset
use: replacerrset
short: Atomically replace an RRset with the supplied RRs
long: |
  Replace an entire RRset in one transaction. The RRset to replace is inferred
  from the owner and type of the supplied records, so they must all describe the
  same one; mixed owners or types are refused rather than guessed at.
  
  This is a single atomic operation, not a delete followed by an add: the empty
  intermediate RRset is never published and no secondary can observe it.
  
    tdns-cli ... zone update replacerrset --zone alpha.dnslab. --via api \
        --rr "foo.alpha.dnslab. IN A 1.2.3.4" \
        --rr "foo.alpha.dnslab. IN A 2.3.4.5"
  
  To remove an RRset entirely, use delrrset -- replacerrset with no records is an
  error, not a silent delete.
flags: |
  -K, --key string       SIG(0) keyfile to use for signing (.private/.key basename)
      --rr stringArray   Resource record in presentation format (repeatable)
  -S, --server string    Server to send the update to (addr:port)
      --signer string    Name of signer (key used to sign the update; defaults to --zone)
      --via string       Transport: "api" or "ddns" (required)
  -z, --zone string      Zone to update
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output

=== tdns-mpcli signer zone write
use: write
short: Write a zone to disk
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone zonemd
use: zonemd
short: Inspect and verify a zone's ZONEMD (RFC 8976)
long: |
  A zone with the publish-zonemd option carries a message digest of its own
  contents at the apex, recomputed inside every publish.
  
  "status" reports what the zone publishes without recomputing anything.
  "verify" digests the zone and reports whether the published value describes it,
  which is what a recipient of an AXFR will do.
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone zonemd status
use: status
short: Show the zone's published ZONEMD and its configuration
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli signer zone zonemd verify
use: verify
short: Recompute the zone's digest and compare it with what is published
long: |
  Digest the zone as it is currently served and compare the result with the
  apex ZONEMD, the way a recipient would.
  
  Costs one full pass over the zone, which for a large zone is not free; that is
  why it is a separate verb from "status".
flags: |
      --ignore-serial   digest against the serial each ZONEMD names rather than the SOA's (a diagnostic for a zone whose serial moved after it was digested, NOT a laxer check)
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -F, --force           force operation
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

=== tdns-mpcli version
use: version
short: Print the version of the app, more or less verbosely
inherited: |
      --config string   config file (default is $TDNS_MPCLI_CONFIG, then /etc/tdns/tdns-mpcli.yaml)
  -d, --debug           debug output
  -H, --headers         show headers
  -Z, --pzone string    parent zone name
  -v, --verbose         verbose output
  -z, --zone string     zone name

